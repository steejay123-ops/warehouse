# گزارش بازبینی پروژه warehouse-front

تاریخ بازبینی: ۲۰۲۶/۰۷/۳۰ — دامنه: فقط `warehouse-front` (پوشه `Archive_Docs` بررسی نشد)
روش: مطالعه مستقیم فایل‌های `src`، `angular.json`، `tsconfig.json` و `package.json`. هر مورد با مسیر فایل و شماره خط ذکر شده و همه موارد از روی خطِ واقعی کد تأیید شده‌اند.

جمع‌بندی کوتاه: ساختار پروژه سالم و مدرن است (Angular 22، کامپوننت‌های standalone، سیگنال‌ها، اینترسپتور خطای متمرکز، RTL و فونت فارسی درست پیاده شده). مشکل اصلی، آمادگی برای استقرار واقعی است: آدرس بک‌اند و WebSocket روی `localhost` قفل شده، نگاشت مجوزها بین منو و گارد ناهمخوان است، و لایه پاک‌سازی اشتراک‌ها (subscription) در بیشتر کامپوننت‌ها وجود ندارد.

---

## بخش ۱ — موارد بحرانی (قبل از استقرار باید حل شوند)

**۱. آدرس API در بیلد پروداکشن هم `localhost` است.**
`src/environments/environment.ts:7` مقدار `apiUrl: 'http://localhost:8000/api'` دارد و دقیقاً همان مقدار در `environment.development.ts:7` تکرار شده. چون در `angular.json:51-56` جایگزینی فایل فقط برای حالت `development` تعریف شده، بیلد پروداکشن همان فایل اول را می‌گیرد. یعنی خروجی نهایی روی هر سروری اجرا شود، به `localhost:8000` درخواست می‌زند. پیشنهاد: `environment.ts` به آدرس واقعی سرور تغییر کند، یا بهتر از آن، مقدار به‌صورت نسبی (`/api`) نوشته شود تا با reverse proxy کار کند و وابسته به دامنه نباشد.

**۲. آدرس WebSocket به‌کلی از environment رد شده است.**
`src/app/core/http/websocket.service.ts:19` مقدار `'ws://localhost:8000/ws/notifications/'` را مستقیم داخل کد دارد. حتی اگر مورد ۱ را درست کنید، اعلان‌های لحظه‌ای در پروداکشن کار نمی‌کنند. ضمناً کامنت بالای همان خط از `wss://` برای HTTPS حرف می‌زند ولی منطقش نوشته نشده. پیشنهاد: آدرس از `environment` خوانده شود و پروتکل بر اساس `location.protocol` انتخاب شود.

**۳. حلقه اتصال مجدد WebSocket بی‌پایان و بدون کنترل است.**
در `websocket.service.ts:40-44` رویداد `onclose` هر ۵ ثانیه دوباره `connect()` صدا می‌زند. چون `disconnect()` (خط ۴۷) فقط `close()` می‌کند و همان `onclose` را فعال می‌کند، بعد از خروج کاربر هم تلاش برای اتصال ادامه پیدا می‌کند. پیشنهاد: یک فلگ `isManualClose` اضافه شود و backoff نمایی با سقف تعداد تلاش گذاشته شود.

**۴. رمز پیش‌فرض کاربر جدید، کد ملی خودش است.**
`src/app/components/users/users.ts:301` — `(payload as any).password = payload.national_code || '123456'`. کد ملی اطلاعات محرمانه‌ای نیست و در همان فرم قابل مشاهده است؛ عملاً یعنی رمز اولیه هر کاربر برای همه همکارانش قابل حدس است. اگر بک‌اند `requires_password_change` را اجباری می‌کند ریسک کمتر است، ولی همچنان بهتر است تولید رمز تصادفی به بک‌اند سپرده شود.

**۵. ناهمخوانی مجوز منو و گارد مسیر — بعضی کاربران به صفحه‌ای که در منو می‌بینند راه پیدا نمی‌کنند.**
در `layout.ts:88` آیتم «صدور فایل برای تغذیه» با مجوز `view_wh_export` نمایش داده می‌شود، ولی `core/auth/auth.guard.ts:16` برای مسیر `export` فقط `view_sys_export` را می‌پذیرد. کاربری که فقط مجوز انبار دارد، آیتم را می‌بیند، کلیک می‌کند و بی‌صدا به صفحه دیگری پرت می‌شود. مورد مشابه: آیتم `count-tracking` در `layout.ts:72,85` با مجوز `view_sys_manager_review` کنترل می‌شود (احتمالاً کپی‌شده) و آیتم `customs` در منوی سیستمی (`layout.ts:69`) مجوز انباری `view_wh_customs` را می‌خواهد.

**۶. چند مسیر اصلاً گارد مجوز ندارند.**
`ROUTE_PERMISSIONS` در `auth.guard.ts:8-28` برای این مسیرها ورودی ندارد: `wh-settings`، `field`، `count-tracking`، `placeholders`، `tasks`، `approvals`. یعنی هر کاربر لاگین‌شده با تایپ مستقیم آدرس می‌تواند وارد «تنظیمات انبار» شود. برعکس، سه ورودی `id-cards`، `label-designer` و `recounts` در همان نگاشت هستند ولی هیچ مسیری در `app.routes.ts` ندارند (این دو کامپوننت به‌صورت جزء داخلی در `users` و `settings/dispatch/wh-settings` استفاده می‌شوند).

**۷. مسیر جایگزین در گارد می‌تواند کاربر را به مسیر ناموجود بفرستد.**
`auth.guard.ts:54-58` وقتی کاربر مجوز صفحه فعلی را ندارد، اولین کلید `ROUTE_PERMISSIONS` که کاربر مجوزش را دارد پیدا می‌کند و به آن هدایت می‌کند. اگر آن کلید `id-cards`، `label-designer` یا `recounts` باشد، مسیر وجود ندارد و wildcard خط ۷۶ فایل `app.routes.ts` کاربر را به `login` می‌فرستد — یعنی کاربر لاگین‌شده ناگهان بیرون انداخته می‌شود. همچنین خط ۶۰ همان فایل، در حالت «هیچ مجوزی ندارد» به `/login` می‌رود که تجربه گیج‌کننده‌ای است؛ بهتر است یک صفحه «دسترسی ندارید» ساخته شود.

---

## بخش ۲ — پاسخ به سؤال دوم: جاهایی که هنوز هارد‌کد است

### ۲-۱. آدرس‌ها و پیکربندی زیرساخت

| مورد | مسیر | توضیح |
|---|---|---|
| `http://localhost:8000/api` | `src/environments/environment.ts:7` و `environment.development.ts:7` | هر دو فایل مقدار یکسان دارند؛ عملاً environment تفکیک‌نشده است |
| `ws://localhost:8000/ws/notifications/` | `core/http/websocket.service.ts:19` | کلاً بیرون از environment |
| فونت از CDN گوگل | `src/index.html:12` | برای شبکه داخلی/آفلاین انبار و با توجه به محدودیت دسترسی به `fonts.googleapis.com`، فونت وزیرمتن لود نمی‌شود و به Tahoma برمی‌گردد. پیشنهاد: فونت را داخل `public/fonts` میزبانی کنید |

نکاتی که **درست** انجام شده‌اند: `accessTokenLifetime` در environment است (خط ۹)، نسخه سامانه از API عمومی گرفته می‌شود (`login.ts:35-43`) و مقدار `'۱.۰'` فقط fallback است، و داده mock پشت فلگ `useMockData: false` خاموش است.

### ۲-۲. کاربران و رمزهای نمونه (کد مرده ولی حساس)

`core/auth/auth.service.ts:41-82` یک «دیتابیس» کامل از پنج کاربر با رمز `'123456'`، نام و نقش‌های واقعی سازمانی دارد. این بلوک فقط وقتی `environment.useMockData` روشن باشد اجرا می‌شود (خطوط ۹۰، ۱۲۱، ۱۵۶) و الان خاموش است، پس رخنه فعال نیست؛ اما این ساختار نقش‌ها و مجوزها در باندل نهایی جاوااسکریپت منتشر می‌شود و اگر روزی فلگ اشتباهی روشن شود، ورود بدون بک‌اند ممکن می‌شود. پیشنهاد: کل بلوک mock به یک فایل جدا منتقل یا حذف شود. مقدار `'123456'` در `components/change-password/change-password.ts:77` هم به‌عنوان رمز ممنوعه هارد‌کد شده که بهتر است از سمت سرور بیاید.

### ۲-۳. قواعد کسب‌وکار و اعداد جادویی

- `page_size: 1000` در `count-tracking/count-tracking.ts:61`، `counter/counter-dashboard.ts:182` و `supervisor/supervisor-dashboard.ts:61` — سه‌جا تکرار شده و عملاً صفحه‌بندی را دور می‌زند؛ با رشد داده، کندی محسوس خواهد داد.
- `filters['page_size'] = 100000` در `components/dispatch/dispatch.ts:830` — درخواست صد هزار ردیف در یک فراخوانی؛ گلوگاه جدی هنگام خروجی گرفتن.
- `page_size = '100'` در `core/api/dynamic-field-api.service.ts:21` — عدد متفاوت برای همان مفهوم.
- `expiryDays: 90` در `components/users/users.ts:46` و `:265` — مدت اعتبار کاربر باید تنظیمات سیستم باشد.
- وضعیت‌های شمارش (`PENDING_COUNT`، `COUNTED`، `MANAGER_REVIEW`، `FINAL_APPROVED`، …) به‌صورت رشته خام در `count-tracking.ts:126-143` و `counter-dashboard.ts:98-113` و `supervisor-dashboard.ts:61` پخش شده‌اند. تایپ آن‌ها در `core/models/count-task.model.ts:4-9` هست، ولی مقادیر و برچسب و رنگشان جای متمرکز ندارند.
- ابعاد کاغذ و مقیاس در `components/label-designer/label-designer.ts:52,55,271-275` (`A4`، `210×297`، `297×420`، `scale: 100`).
- بازه‌های زمانی هارد‌کد: `5000` در `websocket.service.ts:43`، `20000` در `counter-dashboard.ts:70`، `2000` در `settings/settings.ts:162`، `10000` در `label-designer.ts:748`، و `setInterval` در `core/services/import.service.ts:47`، `shared/components/excel-import-modal/excel-import-modal.ts:96`، `docs/docs.ts:309`.
- کد ملی نمونه `'1280954310'` در `components/id-cards/id-cards.ts` به‌عنوان مقدار پیش‌فرض.

### ۲-۴. کلیدهای Storage

نام کلیدها پیشوند منظم `wh_` دارند (خوب است) ولی به‌صورت رشته در چند فایل تکرار شده‌اند: `wh_access_token`/`wh_refresh_token`/`wh_user_profile` در `core/auth/auth.service.ts:13-15`، `wh_active_id`/`wh_context`/`wh_last_tab` در `core/stores/auth.store.ts` و دوباره `wh_active_id` در `services/state.service.ts:11`، و `desktopSidebarCollapsed` (بدون پیشوند، ناهماهنگ) در `components/layout/layout.ts:22,26`. پیشنهاد: یک فایل `core/constants/storage-keys.ts`.

### ۲-۵. متن‌ها و نگاشت‌های تکراری

عنوان صفحه‌ها دو بار تعریف شده‌اند: یک‌بار به‌عنوان `label` در `SYSTEM_NAV_ITEMS`/`WAREHOUSE_NAV_ITEMS` (`layout.ts:64-91`) و یک‌بار در نگاشت `titles` داخل `updateTitle` (`layout.ts:247-267`) — با عبارت‌های متفاوت برای یک صفحه (مثلاً «کاربران و نقش ها» در برابر «کاربران و نقش‌ها»، یکی بدون نیم‌فاصله). نگاشت نقش‌ها هم در `services/state.service.ts:32` و `shared/pipes/status-label.pipe.ts:82` تکرار شده. ده‌ها پیام خطا/موفقیت فارسی هم مستقیم داخل فایل‌های TS نوشته شده‌اند؛ اگر روزی چندزبانه شود یا متن‌ها یکدست شوند، همه باید دستی پیدا شوند.

یک مورد شکننده هم هست: در `components/login/login.ts:85` تشخیص خطای لاگین با مقایسه رشته انگلیسی `'No active account found with the given credentials'` انجام می‌شود. هر تغییر جزئی در پیام بک‌اند این منطق را می‌شکند؛ بهتر است بر اساس کد وضعیت یا کد خطای ساختاریافته تصمیم گرفته شود.

---

## بخش ۳ — پاسخ به سؤال اول: اشکالات ظاهری و رفتاری رابط کاربری

**دکمه ورود با کلید Enter کار نمی‌کند.** `components/login/login.html:35-77` به‌جای `<form>` از `<div class="space-y-4">` استفاده کرده و هیچ `(keyup.enter)` روی ورودی‌ها نیست. کاربری که رمز را تایپ می‌کند و Enter می‌زند، هیچ اتفاقی نمی‌بیند. اضافه کردن `<form (ngSubmit)="handleLogin()">` هم این را حل می‌کند و هم مدیریت رمز مرورگر را درست می‌کند (فعلاً `autocomplete` هم روی ورودی‌ها ست نشده).

**اعتبارسنجی سمت کلاینت در فرم ورود وجود ندارد.** `login.ts:57` بدون بررسی خالی‌بودن `username`/`password` درخواست می‌فرستد؛ دکمه فقط با `isLoggingIn` غیرفعال می‌شود (`login.html:69`). یک شرط ساده روی خالی‌بودن، یک رفت‌وبرگشت بی‌فایده به سرور را حذف می‌کند.

**تگ‌های باز در قالب لاگین.** در `login.html`، دو `<div>` باز شده در خطوط ۱ و ۸ هیچ‌وقت بسته نمی‌شوند (شمارش تگ‌ها تا انتهای فایل این را نشان می‌دهد) و در نتیجه مودال «فراموشی رمز عبور» (خط ۸۵) به‌جای سطح بالای صفحه، داخل کارت لاگین رندر می‌شود. الان چون مودال `fixed inset-0` است ظاهراً درست دیده می‌شود، ولی هر تغییری در `overflow` یا `transform` والدین آن را می‌شکند.

**دکمه‌های آیکونی بدون `aria-label`.** دکمه‌های همبرگری منو (`layout.html:77-82`)، زنگ اعلان (`layout.html:110-113`) و دکمه ویرایش پرسنل که فقط ایموجی ✏️ دارد (`users.html:72`) برای صفحه‌خوان و حتی برای کاربر بینا (بدون tooltip) نامفهوم‌اند. جستجوی کاربران (`users.html:26-28`) هم فقط `placeholder` دارد و `<label>` ندارد.

**مودال‌ها فاقد نقش دسترس‌پذیری و تله فوکوس‌اند.** `shared/components/confirm-dialog/confirm-dialog.component.ts` و `shared/components/modal/modal.component.ts` بدون `role="dialog"`، `aria-modal` و مدیریت Tab/Escape هستند؛ با کیبورد می‌شود از مودال بیرون رفت و پشت آن کلیک کرد.

**حالت بارگذاری/خالی/خطا در همه‌جا یکدست نیست.** فهرست کاربران این سه حالت را دارد (`users.html:50-54`) که خوب است، ولی داشبورد (`dashboard.html`)، لیست انبارها (`projects.html:42-91`) و جدول رهگیری تغییرات (`audit.html:68-117`) اسکلت بارگذاری و پیام خطا با دکمه «تلاش دوباره» ندارند. در `layout.ts:120-133` هم فراخوانی لیست انبارها فقط `next` دارد و شاخه `error` ندارد؛ اگر این درخواست شکست بخورد، سایدبار خالی می‌ماند و کاربر تنها یک toast عمومی می‌بیند.

**جدول‌ها روی موبایل.** جدول `audit.html:69-116` عرض ستون‌های ثابت (`w-12`، `w-32`، `w-40`) دارد؛ روی گوشی اسکرول افقی می‌خورد. اگر این صفحه واقعاً در انبار و روی موبایل استفاده می‌شود، تبدیل ردیف‌ها به کارت در بریک‌پوینت کوچک ارزش کار را دارد.

**یکدستی ظاهری.** ترکیب `style="background: linear-gradient(...)"` درون‌خطی در `login.html:1,14,69` در کنار کلاس‌های Tailwind بقیه پروژه، و CSS اختصاصی مودال Excel (`excel-import-modal`) در برابر مودال‌های Tailwind، دو سبک موازی ساخته است. رنگ‌های وضعیت پیشرفت هم مستقیم در TS نوشته شده‌اند (`layout.ts:209-214`).

**ارقام و تاریخ.** `PersianDatePipe` وجود دارد و در بعضی جاها استفاده شده، اما ارقام در داشبورد با پایپ `| number` لاتین رندر می‌شوند در حالی که متن‌های ثابت فارسی‌اند (`dashboard.html`)؛ یک پایپ ساده تبدیل ارقام، ظاهر را یکدست می‌کند.

**آیکون فلش دکمه ورود.** `login.html:72` از `points="15 18 9 12 15 6"` (فلش چپ) استفاده می‌کند که در چیدمان راست‌به‌چپ، جهت «ورود» را برعکس نشان می‌دهد.

---

## بخش ۴ — بدهی فنی و ساختار

**اشتراک‌ها پاک نمی‌شوند.** فقط پنج فایل در کل پروژه `ngOnDestroy` دارند (`data-table.component.ts`، `label-designer.ts`، `docs.ts`، `counter-dashboard.ts`، `click-outside.directive.ts`) در حالی که ده‌ها کامپوننت `subscribe` می‌کنند. مثال مشخص: `components/dispatch/dispatch.ts:26` یک `loadSubscription` تعریف کرده ولی هیچ‌جا آن را نمی‌بندد، و `layout.ts:109-115` روی `router.events` مشترک می‌شود بدون لغو. راه‌حل تمیز و کم‌هزینه: `takeUntilDestroyed()` از `@angular/core/rxjs-interop`.

**سه لایه موازی برای state.** `core/auth/auth.service.ts` (سیگنال‌های کاربر)، `core/stores/auth.store.ts` (کانتکست انبار و تب) و `services/state.service.ts` (که خودش deprecated علامت خورده ولی هنوز در `layout.ts:8`، `dispatch.ts` و `projects.ts` استفاده می‌شود). در `layout.ts:172-175` مقدار انبار فعال هم‌زمان در store و state نوشته می‌شود — دقیقاً همان‌جایی که به‌مرور ناهمگام می‌شود. پیشنهاد: `StateService` حذف و همه‌چیز به `AuthStore` منتقل شود.

**پوسته‌های قدیمی (shim) باقی‌مانده‌اند.** `src/app/services/auth.service.ts` و `src/app/guards/auth.guard.ts` فقط re-export از `core/auth` هستند و هیچ مسیری از آن‌ها استفاده نمی‌کند. `src/app/app.component.html` هم بلااستفاده است (قالب واقعی `app.html` است که در `app.ts:11` معرفی شده).

**هفت مسیر به کامپوننت Placeholders وصل‌اند** (`app.routes.ts:58-64`: tasks، labels، approvals، export، customs، doc_approvals، feed_approvals). اگر قرار نیست به‌زودی پیاده شوند، بهتر است آیتم منویشان مخفی شود تا کاربر به صفحه خالی نرسد.

**کش مسیرها هنگام خروج کاربر پاک نمی‌شود.** `core/strategies/custom-route-reuse-strategy.ts:30-32` کلید کش را فقط `path` می‌گیرد (بدون پارامتر و کوئری). هنگام تعویض انبار درست پاک می‌شود (`core/stores/auth.store.ts:63-67` — این نکته خوبی است)، ولی `logout()` در `core/auth/auth.service.ts:106-111` آن را پاک نمی‌کند. اگر کاربر دوم روی همان تب لاگین کند و انبار فعال تغییر نکرده باشد، ممکن است کامپوننت کش‌شده با داده کاربر قبلی دوباره متصل شود. یک `strategy.clear()` در مسیر خروج، هم این را حل می‌کند و هم مسئله داده کهنه در تب‌های `reuse: true` را.

**تنظیمات TypeScript سخت‌گیرانه نیست.** `tsconfig.json:5-16` گزینه `"strict": true` ندارد (اسکلت پیش‌فرض Angular آن را دارد). یعنی `strictNullChecks` خاموش است و خطاهای null در زمان کامپایل گرفته نمی‌شوند — که با توجه به استفاده گسترده از `any` (مثلاً `layout.ts:42,62,64,77`) ریسک را دوبرابر می‌کند.

**تست واقعی وجود ندارد.** همه فایل‌های `*.spec.ts` فقط `should create` هستند. حداقلِ ارزشمند: تست برای گارد مجوز و نگاشت `ROUTE_PERMISSIONS`، چون دقیقاً همان‌جاست که در بخش ۱ چند ناسازگاری پیدا شد.

**`puppeteer` در `dependencies` است.** `package.json:22` — این پکیج در مرورگر بی‌استفاده است، حین `npm install` یک نسخه کامل Chromium دانلود می‌کند و اگر جایی برای PDF گرفتن لازم است باید سمت سرور باشد. حداقل به `devDependencies` منتقل شود.

**لینتر تعریف نشده.** در `angular.json` هیچ target مربوط به `lint` نیست؛ اضافه‌کردن ESLint با پلاگین Angular، بخش بزرگی از موارد بالا (importهای بی‌استفاده، `any`، اشتراک‌های رهاشده) را خودکار می‌گیرد.

نکته‌ای که **مشکل نیست** و ممکن است در نگاه اول نگران‌کننده به‌نظر برسد: بیلد پروداکشن source map تولید نمی‌کند (`angular.json:47-50` نشان می‌دهد `sourceMap: true` فقط در حالت development است)، و budgetهای بیلد (خطوط ۳۳-۴۴) منطقی تنظیم شده‌اند.

---

## پیشنهاد ترتیب کار

۱. آدرس API و WebSocket را از environment بخوانید و فونت را داخلی کنید — بدون این سه، استقرار روی سرور واقعی کار نمی‌کند.
۲. نگاشت `ROUTE_PERMISSIONS` و مجوز آیتم‌های منو را با هم یکی کنید و برای مسیرهای بدون گارد ورودی اضافه کنید؛ یک صفحه «۴۰۴/دسترسی ندارید» هم بسازید.
۳. `takeUntilDestroyed` را در کامپوننت‌هایی که subscribe دارند اضافه کنید و `StateService` را بازنشسته کنید.
۴. یک فایل ثابت‌ها بسازید برای کلیدهای storage، `page_size`، وضعیت‌های شمارش و برچسب‌هایشان.
۵. فرم لاگین را به `<form>` تبدیل کنید، تگ‌های باز را ببندید، و `aria-label` دکمه‌های آیکونی را اضافه کنید.
۶. `"strict": true` را روشن کنید و خطاهای به‌دست‌آمده را مرحله‌به‌مرحله رفع کنید؛ سپس ESLint اضافه کنید.
