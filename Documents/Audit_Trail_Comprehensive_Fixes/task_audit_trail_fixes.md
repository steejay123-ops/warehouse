# چک‌لیست وظایف و گیت‌های اعتبارسنجی (Task List & Verification Gates)

<div dir="rtl" align="right">

- [x] **فاز ۱: امنیت، کنترل دسترسی (RBAC) و فیلتر انبار در بک‌اند**
  - [x] افزودن بررسی دسترسی‌های `perm_sys_logs` و `view_wh_audit` در `AuditLogViewSet` و `UserLoginLogViewSet`
  - [x] اعمال فیلتر انبار و دسترسی‌های مجاز کاربر در `get_queryset`، `stats` و `export_csv`
  - [x] **گیت اعتبارسنجی فاز ۱:** تست احراز هویت با کاربر بدون دسترسی (403 Forbidden) و بررسی تفکیک انبارها

- [x] **فاز ۲: رفع خطای N+1 Query و بهینه‌سازی تفاضل در بک‌اند**
  - [x] افزودن `prefetch_related('user__groups__customrole')` به کوئری‌ست `AuditLogViewSet`
  - [x] اصلاح مدیریت فیلدهای `None`، کلیدهای جدید و حذف‌شده در `calculate_model_diff` در `audit_utils.py`
  - [x] **گیت اعتبارسنجی فاز ۲:** بررسی تعداد کوئری‌های SQL (کاهش به ۱-۲ کوئری) و صحت خروجی diff

- [x] **فاز ۳: اتصال لاگ‌گیری به عملیات حساس انبار**
  - [x] فراخوانی `log_audit_event` در فریز/فعال‌سازی انبار، ایمپورت/حذف فایل پایه و تغییر نقش‌ها
  - [x] **گیت اعتبارسنجی فاز ۳:** تست عملیاتی تغییر انبار و بررسی ایجاد رکورد صحیح با before_state و after_state

- [x] **فاز ۴: همگام‌سازی فیلترها و سرویس‌های فرانت‌اند**
  - [x] افزودن شناسه انبار جاری از `AuthStore` به پارامترهای درخواست لاگ‌ها در `Audit`
  - [x] افزودن فیلتر کشویی «نوع عملیات» (`selectedAction`) در تمپلیت `audit.html`
  - [x] افزودن گزینه `system` (رویدادهای سیستمی) به دراپ‌داون ماژول‌ها
  - [x] ریست کامل تمامی فیلترها هنگام تغییر تب در متد `setTab`
  - [x] پاکسازی مقادیر `undefined` در سرویس `AuditApiService` برای دانلود CSV
  - [x] **گیت اعتبارسنجی فاز ۴:** تست فیلترها، ریست تب‌ها و بررسی درخواست دانلود CSV بدون پارامتر اضافه

- [x] **فاز ۵: الگوریتم پیشرفته مقایسه تفاضل در فرانت‌اند**
  - [x] پیاده‌سازی مقایسه عمیق (Deep Value Equality) برای اشیاء و آرایه‌ها در `buildDiffItems`
  - [x] **گیت اعتبارسنجی فاز ۵:** بررسی مدال تفاضل با داده‌های تودرتو و تایید عدم وجود مغایرت کاذب

- [x] **فاز ۶: بهینه‌سازی UI، تایپوگرافی، استایل و اسکلت لودینگ**
  - [x] اصلاح کلاس‌های نامعتبر Tailwind مانند `py-0.2` به `py-0.5`
  - [x] اصلاح چیدمان و جهت متن تاریخ و زمان فارسی
  - [x] پیاده‌سازی اسکلت بارگذاری (Skeleton Loading) و وضعیت خطای درون‌جدولی با دکمه تلاش مجدد
  - [x] شفاف‌سازی عناوین کارت‌های شاخص عملکرد (KPI Cards)
  - [x] **گیت اعتبارسنجی فاز ۶:** بررسی عملکرد لودینگ، نمایش در خطای شبکه و تست واکنش‌گرایی UI

- [x] **فاز ۷: راستی‌آزمایی یکپارچه، تست نهایی و مستندسازی**
  - [x] اجرای `ng build` و `python manage.py check` بدون خطا
  - [x] تهیه گزارش نهایی تغییرات (Walkthrough) و همگام‌سازی مستندات پروژه

</div>
