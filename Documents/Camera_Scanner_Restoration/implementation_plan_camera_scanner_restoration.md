<div dir="rtl" align="right">

# طرح جامع احیا و بازنشانی استاندارد دوربین اسکنر بارکد بر پایه معماری سالم گیت

این طرح به منظور **بازگرداندن معماری پایدار، استاندارد و بدون خطای دوربین اسکنر بر اساس نسخه سالم اولیه گیت (کامیت `5b66605`) و ارتقای همزمان آن برای پشتیبانی از تمامی فرمت‌های بارکد ۱ بعدی و ۲ بعدی صنعتی** تدوین شده است.

---

## ۱. تحلیل و چرایی بازنشانی (Rationale & Architecture Review)

| بخش | وضعیت فعلی (دارای اختلال و صفحه سیاه) | معماری بازنشانی‌شده استاندارد (برگرفته از گیت) |
| :---: | :--- | :--- |
| **موتور استریم ویدیو** | تلاش دستی برای کراپ روی بوم نامرئی (Offscreen Canvas) در هر فریم که باعث اختلال در چرخه اتصال ویدیو و سیاه ماندن صفحه می‌شود. | واگذاری کامل هندلینگ استریم، رزولوشن و رندر بلادرنگ روی تگ `<video>` به متد بومی و پایدار **`decodeFromConstraints`** و **`BarcodeDetector`**. |
| **پشتیبانی از انواع بارکد** | استفاده از مدل QR تکی در نسخه قدیمی | ارتقا به **`BrowserMultiFormatReader`** با پوشش کامل ۱۲ فرمت صنعتی (QR, Code 128, EAN-13, Data Matrix, Code 39, ...) |
| **مدیریت منابع و حافظه** | تداخل در متغیرهای `MediaStream` و متوقف نشدن کامل دوربین | کنترل یکپارچه چرخه حیات با شیء رسمی **`IScannerControls`** و متوقف‌سازی بی‌نقص تمام Trackها هنگام بستن دوربین |
| **رابط کاربری و اورلی** | لایه‌های پیچیده CSS که مانع مشاهده ویدیو می‌شدند | اورلی سبک و مدرن شیشه‌ای، خط راداری لیزر، راهنمای واضح و دکمه بستن با کلید `Escape` |

---

## ۲. نیازمند تایید کاربر (User Review Required)

> [!IMPORTANT]
> با اجرای این طرح، هرگونه پردازش سنگین و دستی بوم نقاشی (Canvas) که باعث قفل شدن وب‌کم و تصویر سیاه می‌شد حذف شده و دوربین دقیقاً مانند نسخه اولیه و به صورت آنی، روان و ۶۰ فریم لود خواهد شد.

---

## ۳. تغییرات پیشنهادی در فایل‌ها (Proposed Changes)

### 📦 کامپوننت مشترک اسکنر (`src/app/shared/components/barcode-scanner`)

#### [MODIFY] [barcode-scanner.component.ts](file:///e:/warehouse%20project/warehouse-front/src/app/shared/components/barcode-scanner/barcode-scanner.component.ts)
- بازنویسی کامل متدهای راه‌اندازی دوربین بر اساس `decodeFromConstraints` از کتابخانه `@zxing/browser`.
- پشتیبانی از موتور بومی `BarcodeDetector` در صورت فعال بودن مرورگر به همراه فال‌بک خودکار به ZXing چندفرمتِه.
- تنظیم قیود تصویری رزولوشن بالا و فوکوس خودکار پیوسته (`continuous focus`).
- پیاده‌سازی متد تمیز `closeCamera()` با توقف کنترلر `zxingControls?.stop()`.
- حفظ ویژگی‌های جدید و کاربردی شامل صدای بوق اسکن، لرزش، میانبر `Escape` و `F2`.

---

## ۴. فازبندی اجرایی (Execution Phases)

### ⚡ فاز ۱: بازنشانی متدهای اتصال دوربین و استریم در `BarcodeScannerComponent`
- پیاده‌سازی متدهای `startNativeDetector` و `startZxing` با استفاده از `decodeFromConstraints`.
- پاک‌سازی کدهای لوپ دستی کراپ و بوم‌های آف‌اسکرین.

### 🎨 فاز ۲: ساده‌سازی و استانداردسازی قالب بصری اورلی دوربین
- بهینه‌سازی تمپلیت HTML و CSS کادر لیزر برای نمایش شفاف و روان ویدیو در تمام ابعاد صفحه.

### 🧪 فاز ۳: کامپایل و راستی‌آزمایی بیلد
- اجرای `npm run build` و بررسی عدم وجود هرگونه خطای تایپ‌اسکریپت و باندل.

---

## ۵. برنامه راستی‌آزمایی (Verification Plan)

### تست‌های خودکار:
1. اجرای `npm run build` در پوشه `warehouse-front` و دریافت خروجی سبز با Exit Code: 0.

### تست‌های دستی توسط شما:
1. باز کردن پروژه در مرورگر و کلیک روی آیکون دوربین در کارتابل مالی یا انبارگردان.
2. مشاهده باز شدن فوری تصویر زنده وب‌کم بدون هیچ تاخیر و صفحه سیاه.
3. اسکن یک بارکد میله‌ای یا QR و تایید باز شدن مستقیم فرم کالا.

</div>
