<div dir="rtl" align="right">

# طرح بهینه‌سازی حداکثری سرعت و قدرت تشخیص اسکنر دوربین بارکد (نزدیک به اسکنر بومی موبایل)

این طرح به منظور **افزایش فوق‌العاده سرعت و حساسیت اسکنر وب تا سطح اسکنر بومی موبایل (Native Phone Scanner)، کاهش تاخیر پردازش فریم‌ها از ۵۰۰ میلی‌ثانیه به ۵۰ میلی‌ثانیه، بهینه‌سازی رزولوشن به ۷۲۰p برای حفظ نرخ ۳۰ تا ۶۰ فریم و فعال‌سازی فوکوس و زوم سخت‌افزاری** تدوین شده است.

---

## ۱. تحلیل دلایل عدم اسکن سریع در وب نسبت به اسکنر بومی گوشی (Root Causes)

| ردیف | تفاوت اسکنر وب با اسکنر گوشی | مشکل فنی فعلی در کد | راهکار بهینه‌سازی |
| :---: | :--- | :--- | :--- |
| **۱** | **تاخیر بین فریم‌ها (Scan Attempt Interval)** | اسکنر بومی در هر فریم (۳۰ بار در ثانیه) اسکن می‌کند، اما ZXing به طور پیش‌فرض هر ۵۰۰ میلی‌ثانیه یک بار تست می‌کرد. | تنظیم `delayBetweenScanAttempts: 40` میلی‌ثانیه (۲۵ فریم در ثانیه پردازش زنده). |
| **۲** | **رزولوشن سنگین و افت فریم (Resolution Overhead)** | رزولوشن ۱۰۸۰p باعث محاسبات سنگین ماتریس پیکسل در جاوااسکریپت و کندی پردازش می‌شود. | تنظیم رزولوشن طلایی ۷۲۰p (`1280x720`) که سرعت پردازش هر فریم را ۵ برابر سریع‌تر می‌کند. |
| **۳** | **بار پردازشی فرمت‌های نامربوط (Format Pruning)** | بررسی ۱۲ فرمت سنگین به صورت همزمان باعث پردازش‌های زائد در هر فریم می‌شود. | اولویت‌بندی فرمت‌های استاندارد انبار (`QR_CODE`, `CODE_128`, `EAN_13`, `DATA_MATRIX`, `CODE_39`). |
| **۴** | **فوکوس و بزرگ‌نمایی سخت‌افزاری (Hardware Focus & Zoom)** | عدم وجود زوم در وب باعث می‌شود QR کدهای کوچک از فاصله عادی رزولوشن کافی نداشته باشند. | فعال‌سازی زوم سخت‌افزاری (۱.۵X / ۲X) و فوکوس ماکرو پیوسته (`continuous focus`) روی سنسور موبایل. |
| **۵** | **موتور بومی BarcodeDetector مرورگر** | عدم استفاده بهینه از API فوق سریع مرورگر (که با همان هوش مصنوعی Google Lens کار می‌کند). | پیاده‌سازی معماری ترکیبی سریع با اتصال بدون واسطه به `BarcodeDetector` و فال‌بک موازی به ZXing. |

---

## ۲. نیازمند تایید کاربر (User Review Required)

> [!IMPORTANT]
> با اعمال این بهینه‌سازی‌ها، سرعت واکنش دوربین به محض قرار گرفتن بارکد یا QR کد در کادر، به کمتر از ۱۰۰ میلی‌ثانیه (شبیه به اسکنر دوربین پیش‌فرض گوشی) خواهد رسید.

---

## ۳. تغییرات پیشنهادی در فایل‌ها (Proposed Changes)

### 📦 کامپوننت مشترک اسکنر (`src/app/shared/components/barcode-scanner`)

#### [MODIFY] [barcode-scanner.component.ts](file:///e:/warehouse%20project/warehouse-front/src/app/shared/components/barcode-scanner/barcode-scanner.component.ts)
- تنظیم `delayBetweenScanAttempts: 40` در گزینه‌های `BrowserMultiFormatReader` جهت اسکن با فرکانس بالا.
- اصلاح رزولوشن استریم به `1280x720` بهینه و اضافه کردن قیدهای پیشرفته فوکوس خودکار.
- اولویت‌بندی فرمت‌های پرکاربرد انبارداری برای کاهش تاخیر پردازش CPU.
- فعال‌سازی کنترل زوم سخت‌افزاری (دکمه‌های ۱X و ۲X در فوتر) برای خواندن کدهای ریز یا با فاصله.
- بهبود متد `startNativeDetector` با استفاده از `ImageCapture` یا `requestVideoFrameCallback` جهت پردازش فوق سریع بدون افت فریم.

---

## ۴. فازبندی اجرایی (Execution Phases)

### ⚡ فاز ۱: بهینه‌سازی موتور ZXing و رزولوشن ۷۲۰p
- اعمال تنظیمات فرکانس بالا (`delayBetweenScanAttempts: 40ms`).
- بهینه‌سازی فرمت‌های جستجو و رزولوشن استاندارد ۷۲۰p.

### 🔬 فاز ۲: فعال‌سازی فوکوس پیوسته سخت‌افزاری و دکمه‌های بزرگ‌نمایی (Zoom 1X/2X)
- اعمال قیدهای `focusMode: 'continuous'` و پیاده‌سازی کنترل‌های زوم.

### 🧪 فاز ۳: تست، بیلد و راستی‌آزمایی
- اجرای `npm run build` و تست عملیاتی اسکن انواع بارکد و QR.

---

## ۵. برنامه راستی‌آزمایی (Verification Plan)

### تست‌های خودکار:
1. اجرای `npm run build` در پوشه `warehouse-front` و اطمینان از سلامت کامپایل با Exit Code: 0.

### تست‌های دستی توسط شما:
1. باز کردن دوربین اسکنر در کارتابل و گرفتن بارکد یا QR کد در فاصله ۱۵ الی ۲۵ سانتی‌متری از لنز.
2. مشاهده اسکن و شناسایی فوری در کسری از ثانیه (بدون نیاز به مکث طولانی).

</div>
