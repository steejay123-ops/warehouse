<div dir="rtl" align="right">

# لیست وظایف فازبندی‌شده همسان‌سازی رابط‌کاربری کارتابل‌های مالی و انبارگردان

- [x] **فاز ۱: اصلاحات ابعاد، عناوین و فیلترهای کارتابل انبارگردان (`Counter Dashboard Core UI`)**
  - [x] <!-- id: 101 --> ۱.۱. تغییر عرض کانتینر اصلی صفحه و فوتر از `max-w-2xl` به `max-w-7xl` در `counter-dashboard.html`
  - [x] <!-- id: 102 --> ۱.۲. تغییر برچسب کارت آمار و چیپ وضعیت از «شمرده شده» به «ارسال شده» در `counter-dashboard.html`
  - [x] <!-- id: 103 --> ۱.۳. تغییر مقدار اولیه `statusFilter` به `'pending'` در `counter-dashboard.ts`
  - [x] <!-- id: 104 --> **گیت اعتبارسنجی فاز ۱:** تست کامپایل و راستی‌آزمایی بصری عرض صفحه و فیلترها قبل از ورود به فاز ۲ (با موفقیت پاس شد)

- [x] **فاز ۲: قابلیت‌های تعاملی استخر و اکسل انبارگردان (`Counter Pool & Export Features`)**
  - [x] <!-- id: 201 --> ۲.۱. پیاده‌سازی نوار سراسری انتخاب همه اقلام در تب استخر کالاها با وضعیت `indeterminate`
  - [x] <!-- id: 202 --> ۲.۲. تعریف متدهای `isAllPoolSelected()`, `isPoolIndeterminate()`, `toggleSelectAllPool()` در `counter-dashboard.ts`
  - [x] <!-- id: 203 --> ۲.۳. افزودن کارت دانلود قالب نمونه اکسل در مودال اکسل و پیاده‌سازی متد `downloadSampleTemplate()`
  - [x] <!-- id: 204 --> **گیت اعتبارسنجی فاز ۲:** تست انتخاب گروهی استخر و دانلود اکسل نمونه قبل از ورود به فاز ۳ (با موفقیت پاس شد)

- [x] **فاز ۳: اصلاحات اکشن‌ها، فرم جزئیات و تایم‌لاین کارتابل مالی (`Customs Actions & Details`)**
  - [x] <!-- id: 301 --> ۳.۱. یکدست‌سازی رنگ دکمه اکشن انتخابی بالا به سبز زمردی (`bg-emerald-600`) و آیکون تیک در `customs.html`
  - [x] <!-- id: 302 --> ۳.۲. افزودن دکمه «بازگشت به وضعیت قبل» در فوتر ثابت فرم جزئیات مالی
  - [x] <!-- id: 303 --> ۳.۳. اصلاح نمایش لوکیشن قدیم/جدید (`new_location || old_location`) در کارت‌های لیست مالی
  - [x] <!-- id: 304 --> ۳.۴. ارتقای گرافیکی و افزودن نشانگرهای رنگی وضعیت به تایم‌لاین سوابق در مالی
  - [x] <!-- id: 305 --> **گیت اعتبارسنجی فاز ۳:** تست تعاملی دکمه بازگشت در فرم مالی و بررسی اکشن‌بار سبز (با موفقیت پاس شد)

- [x] **فاز ۴: راستی‌آزمایی نهایی، بیلد سراسری و ثبت مستندات (`Final Build & Dual-Save Walkthrough`)**
  - [x] <!-- id: 401 --> ۴.۱. اجرای تست سلامت بیلد فرانت‌اند و بررسی عدم وجود خطای کامپایل
  - [x] <!-- id: 402 --> ۴.۲. بررسی نهایی تجربه کاربری در مرورگر و تکمیل مستندات در `walkthrough.md`

</div>
