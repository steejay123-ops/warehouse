<div dir="rtl" align="right">

# طرح بهبود امکانات مدیریتی پنل پیگیری شمارش

این طرح برای پیاده‌سازی ۵ ویژگی پیشنهادی در پنل پیگیری وضعیت شمارش (Count Tracking) تدوین شده است تا تجربه کاربری و سرعت مدیریت انبارگردانی را به شکل چشمگیری افزایش دهد.

## اهداف و رویکرد

1. **فیلتر سریع مغایرت‌ها:** افزودن یک دکمه (Toggle) برای نمایش سریع ردیف‌هایی که بین موجودی سیستم و مقدار شمارش‌شده تفاوت دارند.
2. **ستون «مقدار مغایرت»:** نمایش عددی میزان مغایرت در جدول (مانند `+5` یا `-2`) برای امکان مرتب‌سازی (Sorting) و بررسی دقیق‌تر.
3. **آمار خطی کوچک در بالای صفحه (Mini Dashboard):** نمایش لحظه‌ای مجموع تسک‌ها، تسک‌های شمرده شده و تسک‌های دارای مغایرت در بالای جدول.
4. **تأیید گروهی سمت مدیر (Bulk Manager Approve):** ایجاد یک دکمه برای انتخاب سریع تمام مواردِ «سبز» (بدون مغایرت) و تأیید نهایی آن‌ها با یک کلیک. طبق تأیید شما، این بخش متد `bulk_manager_approve` را برای نقش مدیر فراخوانی می‌کند و وضعیت به `FINAL_APPROVED` می‌رود.
5. **بروزرسانی زنده (Real-time):** اتصال صفحه به WebSockets جهت دریافت خودکار تغییرات وضعیت‌ها بدون نیاز به رفرش صفحه.

---

## بررسی ایرادات ساختاری (گزارش به کاربر)

> [!WARNING]
> **ایراد پیدا شده در سیستم مرتب‌سازی (Sorting):**
> در حال حاضر کامپوننت جدول (`DataTable`) در صفحه پیگیری شمارش، رویداد کلیک روی هدر ستون‌ها را به درستی منتشر می‌کند (`sortChanged`)، اما کامپوننت `CountTracking` به آن گوش نمی‌دهد! یعنی قابلیت سورت کلاً در این صفحه قطع است.
> **راه حل قطعی (و خبر خوب):** با توجه به اینکه این صفحه داده‌ها را به صورت یکجا (تا ۱۰۰۰ رکورد) از سرور می‌گیرد، من یک فانکشن **مرتب‌سازی در کلاینت‌ساید** برای `CountTracking` می‌نویسم تا نه‌تنها مشکل برطرف شود، بلکه بتوانید روی **تمام فیلدها** (حتی فیلدهای محاسبه‌شده مثل مغایرت، نام‌ها و مدت زمان‌ها) سورت انجام دهید!

---

## تغییرات پیشنهادی سیستم

### فرانت‌اند (Angular)

#### [MODIFY] [count-tracking.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/count-tracking/count-tracking.ts)
- اضافه کردن متغیرهای محاسبه آمار (Mini Dashboard).
- اضافه کردن منطق فیلتر `showOnlyDiscrepancies`.
- **اضافه کردن متد `onSortChanged(sort)` برای مرتب‌سازی تمام فیلدها در حافظه (شامل فیلد جدید `discrepancy`).**
- محاسبه مقدار مغایرت به عنوان یک فیلد درون آبجکت `t.discrepancy` هنگام دریافت اطلاعات برای راحتی در سورت.
- اضافه کردن اتصال WebSocket به لیست `CountTask`ها (مشابه صفحه Notifications).
- اضافه کردن متد `bulkApproveGreenTasks()` برای فراخوانی API `bulk_manager_approve`.

#### [MODIFY] [count-tracking.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/count-tracking/count-tracking.html)
- متصل کردن `(sortChanged)="onSortChanged($event)"` به جدول.
- اضافه کردن بخش ویجت‌های آماری (Mini Dashboard) در هدر صفحه.
- اضافه کردن دکمه فیلتر «فقط دارای مغایرت» در کنار دکمه‌های بالا.
- اضافه کردن دکمه «تأیید گروهی بدون مغایرت‌ها» (Manager Approve).
- اضافه کردن ستون جدید «مقدار مغایرت» (Discrepancy) در `appTableCol`.

#### [MODIFY] [count-task-api.service.ts](file:///e:/warehouse%20project/warehouse-front/src/app/core/api/count-task-api.service.ts)
- بررسی و اطمینان از وجود متد `bulkManagerApprove(taskIds)`.

### بک‌اند (Django)

#### [MODIFY] [views.py](file:///e:/warehouse%20project/warehouse-backend/inventory/views.py)
- در اکشن‌های `CountTaskViewSet` بررسی می‌کنیم که بعد از تغییرات مهم، سیگنال WebSocket ارسال شود (هرچند احتمالاً در مدل‌ها این سیگنال از قبل برای `channels` پیکربندی شده است). در صورت نیاز، `Group_send` مربوط به آپدیت وضعیت انبارگردانی بررسی می‌شود.

> [!NOTE]
> (نکته) ویژگی شماره ۵ (WebSocket) وابسته به این است که کدهای Backend از قبل رویدادهای `post_save` را به Channels ارسال کنند. در غیر این صورت، این بخش در بک‌اند اضافه می‌شود.

---

## سؤالات از کاربر

دیگر سؤالی نیست. با پاسخ‌های شما، هدف‌گذاری روشن شد:
- نقش تأیید گروهی: Manager 
- سورت برای تمام فیلدها با سیستم کلاینت‌ساید فعال می‌شود.
- ایراد قطعیِ سورت شناسایی شد و در این طرح برطرف می‌شود.

## برنامه راستی‌آزمایی (Verification Plan)
1. کامپایل و بیلد موفقیت‌آمیز Angular.
2. مشاهده صحیح آمارهای Mini Dashboard در بالای صفحه و تطابق با داده‌های جدول.
3. فیلتر صحیح مغایرت‌ها بدون به هم ریختن جستجوهای قبلی.
4. تست دکمه Bulk Approve روی ردیف‌های بدون مغایرت و مشاهده تغییر وضعیت به «تأیید نهایی».
5. دریافت خودکار تغییرات در صورت تغییر وضعیت از یک دستگاه دیگر.

</div>
