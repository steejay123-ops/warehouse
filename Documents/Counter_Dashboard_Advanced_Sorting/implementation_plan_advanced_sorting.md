<div dir="rtl" align="right">

# طرح پیاده‌سازی گزینه‌های زمانی مرتب‌سازی و تاگل جهت در کارتابل انبارگردان

این طرح به منظور ارتقای گزینه‌های مرتب‌سازی (افزودن زمان ایجاد و زمان تغییر)، تنظیم پیش‌فرض قطعی سیستم روی «زمان ایجاد (جدیدترین)» و پیاده‌سازی رفتار تاگل جهت صعودی/نزولی با کلیک مجدد روی هر گزینه تدوین شده است.

---

## ۱. مرور نیازمندی‌ها و تصمیمات طراحی (Design Decisions)

1. **افزودن گزینه‌های زمانی جدید:**
   - **زمان ایجاد (`created_at`):** بر اساس تاریخ و ساعت ایجاد تسک در دیتابیس.
   - **زمان تغییر / ویرایش (`updated_at`):** بر اساس آخرین زمان ویرایش کالا یا ثبت شمارش.
2. **پیش‌فرض همیشگی سیستم:**
   - پیش‌فرض همواره روی `sortField = 'created_at'` و `sortDirection = 'desc'` (جدیدترین تسک‌ها در بالا) تنظیم می‌شود تا به محض ورود، اقلام جدیداً تخصیص‌یافته در صدر باشند.
3. **رفتار تاگل جهت با کلیک مجدد روی همان فیلد:**
   - اگر کاربر روی گزینه‌ای که هم‌اکنون فعال است (مثلاً کد کالا) مجدداً کلیک کند، جهت آن بلافاصله از صعودی به نزولی (یا برعکس) تغییر می‌یابد.
   - در داخل منوی مرتب‌سازی، کنار گزینه انتخاب‌شده آیکون جهت (`↑` یا `↓`) و برچسب مربوطه (مثلاً «صعودی» یا «نزولی») نمایش داده می‌شود تا کاربر کاملاً از وضعیت مطلع باشد.
4. **فهرست کامل گزینه‌های مرتب‌سازی (۶ گزینه):**
   - زمان ایجاد (`created_at`) — پیش‌فرض نزولی (جدیدترین)
   - زمان تغییر (`updated_at`) — پیش‌فرض نزولی (جدیدترین)
   - مسیر لوکیشن (`location`) — پیش‌فرض صعودی (قفسه‌بندی طبیعی)
   - اولویت با بازشماری‌ها (`recount_first`)
   - کد کالا (`code`) — پیش‌فرض صعودی
   - شرح کالا (`title`) — پیش‌فرض صعودی

---

## ۲. فایل‌های تحت تغییر (Proposed File Changes)

### فرانت‌اند (`warehouse-front`)

#### [MODIFY] [counter-dashboard.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts)
- توسعه تایپ `sortField` به: `'created_at' | 'updated_at' | 'location' | 'recount_first' | 'code' | 'title'`.
- تنظیم پیش‌فرض اولیه به `sortField = 'created_at'` و `sortDirection = 'desc'`.
- ارتقای منطق مقایسه در `applyFilters()` برای فیلدهای `created_at` و `updated_at` (تبدیل به `getTime()` و مرتب‌سازی دقیق میلی‌ثانیه‌ای).
- ارتقای متد `setSort(field)`:
  - اگر `this.sortField === field`: جهت تاگل می‌شود (`this.sortDirection = this.sortDirection === 'asc' ? 'desc' : 'asc'`).
  - اگر فیلد جدید است: برای فیلدهای زمانی (`created_at`, `updated_at`) جهت روی `desc` و برای متنی روی `asc` تنظیم می‌شود.
- به‌روزرسانی متد `getSortLabel()` جهت پوشش نام‌های فارسی ۶ گزینه.

#### [MODIFY] [counter-dashboard.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.html)
- افزودن گزینه‌های «زمان ایجاد» و «زمان تغییر» به منوی پاپ‌اوور مرتب‌سازی.
- نمایش آیکون و نشانگر جهت در منو برای گزینه‌ای که در حال حاضر فعال است.

---

## ۳. طرح راستی‌آزمایی (Verification Plan)

1. **تست کامپایل:**
   - اجرای `npx ng build` برای تایید صحت کامل کامپایل.
2. **تست در مرورگر:**
   - تایید قرارگیری کالاها بر اساس زمان ایجاد (جدیدترین در ابتدا).
   - تست کلیک روی هر گزینه و سپس کلیک مجدد برای تاگل از صعودی به نزولی و برعکس.
   - تست مرتب‌سازی بر اساس زمان تغییر، کد کالا، لوکیشن و شرح کالا.

</div>
