<div dir="rtl" align="right">

# طرح جامع اصلاحات، بهینه‌سازی و ارتقای داشبورد انبارگردان (Counter Dashboard Enhancements)

این سند فنی شامل جزئیات پیاده‌سازی ۱۲ مورد عیب‌یابی‌شده به همراه ۳ نیازمندی جدید کاربر جهت بهبود عملکرد، سرعت، تجربه کاربری (UX) و قابلیت اطمینان در صفحه انبارگردان ([`counter-dashboard.ts`](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts) و [`counter-dashboard.html`](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.html)) می‌باشد.

---

## ۱. تصمیمات کلیدی و خروجی مصاحبه فنی (Key Decisions & Interview Outcomes)

> [!IMPORTANT]
> **۱. ارسال مستقیم کالا به سرپرست از نمای جزئیات (Direct Submit):**
> در صفحه جزئیات کالا دو دکمه قرار می‌گیرد: «ثبت موقت (آماده ارسال)» و «ثبت و ارسال به سرپرست». با کلیک روی ارسال به سرپرست، کالا بدون نیاز به رفتن به لیست و انتخاب مجدد، فوراً با یک کلیک (بدون تاییدیه مزاحم) به وضعیت `COUNTED` (یا `MANAGER_REVIEW`) ارتقا می‌یابد و کاربر به لیست بازمی‌گردد.

> [!IMPORTANT]
> **۲. تغییر نام «شمارش اولیه» به «آماده ارسال» و بازچینی چیپ‌ها:**
> عنوان «شمارش اولیه» در تمام بخش‌های رابط کاربری و وضعیت‌ها به **«آماده ارسال»** تغییر می‌کند و ترتیب چیپ‌ها به صورت زیر تنظیم می‌گردد:
> 1. دست‌نخورده (`pending`)
> 2. بازشماری (`recount`) [نمایش شرطی]
> 3. آماده ارسال (`initial`) [نمایش شرطی]
> 4. شمرده شده (`completed`)
> 5. همه موارد (`all`)

> [!TIP]
> **۳. نمایش شرطی چیپ‌های «آماده ارسال» و «بازشماری»:**
> چیپ‌های «آماده ارسال» و «بازشماری» تنها در صورتی نمایش داده می‌شوند که تعداد آن‌ها بزرگتر از صفر باشد (`count > 0`) یا فیلتر جاری صفحه باشند. در صورت صفر شدن تعداد، سیستم به طور خودکار و هوشمند کاربر را به تب پیش‌فرض **«دست‌نخورده»** هدایت می‌کند.

---

## ۲. خلاصه تغییرات پیشنهادی به تفکیک لایه‌ها (Proposed Changes)

```
warehouse-front/src/app/components/counter/counter-dashboard/
 ├── counter-dashboard.ts    [MODIFY] -> منطق فیلترها، سابمیت مستقیم، تبدیل ارقام فارسی، بهینه‌سازی ذخیره فیلدها و Long-Press
 └── counter-dashboard.html  [MODIFY] -> فوتر دو دکمه‌ای، نمایش شرطی چیپ‌ها، اصلاح لیبل‌ها و استایل پاپ‌اور سورت
```

---

### فاز ۱: منطق وضعیت‌ها، اعتبارسنجی فرم و ارسال مستقیم (State & Form Logic)

#### [MODIFY] [counter-dashboard.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts)
1. **نرمال‌سازی ارقام فارسی در ورودی شمارش:**
   - اعمال تابع `normalizeDigits` بر روی `countedBalanceStr` در زمان ذخیره یا ارسال تا ارقام کیبورد فارسی/عربی با خطای `NaN` مواجه نشوند.
2. **اصلاح وضعیت در صورت خالی کردن عدد شمارش:**
   - اگر کاربر عدد شمارش را پاک کرد (`countedBalanceStr === ''`) و وضعیت قبلی `INITIAL_COUNT` بود، وضعیت به `PENDING_COUNT` و `counted_balance` به `null` بازنشانی شود.
3. **متد جدید `submitDirectlyToSupervisor()`:**
   - ثبت عدد شمارش و ارسال مستقیم تسک به سرپرست:
     - در حالت آنلاین: فراخوانی `countTaskApi.bulkSubmit({ task_ids: [task.id] })` یا ثبت مستقیم.
     - در حالت آفلاین (`localFirst`): به‌روزرسانی محلی و فراخوانی `store.submitTasks([task], userId)`.
4. **اصلاح فیلتر تاریخ بر مبنای آخرین زمان تغییر (`updated_at`):**
   - مقایسه تاریخ فیلترهای «امروز»، «دیروز» و «این هفته» با `updated_at` (با fallback به `created_at`) تا فعالیت‌های همان روز مخفی نشوند.
5. **انتقال هوشمند در صورت خالی شدن چیپ‌های شرطی:**
   - در `applyFilters`، اگر `statusFilter === 'initial'` و `statusCounts.initial === 0`، یا `statusFilter === 'recount'` و `statusCounts.recount === 0`، فیلتر جاری خودکار به `'pending'` سوئیچ کند و URL همگام شود.

---

### فاز ۲: تعاملات، بهینه‌سازی عملکرد و مالتی‌سلکت (Performance & Interaction)

#### [MODIFY] [counter-dashboard.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts)
1. **بهینه‌سازی ذخیره فیلدهای سفارشی کالا (`saveExtraEditedFields`):**
   - مقایسه مقادیر فرم با مقادیر موجود در `selectedTask.item_details` و فقط ارسال درخواست HTTP PATCH در صورت وجود تغییر واقعی (`dirty checking`).
2. **پشتیبانی امن از حالت آفلاین برای ویرایش فیلدهای کالا:**
   - جلوگیری از نمایش خطای ناشی از عدم اتصال به شبکه در زمان ذخیره محلی.
3. **اصلاح تداخل Long-Press و کلیک:**
   - تنظیم `justLongPressed = true` در تمامی شرایط تایم‌اوت لمس، حتی اگر آیتم از قبل انتخاب شده باشد.
4. **حفظ حالت مالتی‌سلکت و جلوگیری از پرش ناخواسته به Detail:**
   - در `onTaskClick`، اگر `selectedTasks.size > 0` باشد، کلیک روی هر کارتی صرفاً وضعیت انتخاب آن را کنترل کند یا پیام راهنما دهد و وارد Detail نشود.
5. **اصلاح متد `toggleAll()`:**
   - انتخاب تنها تسک‌هایی که شمرده شده‌اند و وضعیت فقط‌خواندنی ندارند (`!this.isReadOnly(t)`).

---

### فاز ۳: رابط کاربری، تمپلیت و واکنش‌گرایی (UI & Template Enhancements)

#### [MODIFY] [counter-dashboard.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.html)
1. **طراحی فوتر جدید در نمای جزئیات (Detail View):**
   - ایجاد دو دکمه اصلی:
     - دکمه سبز رنگ: **«ثبت و ارسال به سرپرست»** (با آیکون ارسال)
     - دکمه بنفش رنگ: **«ثبت موقت (آماده ارسال)»**
     - دکمه خاکستری کناری: **«انصراف / بازگشت»**
2. **تغییر برچسب‌ها و رنگ چیپ «آماده ارسال»:**
   - تغییر تمامی متون «شمارش اولیه» به «آماده ارسال» در بج‌ها، چیپ‌ها و تاریخچه.
3. **نمایش شرطی چیپ‌های «بازشماری» و «آماده ارسال»:**
   - افزودن `*ngIf="statusCounts.recount > 0 || statusFilter === 'recount'"` برای چیپ بازشماری.
   - افزودن `*ngIf="statusCounts.initial > 0 || statusFilter === 'initial'"` برای چیپ آماده ارسال.
   - قرار گرفتن چیپ «آماده ارسال» بعد از «بازشماری».
4. **اصلاح نمایش موجودی سیستمی (`bal4miv`):**
   - اصلاح شرط به `selectedTask.item_details?.bal4miv !== null && selectedTask.item_details?.bal4miv !== undefined`.
5. **اصلاح موقعیت منوی پاپ‌اور سورت در صفحات موبایل:**
   - تنظیم استایل دراپ‌داون سورت جهت جلوگیری از سرریز شدن در نمایشگرهای باریک.

---

## ۳. جدول مقایسه‌ای وضعیت قبل و بعد (Comparison Matrix)

| ویژگی | وضعیت فعلی | وضعیت هدف پس از پیاده‌سازی |
| :--- | :--- | :--- |
| **ارسال به سرپرست از Detail** | ❌ وجود ندارد (باید به لیست بازگشته و انتخاب گروهی کرد) | ✅ دکمه مستقیم «ثبت و ارسال به سرپرست» در فوتر با یک کلیک |
| **عنوان وضعیت ثبت موقت** | «شمارش اولیه» | **«آماده ارسال»** |
| **نمایش چیپ بازشماری و آماده ارسال** | همیشه نمایش داده می‌شوند (حتی با شمارنده صفر) | **نمایش شرطی (فقط در صورت وجود آیتم > ۰)** |
| **ترتیب چیپ‌ها** | دست‌نخورده ← شمارش اولیه ← بازشماری ← شمرده‌شده | **دست‌نخورده ← بازشماری ← آماده ارسال ← شمرده‌شده** |
| **ارقام فارسی کیبورد** | خطای مقدار نامعتبر | **پشتیبانی کامل و تبدیل خودکار به انگلیسی** |
| **پاک کردن عدد شمارش** | گیر افتادن در وضعیت INITIAL_COUNT | **بازگشت خودکار به PENDING_COUNT (دست‌نخورده)** |
| **درخواست‌های PATCH کالا** | ارسال در هر ذخیره پیش‌نویس | **فقط در صورت تغییر واقعی فیلدها (Dirty Check)** |
| **فیلتر تاریخ** | مقایسه با تاریخ تخصیص قدیمی (`created_at`) | **مقایسه با آخرین زمان فعالیت (`updated_at`)** |

---

## ۴. برنامه اعتبارسنجی و تست (Verification Plan)

### تست‌های خودکار و بیلد:
- اجرای `npm run build` جهت اطمینان از صفر بودن خطاهای TypeScript و تطابق کامل تمپلیت HTML با کلاس کامپوننت.

### سناریوهای تست دستی:
1. **تست ارقام فارسی:** تایپ عدد فارسی (مثلاً «۱۵») در صفحه ثبت شمارش و تایید ذخیره شدن صحیح.
2. **تست ارسال مستقیم:** باز کردن یک کالای دست‌نخورده، وارد کردن عدد و کلیک روی «ثبت و ارسال به سرپرست»؛ بررسی تغییر وضعیت به `COUNTED` در لیست.
3. **تست نمایش شرطی چیپ‌ها:** بررسی اینکه در صورت نبود کالای آماده ارسال یا بازشماری، چیپ‌های مربوطه پنهان هستند و با ثبت موقت یک کالا، چیپ «آماده ارسال» فوراً ظاهر می‌شود.
4. **تست پاک کردن مقدار:** باز کردن کالای آماده ارسال و پاک کردن عدد آن و بررسی بازگشت آن به تب دست‌نخورده.

</div>
