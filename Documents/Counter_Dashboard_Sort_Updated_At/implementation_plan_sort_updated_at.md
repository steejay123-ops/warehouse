<div dir="rtl" align="right">

# طرح اجرایی تغییر مرتب‌سازی پیش‌فرض داشبورد انبارگردان به «آخرین زمان تغییر» (Counter Dashboard Default Sort: Updated At)

این طرح بر اساس مصوبات نشست تعاملی طراحی (`/grill-me`) جهت ارتقای اولویت‌بندی و دسترسی سریع انبارگردان به آخرین اقلام تغییریافته و بازشماری‌شده تنظیم شده است.

---

## ۱. اهداف و نیازمندی‌ها (Requirements & Scope)

1. **تغییر مرتب‌سازی پیش‌فرض به `updated_at` (آخرین زمان تغییر):**
   * هنگام ورود به صفحه انبارگردان، اقلام به صورت پیش‌فرض بر اساس آخرین تاریخ و زمان تغییر (`updated_at`) مرتب می‌شوند.
   * در نتیجه، هر کالایی که انبارگردان پیش‌نویس آن را ثبت کرده، یا سرپرست آن را برای بازشماری عودت داده، مستقیماً در صدر لیست قرار می‌گیرد.
2. **جهت پیش‌فرض نزولی (`sortDirection: 'desc'`):**
   * نمایش از جدیدترین تغییر به قدیمی‌ترین.
3. **بازآرایی منوی کشویی مرتب‌سازی:**
   * انتقال گزینه «آخرین زمان تغییر» به ردیف اول (صدر منوی دراپ‌داون).
   * نمایش وضعیت برچسب فعال به صورت «آخرین تغییر: جدیدترین» در هدر بخش مرتب‌سازی.
4. **همگام‌سازی تمیز با URL مرورگر:**
   * در صورت بودن روی مقدار پیش‌فرض (`updated_at` با جهت `desc`)، پارامتر اضافی `sort` در آدرس URL قرار نمی‌گیرد و سایر انتخاب‌ها به درستی در URL ذخیره و بازیابی می‌شوند.

---

## ۲. بررسی نیازمندی به تایید کاربر (User Review Required)

> [!IMPORTANT]
> **نکات مصوب در نشست طراحی:**
> * پیش‌فرض مرتب‌سازی روی `updated_at` با جهت `desc` (جدیدترین به قدیمی‌ترین) خواهد بود.
> * گزینه «آخرین زمان تغییر» در ابتدای منوی کشویی سورت قرار خواهد گرفت.

---

## ۳. تغییرات پیشنهادی در فایل‌ها (Proposed Changes)

| ردیف | فایل | شرح تغییرات |
| :--- | :--- | :--- |
| **۱** | [counter-dashboard.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts) | تغییر مقدار اولیه `sortField` به `'updated_at'` و `sortDirection` به `'desc'`، به‌روزرسانی مدیریت پارامترهای URL |
| **۲** | [counter-dashboard.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.html) | انتقال گزینه «آخرین زمان تغییر» به ابتدای لیست منوی کشویی مرتب‌سازی |

---

## ۴. فازبندی اجرایی گام‌به‌گام (Implementation Phases)

### 🔹 فاز ۱: منطق کامپوننت و مدیریت URL (TypeScript Logic)
- تنظیم `sortField: 'updated_at'` و `sortDirection: 'desc'` در تعاریف اولیه کامپوننت.
- به‌روزرسانی متدهای `updateUrlState` و `syncStateFromUrl` برای در نظر گرفتن `updated_at` به عنوان پیش‌فرض تمیز URL.

### 🔹 فاز ۲: ساختار قالب و منوی دراپ‌داون (HTML Template)
- قرار دادن گزینه «آخرین زمان تغییر» در صدر منوی کشویی سورت.

### 🔹 فاز ۳: تست و اعتبارسنجی (Verification & Build Check)
- اجرای تست بیلد فرانت‌اند (`npm run build`).
- راستی‌آزمایی عملکرد مرتب‌سازی هنگام ثبت پیش‌نویس، سوییچ بین فیلدها و بازخوانی صفحه.

</div>
