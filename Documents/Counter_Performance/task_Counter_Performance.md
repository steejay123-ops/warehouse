<div dir="rtl" align="right">

# لیست وظایف: بهبود عملکرد پنل انبارگردان

- `[ ]` تبدیل متدهای آماری `completedTasksCount` و `remainingTasksCount` از `get` به متغیر در `counter-dashboard.ts`.
- `[ ]` ایجاد متغیر `filteredTasks` در `counter-dashboard.ts`.
- `[ ]` ایجاد تابع `applyFilters()` برای محاسبه مجدد آمارها در زمان دریافت کالاها یا تغییر تب در `counter-dashboard.ts`.
- `[ ]` فراخوانی `applyFilters()` درون `loadTasks` و `setFilter`.
- `[ ]` ایجاد تابع `trackByTaskId` در `counter-dashboard.ts`.
- `[ ]` بروزرسانی متدهای `pendingTasks` و `readyToSubmitCount` (تبدیل به متغیر).
- `[ ]` اعمال `trackBy: trackByTaskId` درون حلقه `*ngFor` در فایل `counter-dashboard.html`.
- `[ ]` کامپایل پروژه و اطمینان از عدم وجود خطای TypeScript.

</div>
