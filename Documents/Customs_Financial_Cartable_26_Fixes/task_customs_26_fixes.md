<div dir="rtl" align="right">

# چک‌لیست اجرایی پیاده‌سازی ۲۶ مورد کارتابل مالی و مدارک (`Customs Dashboard`)

- [x] <!-- id: phase1 --> **فاز ۱: امنیت داده، تراکنش‌ها و فیلترهای بک‌اند جنگو**
  - [x] <!-- id: task1 --> ۱. اضافه کردن `updated_at = timezone.now()` و قرار دادن `updated_at` در `DocTask.objects.bulk_update` در متد `bulk_submit`
  - [x] <!-- id: task2 --> ۲. افزودن تراکنش اتمیک `with transaction.atomic():` و قفل `select_for_update()` در متدهای `bulk_submit` و `claim_tasks`
  - [x] <!-- id: task3 --> ۳. پردازش و اعمال فیلترهای جستجو (`q`)، وضعیت (`status`) و تاریخ (`date`) در متد `DocTaskViewSet.get_queryset`
  - [x] <!-- id: guardian1 --> 🛡️ **بازبینی و تایید ایجنت نگهبان برای فاز ۱** (تایید ۱۰۰٪ و پاس شدن کامل ۲۰ تست بک‌اند)

- [x] <!-- id: phase2 --> **فاز ۲: چرخه حیات، ارسال سریع، پایداری همگام‌سازی و خروجی اکسل**
  - [x] <!-- id: task4 --> ۴. افزودن دکمه «ارسال سریع تک‌کلیکه» (`Quick Inline Submit`) روی کارت‌های دارای پیش‌نویس در لیست
  - [x] <!-- id: task5 --> ۵. رفرش خودکار لیست مخزن اسناد (`loadPoolTasks(false)`) پس از به عهده گرفتن در حالت سروری
  - [x] <!-- id: task6 --> ۶. مدیریت پایداری آفلاین برای تسک‌های فاقد `sync_id` در `doc-task-store.ts`
  - [x] <!-- id: task7 --> ۷. ارسال صریح پارامترهای فعال `q`، `status`، `date` و `warehouse_id` در متد `executeExport`
  - [x] <!-- id: task8 --> ۸. اصلاح وضعیت اقلام برگشتیِ ویرایش‌شده به پیش‌نویس (کادر کهربایی و برچسب تفکیک‌شده)
  - [x] <!-- id: task9 --> ۹. ریست کردن پرچم‌های لود دیتای تب‌ها (`tasksLoaded` و `poolTasksLoaded`) در زمان تغییر انبار
  - [x] <!-- id: task10 --> ۱۰. ارسال صریح `warehouse_id` در متد `submitCurrentTask`
  - [x] <!-- id: guardian2 --> 🛡️ **بازبینی و تایید ایجنت نگهبان برای فاز ۲** (تایید ۱۰۰٪ منطق همگام‌سازی و چرخه حیات)

- [x] <!-- id: phase3 --> **فاز ۳: موتور جستجو، سورتینگ الفبایی و فیلتر سریع لوکیشن**
  - [x] <!-- id: task11 --> ۱۱. افزودن جستجوی شماره فنی کالا (`item_no`) در موتور جستجوی متنی و اسکنر تک‌بارکد
  - [x] <!-- id: task12 --> ۱۲. نرمال‌سازی الف و آ کلاه‌دار (`replace(/[آأإ]/g, 'ا')`) در متد `normalizeText`
  - [x] <!-- id: task13 --> ۱۳. اصلاح و استانداردسازی سورت الفبایی شرح کالا با `localeCompare('fa')`
  - [x] <!-- id: task14 --> ۱۴. پیاده‌سازی متغیر و فیلتر سریع بر اساس محدوده لوکیشن / قفسه انبار (`locationFilter`)
  - [x] <!-- id: task15 --> ۱۵. افزودن کلید «پاک کردن جستجو» و پیام راهنما در حالت خالی شدن نتایج تب مخزن اسناد
  - [x] <!-- id: task16 --> ۱۶. افزودن مترادف‌های شماره فنی کالا (`item_no`) در متد `mapHeaderToKey` پارسر اسکنر دسته‌ای
  - [x] <!-- id: guardian3 --> 🛡️ **بازبینی و تایید ایجنت نگهبان برای فاز ۳** (تایید ۱۰۰٪ موتور جستجو و نرمال‌سازی)

- [x] <!-- id: phase4 --> **فاز ۴: فرم جزئیات، محاسبات مالی و تعاملات وب‌سوکت**
  - [x] <!-- id: task17 --> ۱۷. تصحیح فرمول محاسبه ارزش کل (`calculateTotalValue`) با اضافه کردن فیلد اصلی `balance` کالا
  - [x] <!-- id: task18 --> ۱۸. تبدیل متد `saveExtraEditedFields` به یک متد `async` قابل انتظار (`await`) همراه با مدیریت خطای پایدار
  - [x] <!-- id: task19 --> ۱۹. تبدیل تاریخ میلادی بازگشتی از سرور (`invoice_date`) به شمسی در باز شدن فرم جزئیات
  - [x] <!-- id: task20 --> ۲۰. اعتبارسنجی لمس طولانی و محدودسازی انتخاب گروهی به اقلام قابل ارسال (`isTaskSelectable`)
  - [x] <!-- id: task21 --> ۲۱. افزودن گارد بررسی آنلاین بودن (`navigator.onLine`) قبل از تحویل مخزن در حالت سروری
  - [x] <!-- id: task22 --> ۲۲. اصلاح اسکن دسته‌ای در حالت لوکال‌فرست جهت استفاده از `this.store.claimTasks`
  - [x] <!-- id: task23 --> ۲۳. مدیریت دریافت وب‌سوکت `doc_task_update` برای فرم باز جاری و نمایش هشدار یا رفرش این‌پلیس
  - [x] <!-- id: guardian4 --> 🛡️ **بازبینی و تایید ایجنت نگهبان برای فاز ۴** (تایید ۱۰۰٪ تعاملات و محاسبات مالی)

- [x] <!-- id: phase5 --> **فاز ۵: بومی‌سازی تقویم، ظواهر هدر و راستی‌آزمایی با مرورگر**
  - [x] <!-- id: task24 --> ۲۴. ایمپورت `PersianDatePipe` و نمایش تاریخ و ساعت شمسی در تاریخچه بررسی‌ها
  - [x] <!-- id: task25 --> ۲۵. مستقل‌سازی کادر اطلاعات پایه کالا از شرط `visibleInfoFields.length > 0`
  - [x] <!-- id: task26 --> ۲۶. نمایش برچسب واحد سنجش کالا (`unit`) در کارت‌های تب مخزن اسناد
  - [x] <!-- id: task27 --> ۲۷. افزودن عنوان رسمی و آیکون «کارتابل مالی و مدارک» در بالای صفحه
  - [x] <!-- id: task28 --> ۲۸. نمایش پیام راهنما در صورت اسکن بارکد در زمان باز بودن فرم جزئیات
  - [x] <!-- id: guardian5 --> 🛡️ **بازبینی نهایی ایجنت نگهبان برای فاز ۵** (تایید ۱۰۰٪ بیلد بدون خطا و تست‌های مرورگر)
  - [x] <!-- id: browser_testing --> 🌐 **اجرای تست تعاملی مرورگر و راستی‌آزمایی ۲۶ مورد** (تایید ۱۰۰٪ در محیط مرورگر)

</div>
