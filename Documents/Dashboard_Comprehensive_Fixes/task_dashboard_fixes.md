<div dir="rtl" align="right">

# لیست وظایف: اصلاح و بازطراحی جامع داشبورد مانیتورینگ انبار

### فاز ۱: لایه بک‌اند (Django / DRF)
- [x] <!-- id: 0 --> ۱. اصلاح `get_permissions` در `ItemViewSet` در فایل `inventory/views.py` و مجاز کردن `dashboard_stats` برای کاربران احرازشده
- [x] <!-- id: 1 --> ۲. اصلاح محاسبات متد `dashboard_stats` (افزودن `counted` و `docs_approved` به بخش `overall` و تنظیم نام روزهای فارسی/شمسی بر اساس تاریخ روز)

### فاز ۲: لایه فرانت‌اند (Angular / Component)
- [x] <!-- id: 2 --> ۳. به‌روزرسانی `dashboard.ts` (خواندن `totalCounted` مستقیم از پاسخ API، سابسکرایب به تغییر انبار، مدیریت خطا و ترند روزانه)
- [x] <!-- id: 3 --> ۴. اصلاح و بازسازی ساختار HTML در `dashboard.html` (تفکیک ردیف‌ها، تراز دقیق خط تارگت، قیف عملیات و اکشن سنتر تعاملی)
- [x] <!-- id: 4 --> ۵. افزودن انیمیشن `animate-shine` و استایل‌های بهینه‌ساز در `dashboard.css`

### فاز ۳: اعتبارسنجی، تست و مستندسازی
- [x] <!-- id: 5 --> ۶. بررسی ترمینال و تست فراخوانی ای‌پی‌آی داشبورد بدون خطای ۵۰۰
- [x] <!-- id: 6 --> ۷. تست سوییچر انبار و بارگذاری واکنش‌پذیر و ناوبری اکشن سنتر
- [x] <!-- id: 7 --> ۸. ایجاد مستند گزارش و walkthrough نهایی طبق پروتکل DUAL-SAVE

</div>
