<div dir="rtl" align="right">

# لیست وظایف: پیاده‌سازی ستون‌های اکسل داینامیک

- `[/]` **مرحله ۱: بروزرسانی بک‌اند (Backend)**
  - `[ ]` بک‌اند: تغییر منطق هدر در `export_excel` برای قرار دادن نام فارسی در ردیف ۱ و نام واقعی فیلد در ردیف ۲.
  - `[ ]` بک‌اند: اضافه کردن متد `export_columns` به `ItemViewSet` برای ارسال نام تمام فیلدها.
- `[ ]` **مرحله ۲: یکپارچه‌سازی فرانت‌اند (Frontend)**
  - `[ ]` فرانت‌اند: اضافه‌کردن متد `getExportColumns` به `ItemApiService`.
  - `[ ]` فرانت‌اند: تغییر `availableExportColumns` در `dispatch.ts` و مقداردهی داینامیک آن از طریق بک‌اند در هنگام بالا آمدن کامپوننت.
- `[ ]` **مرحله ۳: بررسی و تست نهایی**
  - `[ ]` تست: اطمینان از نمایش تمام فیلدها (شامل `id`) در مودال.
  - `[ ]` تست: دانلود فایل و بررسی ردیف اول (فارسی) و دوم (دیتابیس).

</div>
