<div dir="rtl" align="right">

# طرح جامع، ارتقایافته و فازبندی‌شده بازآرایی تخصیص کالا و هسته سیستم (نسخه تفصیلی)

این سند نقشه راه نهایی برای ارتقا و اصلاح اساسی صفحه تخصیص کالا (`Dispatch`) و زیرساخت‌های کلان نرم‌افزار است. طبق دستورالعمل شما، ساختار اجرایی بر پایه **نظارت دوگانه و تایید موشکافانه ایجنت دوم (Peer Review & Audit Protocol)** طراحی شده است.

---

## پروتکل نظارت دوگانه و تایید مرحله‌به‌مرحله (Dual-Agent Peer Review Protocol)

> [!IMPORTANT]
> **قانون سخت‌گیرانه عبور از فازها (Gate Verification Rule):**
> 1. در پایان هر فاز، **ایجنت اول (مجری پیاده‌سازی)** تغییرات را اعمال و تست اولیه را انجام می‌دهد.
> 2. **ایجنت دوم (ناظر مستقل کیفیت، امنیت و معماری)** کلیه فایل‌های اصلاح‌شده، خطاهای کامپایل، تست‌های رگرسیون و یکپارچگی داده‌ها را به‌صورت خط‌به‌خط ممیزی می‌کند.
> 3. **شرط ورود به فاز بعدی:** فقط در صورت **تایید سبز و ۱۰۰٪ هر دو ایجنت**، گزارش تایید به کاربر ارائه شده و ورود به فاز بعد امکان‌پذیر خواهد بود.
> 4. **در صورت رد توسط ایجنت دوم:** دلایل رد، خطاهای احتمالی و موارد جامانده فوراً استخراج شده و فرآیند اصلاح در همان فاز تکرار می‌شود تا تاییدیه کامل اخذ گردد.

---

## ماتریس جامع فازهای ۶‌گانه و معیارهای تایید

| فاز | عنوان فاز | لایه‌ها و فایل‌های اصلی | خروجی کلیدی و معیار تایید ایجنت ناظر |
| :--- | :--- | :--- | :--- |
| **فاز ۱** | **پاکسازی پیش‌شرط چاپ لیبل** | `dispatch.html`, `dispatch.ts`, `settings.html`, `wh-settings.*`, `services.py` | حذف ۱۰۰٪ متون و شروط مسدودکننده ارجاع، حذف تنظیم `default_tag_status` و تطبیق مقادیر فارسی وضعیت لیبل. |
| **فاز ۲** | **حذف منطق حذف نرم (Soft-Delete)** | `smart-delete-modal.*`, `dispatch.*`, `projects.*`, `users.*` | حذف کامل تب غیرفعال‌سازی از مودال هوشمند حذف، مستقیم‌سازی حذف فیزیکی با حفظ تحلیل اثرات و تایمر امنیتی. |
| **فاز ۳** | **موتور خروجی اکسل حجیم (ZIP / Multi-file)** | `inventory/views.py`, `dispatch.ts`, `item-api.service.ts` | حذف هاردکد ۱۰۰ هزار، پیاده‌سازی تفکیک پارت‌های ۱۰۰ هزارتایی با `openpyxl(write_only)` در قالب بایگانی ZIP تا ۱ میلیون رکورد. |
| **فاز ۴** | **اصلاح باگ‌های هسته، تگ‌ها و فیلترها** | `dispatch.ts`, `dispatch.html` | اصلاح شرط‌های تودرتوی سرپرست، جلوگیری از تخریب تگ‌ها در صفحات دیگر، رفع بن‌بست `__NONE__` و تکمیل لیست کاربران. |
| **فاز ۵** | **ستون‌های داینامیک و فیلترینگ هوشمند** | `dispatch.ts`, `dispatch.html`, `data-table.component.ts` | لود داینامیک تمام فیلدهای استاندارد + داینامیک انبار، دسته‌بندی موضوعی ۶‌گانه، ۴ نمای پیش‌فرض سریع و فیلتر خودکار بر اساس تایپ داده. |
| **فاز ۶** | **اعتبارسنجی جامع، بیلد و تاییدیه نهایی** | فرانت‌اند و بک‌اند | اجرای موفق `ng build`، بررسی تست‌های سرور، تست یکپارچه تمام سناریوها و صدور تاییدیه نهایی مشترک دو ایجنت. |

---

## جزئیات تفصیلی فازهای اجرایی

### فاز ۱: پاکسازی پیش‌شرط چاپ لیبل از سراسر سیستم
1. **پاکسازی UI تخصیص:** حذف متن مسدودکننده خط ۸۹ در [dispatch.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.html) («فقط رکوردهایی که لیبل آنها چاپ شده است قابل ارجاع هستند»).
2. **پاکسازی فرم‌های تنظیمات:**
   * حذف انتخابگر `default_tag_status` از [settings.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/settings/settings.html).
   * حذف انتخابگر `default_tag_status` از [wh-settings.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/wh-settings/wh-settings.html) و کلیدهای متناظر در [wh-settings.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/wh-settings/wh-settings.ts).
3. **تطهیر سرویس‌های بک‌اند:** پاکسازی متغیر `default_tag_status` در [services.py](file:///e:/warehouse%20project/warehouse-backend/warehouses/services.py) و بخش ایمپورت [views.py](file:///e:/warehouse%20project/warehouse-backend/inventory/views.py).
4. **اصلاح نگاشت وضعیت لیبل:** اصلاح خطوط ۳۰۸، ۳۵۱ و ۴۶۸ در [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts) برای هماهنگی مقادیر فارسی دیتابیس (`چاپ شده` / `چاپ نشده` / `چاپ مجدد`) با استایل‌های جدول.
* **ممیزی ایجنت ناظر:** تست ارجاع کالای چاپ‌نشده و بررسی عدم وجود هیچ مانع یا خطای منطقی.

---

### فاز ۲: حذف منطق حذف نرم (Soft-Delete) و یکپارچه‌سازی حذف مستقیم
1. **بازطراحی کامپوننت [SmartDeleteModalComponent](file:///e:/warehouse%20project/warehouse-front/src/app/shared/components/smart-delete-modal/smart-delete-modal.ts):**
   * حذف وضعیت `activeTab: 'soft' | 'hard'` و حذف تب غیرفعال‌سازی از قالب HTML.
   * حذف خروجی `@Output() softDelete`.
   * ورود مستقیم به صفحه هشدار، تحلیل اثرات وابستگی‌ها (`deleteImpactUrl`)، شمارش معکوس ۳ ثانیه‌ای و الزام به نوشتن کلمه «حذف».
2. **پاکسازی متدهای زائد در صفحات:**
   * حذف توابع `handleSoftDelete()` و بایندینگ‌های آن از [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts)، [projects.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/projects/projects.ts)، و [users.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/users/users.ts).
* **ممیزی ایجنت ناظر:** اطمینان از حذف کامل رفرنس‌های `softDelete` در کامپایل تایپ‌اسکریپت و بررسی صحت باز شدن مودال حذف در هر ۳ صفحه.

---

### فاز ۳: پیاده‌سازی موتور خروجی اکسل هوشمند (ZIP / Multi-file تا ۱ میلیون رکورد)
1. **بک‌اند ([inventory/views.py](file:///e:/warehouse%20project/warehouse-backend/inventory/views.py) - متد `export_excel`):**
   * بررسی `total_count = queryset.count()`.
   * **اگر `total_count <= 100,000`:** تولید فایل اکسل با `openpyxl(write_only=True)` و ارسال مستقیم با هدر `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`.
   * **اگر `total_count > 100,000` (تا ۱ میلیون رکورد):**
     * ایجاد جریان فشرده `zipfile.ZipFile` در حافظه.
     * پیمایش بهینه دیتابیس با `queryset.iterator(chunk_size=5000)`.
     * تفکیک خودکار رکوردها به پارت‌های ۱۰۰ هزارتایی (`export_part_1.xlsx`, `export_part_2.xlsx`, ...).
     * بسته‌بندی فایل‌ها در بایگانی ZIP و ارسال با `Content-Type: application/zip` و هدرهای مربوطه.
2. **فرانت‌اند ([dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts)):**
   * حذف هاردکد `filters['page_size'] = 100000`.
   * خواندن نوع فایل برگشتی (`blob.type` یا نام فایل) و ذخیره‌سازی با پسوند دقیق `.xlsx` یا `.zip`.
* **ممیزی ایجنت ناظر:** بررسی عملکرد مدیریت حافظه در بک‌اند، نبود Memory Leak، و تست دانلود موفق برای داده‌های کم‌حجم و پرحجم.

---

### فاز ۴: اصلاح باگ‌های منطقی، فیلترها و عملیات چندصفحه‌ای
1. **اصلاح شرط‌های سرپرست در `ngOnInit`:** مجزا کردن شرط‌های `requireSupervisor` و `requireDocSupervisor` در خطوط ۱۴۳ تا ۱۵۲ [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts).
2. **اصلاح تگ‌گذاری چندصفحه‌ای:** اصلاح متدهای `applyBatchTags`، `removeTagFromSelected` و `clearBatchTags` تا با تکیه بر شناسه‌ها عملیات بهینه‌ای در بک‌اند انجام شود و تگ رکوردهای صفحات دیگر پاک نشود.
3. **اصلاح چاپ لیبل چندصفحه‌ای:** ارسال شناسه‌های انتخابی به کامپوننت طراح لیبل و اطمینان از چاپ کامل تمام رکوردهای انتخاب‌شده.
4. **رفع تداخل فیلتر سریع:** اصلاح `buildApiFilters` در [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts) جهت جلوگیری از شرط مسدودکننده `__NONE__` هنگام فیلتر کردن از هدر جدول.
5. **تکمیل دراپ‌داون کاربران:** اتصال فیلترهای تیم شمارش، اسناد و ایجادکنندگان به متغیرهای کامل `fieldWorkers`، `docWorkers` و `users`.
6. **پاکسازی کدهای تکراری:** حذف انتساب مضاعف `supervisor_assignee` در `executeFieldDispatch`.
* **ممیزی ایجنت ناظر:** تست سناریوی انتخاب رکوردها در ۲ صفحه مختلف، افزودن تگ و بازبینی سلامت داده‌ها.

---

### فاز ۵: ستون‌های داینامیک دسته‌بندی‌شده و فیلترینگ هوشمند جامع
1. **دریافت متادیتای جامع ستون‌ها:** دریافت لیست ۱۰۰٪ ستون‌های پایه و فیلدهای داینامیک هر انبار از `ItemApiService.getExportColumns(whId)`.
2. **دسته‌بندی موضوعی ۶‌گانه ستون‌ها:**
   * *۱. مشخصات و ردیابی کالا* (کد یکتا، شرح کالا، پکینگ لیست PL، سفارش خرید PO، پکیج PK، ردیف کالا، واحد سنجش، دیسیپلین، تگ فیزیکی، سایز، شماره درخواست)
   * *۲. موجودی و لوکیشن* (موجودی فیزیکی، موجودی مجاز MIV، لوکیشن قدیم، لوکیشن جدید)
   * *۳. خرید، تامین و فرآیند* (شماره HOV، تاریخ HOV، وضعیت MSR، سازنده Vendor، تامین‌کننده، شماره IRN، تقاضای خرید Indent، ردیف فرعی Item2، طبقه‌بندی انبار، ملاحظات)
   * *۴. مالی، قیمت و گمرک* (قیمت واحد، قیمت کالای مشابه، ارزش کل، ارز، نوع فاکتور، تاریخ فاکتور، شماره RTI، شماره RTI افزوده‌شده، ردیف فاکتور، صفحه فاکتور، تامین‌کننده اسناد، مسیر پوشه، هایپرلینک، وضعیت مهر، وضعیت امضا، فیلد گمرکی، آدرس گمرکی، صفحه گمرکی، توضیحات قیمت، ملاحظات صدور)
   * *۵. فیلدهای داینامیک انبار* (تمام فیلدهای پویای تعریف‌شده اختصاصی برای انبار فعال)
   * *۶. وضعیت‌های سیستمی و مدیریت* (وضعیت لیبل، تیم شمارش، فاز میدانی، تیم مدارک، فاز اسناد، تگ‌ها، مغایرت، شکستنی، سنگین، کنترل کیفی، تاریخ ایجاد، تاریخ بروزرسانی، ایجادکننده، ویرایش‌کننده، عملیات)
3. **الگوهای پیش‌فرض سریع (Preset Views):**
   * دکمه‌های سوئیچ سریع ستون‌ها: «نمای میدانی و شمارش»، «نمای مالی و اسناد»، «نمای جامع انبار»، و «نمای سفارشی کاربر» با ذخیره‌سازی در `ui_preferences`.
4. **فیلترینگ هوشمند بر اساس نوع داده:**
   * فیلدهای متنی: جستجوی متنی با Debounce هوشمند
   * فیلدهای عددی: فیلتر بازه‌ای Min/Max
   * فیلدهای تاریخ: تقویم شمسی و بازه تاریخی
   * فیلدهای وضعیتی/کاربری: چک‌باکس چندانتخابی Multi-select
* **ممیزی ایجنت ناظر:** بررسی کارکرد سوئیچ نماها، ذخیره ترجیحات در دیتابیس و صحت فیلتر هوشمند تمام ستون‌ها.

---

### فاز ۶: اعتبارسنجی نهایی، تست بیلد و تاییدیه رسمی
1. **اجرای تست کامپایل:** اجرای `npm run build` در فرانت‌اند و بررسی سلامت کامل فایل‌های خروجی بدون هیچ خطا یا هشداری.
2. **بررسی تست‌های بک‌اند:** اطمینان از سلامت سرویس‌های جانبی و عدم شکست سناریوهای قبلی.
3. **آزمون نهایی چرخه کامل (E2E Test):**
   * تست ارجاع مستقیم کالا بدون پیش‌شرط لیبل
   * تست حذف مستقیم بدون تب غیرفعال‌سازی
   * تست خروجی اکسل عادی و چندفایلی ZIP
   * تست تگ‌گذاری چندصفحه‌ای
   * تست تغییر و فیلتر ستون‌های داینامیک
4. **صدور گزارش پایانی و تاییدیه دوگانه مشترک.**

---

> [!IMPORTANT]
> **قانون تایید و گام‌به‌گام:** هیچ کدی تغییر نخواهد کرد مگر با ارسال دستور صریح **«تایید»** یا **«شروع»** از جانب شما. پس از تایید شما، هر فاز به‌ترتیب اجرا، توسط ایجنت دوم ممیزی و گزارش دقیق آن خدمت شما تقدیم خواهد شد.

</div>
