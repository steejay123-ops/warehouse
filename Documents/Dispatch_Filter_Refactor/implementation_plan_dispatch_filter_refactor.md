<div dir="rtl" align="right">

# جایگزینی چک‌باکس‌های فیلتر با مودال "فیلتر سریع" (فیلترهای برچسبی)

این طرح برای بهبود رابط کاربری (UI) بخش تخصیص و هدایت کالاها در نظر گرفته شده است. چک‌باکس‌های قدیمی (شمرده شده و در حال شمارش) جای خود را به یک پنجره مقیاس‌پذیر می‌دهند که با کلیک روی یک دکمه باز می‌شود و شامل فیلترهای برچسبی (Chips) خواهد بود.

## User Review Required

> [!IMPORTANT]
> **نیاز به تایید مکانیزم بک‌اند (Backend Mechanism)**
> در حال حاضر بک‌اند فیلترینگ لیست را بر اساس دو فلگ `show_counted` و `show_counting` انجام می‌دهد. اگر هر دو `false` باشند، فقط لیست کالاهای «در انتظار تخصیص» برمی‌گردد. ما در طراحی جدید، برچسب‌های (Chips) منطبق با همین شرایط را روی UI پیاده می‌کنیم تا نیازی به تغییر بک‌اند نباشد.

## Open Questions

> [!QUESTION]
> آیا مایلید اعمال فیلترها **لحظه‌ای (Instant)** باشد (یعنی با هر کلیک روی یک برچسب، جدول همان لحظه به‌روزرسانی شود) یا اینکه می‌خواهید کاربر پس از انتخاب برچسب‌ها، دکمه **«اعمال فیلتر»** را کلیک کند؟ (در طرح فعلی من حالت لحظه‌ای را به عنوان پیش‌فرض در نظر گرفته‌ام چون تجربه کاربری سریع‌تری دارد).

## Proposed Changes

---

### Frontend Component: Dispatch (تخصیص کالا)

#### [MODIFY] [dispatch.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.html)
- حذف بخش چک‌باکس‌های قدیمی از سربرگ جدول.
- اضافه کردن دکمه **"فیلتر سریع"** با آیکون قیف (Funnel) در کنار دکمه‌های دیگر.
- پیاده‌سازی مودال جدید (`isQuickFilterModalOpen`) با ظاهری مدرن (شبیه به سایر مودال‌های سیستم).
- قرار دادن فیلترهای برچسبی با استایل مدرن و پشتیبانی از انتخاب چندگانه (Multi-select) برای وضعیت‌های زیر:
  - **در انتظار تخصیص**
  - **در حال شمارش**
  - **شمرده شده**

#### [MODIFY] [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts)
- اضافه کردن متغیر `isQuickFilterModalOpen` برای کنترل نمایش مودال.
- تغییر منطق `showCountedItems` و `showCountingItems` به متغیرهای جدیدتر و منعطف‌تر (یک آرایه از وضعیت‌های انتخاب شده).
- افزودن توابع `toggleQuickFilter()`، `applyQuickFilters()` و منطق به‌روزرسانی `loadItems()` بر اساس برچسب‌های انتخاب شده.
- اتصال وضعیت فیلترها به State سیستم برای ذخیره‌سازی Preferences کاربر.

## Verification Plan

### Manual Verification
- ورود به منوی تخصیص و هدایت هوشمند.
- بررسی عملکرد باز و بسته شدن مودال "فیلتر سریع".
- انتخاب وضعیت‌های مختلف و مشاهده تغییر دیتا به درستی و بدون رفرش کل صفحه.
- اطمینان از عملکرد دکمه "پاک کردن فیلترها" که وضعیت جدول را به حالت اولیه (نمایش پیش‌فرض) بازمی‌گرداند.

</div>
