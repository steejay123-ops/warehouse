<div dir="rtl" align="right">

# وظایف اجرای تفکیک فیلترها و رنگ‌های داشبورد تخصیص کالا

## ۱. بک‌اند (تغییر در نحوه فیلتر کردن)
- `[x]` در `views.py` متد `ItemViewSet.get_queryset`: دریافت دو پارامتر `show_counted` و `show_counting`
- `[x]` فیلتر کردن موارد مربوطه بر اساس این دو متغیر در `views.py`

## ۲. فرانت‌اند (تنظیم متغیرها و ارسال به سرور)
- `[x]` در `dispatch.ts`: اضافه‌کردن متغیر `showCountingItems` با مقدار پیش‌فرض `false`
- `[x]` در `dispatch.ts`: تغییر متد `loadItems()` برای ارسال دو پارامتر `show_counted` و `show_counting` به سرور
- `[x]` در `dispatch.ts`: تغییر رنگ‌ها در متد `getRowClass()` به `bg-orange-100`, `bg-emerald-100`, `bg-blue-100`

## ۳. فرانت‌اند (رابط کاربری)
- `[x]` در `dispatch.html`: تبدیل یک چک‌باکس به دو چک‌باکس با استایل‌های مجزا (سبز و آبی)

## ۴. بررسی نهایی
- `[x]` اطمینان از صحت عملکرد فیلترها
- `[x]` اطمینان از نداشتن خطای کامپایل در تایپ‌اسکریپت
- `[x]` نوشتن فایل‌های walkthrough و مستندات محلی

</div>
