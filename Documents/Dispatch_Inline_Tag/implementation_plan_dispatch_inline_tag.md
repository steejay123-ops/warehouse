<div dir="rtl" align="right">

# انتقال مدیریت تگ‌ها به صورت یکپارچه (Inline)

بر اساس بازخورد شما، قصد داریم پنجره پاپ‌آپ (Modal) «مدیریت تگ‌های رکوردهای منتخب» را حذف کرده و عملکرد آن را دقیقاً در فضای خالی موجود در بخش «۱. سازماندهی و لیبلینگ» در صفحه تخصیص کالا قرار دهیم.

## تغییراتی که نیاز به تأیید شما دارد (User Review Required)
> [!IMPORTANT]
> **محل قرارگیری دقیق:** تگ‌ها دقیقاً بین عنوان «۱. سازماندهی و لیبلینگ» و شمارنده «رکوردهای منتخب» قرار خواهند گرفت. 
> **دکمه مدیریت تگ:** دکمه فعلی «مدیریت تگ‌ها» که آیکون تگ دارد حذف خواهد شد، زیرا این بخش همیشه باز و در دسترس خواهد بود. وقتی رکوردی انتخاب نشده باشد، پیام «رکوردی انتخاب نشده است» نمایش داده می‌شود. آیا با این تغییرات موافق هستید؟

## سؤالات باز (Open Questions)
> [!NOTE]
> آیا می‌خواهید دکمه «پاک کردن همه تگ‌ها» همچنان حفظ شود؟ در طراحی جدید آن را به صورت یک آیکون کوچک سطل زباله یا دکمه متنی در کنار تگ‌ها قرار می‌دهیم.

## تغییرات پیشنهادی (Proposed Changes)

---

### کامپوننت Dispatch (تخصیص کالا)
این کامپوننت وظیفه مدیریت تخصیص‌ها را بر عهده دارد و تغییرات در فایل‌های HTML و TS آن اعمال خواهد شد.

#### [MODIFY] [dispatch.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.html)
- **حذف Modal:** کدهای مربوط به Modal مدیریت تگ‌ها در انتهای فایل حذف خواهند شد.
- **حذف دکمه تگ:** دکمه `openTagModal()` از کنار دکمه رفرش حذف می‌شود.
- **افزودن رابط کاربری یکپارچه (Inline UI):** ساختار تگ‌ها (شامل نمایش تگ‌های مشترک، اینپوت افزودن تگ و دکمه حذف همه) به زیر عنوان «۱. سازماندهی و لیبلینگ» منتقل می‌شود.
- **مدیریت فضا و اسکرول:** برای اینکه اگر تعداد تگ‌ها زیاد شد ساختار بخش به هم نریزد، از `max-height` و اسکرول داخلی استفاده خواهد شد و بقیه فضا را پر خواهد کرد `flex-1`.

#### [MODIFY] [dispatch.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/dispatch/dispatch.ts)
- متدهای `openTagModal` و `closeTagModal` حذف خواهند شد.
- متغیر `isTagModalOpen` حذف خواهد شد.
- متد `onSelectionChange` تغییر می‌کند تا به صورت خودکار `this.updateSelectedItemsTags()` را فراخوانی کند. بنابراین هر زمان که رکوردی در جدول انتخاب یا لغو انتخاب شود، لیست تگ‌های مشترک آپدیت می‌شود.
- در متدهای `applyBatchTags` و `removeTagFromSelected` و `clearBatchTags` و پس از لود داده‌ها، لیست تگ‌ها به‌روزرسانی می‌شود.

---

## طرح اعتبارسنجی (Verification Plan)

### اعتبارسنجی خودکار
- اجرای برنامه Angular بدون خطای کامپایل.

### اعتبارسنجی دستی
- مراجعه به صفحه تخصیص کالا و انتخاب چند رکورد از جدول.
- بررسی نمایش تگ‌های مشترک به صورت لحظه‌ای در کادر جدید.
- افزودن یک تگ جدید و اطمینان از اعمال آن.
- حذف یک تگ و بررسی صحت عملکرد.
- بررسی چیدمان صفحه و اطمینان از این موضوع که کادر دکمه‌های پرینت به پایین رانده نشود.

</div>
