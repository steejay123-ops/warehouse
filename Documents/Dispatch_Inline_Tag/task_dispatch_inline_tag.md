<div dir="rtl" align="right">

# لیست وظایف (Task List): انتقال مدیریت تگ‌ها به صفحه اصلی

- `[x]` **فایل `dispatch.html`**
  - `[x]` حذف کامل بخش Modal با آیدی `isTagModalOpen`.
  - `[x]` جایگزینی ساختار رابط کاربری تگ‌ها در فضای خالی بالای جدول.
  - `[x]` حذف دکمه «مدیریت تگ‌ها» از نوار کنترل جدول.
  - `[x]` افزودن پیام «هیچ رکوردی انتخاب نشده است» برای زمان‌هایی که رکوردی انتخاب نیست.

- `[x]` **فایل `dispatch.ts`**
  - `[x]` حذف متغیرها و متدهای مربوط به Modal (`isTagModalOpen`, `openTagModal`, `closeTagModal`).
  - `[x]` اضافه کردن فراخوانی `updateSelectedItemsTags()` درون متد `onSelectionChange`.
  - `[x]` بررسی متدهای `applyBatchTags` و `removeTagFromSelected` برای اطمینان از آپدیت شدن لیست پس از اجرای موفقیت‌آمیز.

- `[x]` **تست و بررسی**
  - `[x]` اطمینان از عدم به هم ریختگی قالب هنگام زیاد شدن تعداد تگ‌ها (اسکرول دار شدن بخش).
  - `[x]` تست افزودن، حذف و پاک کردن همه تگ‌ها.

</div>
