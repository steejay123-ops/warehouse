<div dir="rtl" align="right">

# لیست وظایف: رفع بدهی فنی فیلترهای تخصیص

- `[x]` بک‌اند (`views.py`): حذف خواندن پارامترهای `show_counted` و `show_counting` از ریکوئست.
- `[x]` بک‌اند (`views.py`): حذف لیست `excluded_statuses` و دستور `exclude` مربوط به آن.
- `[x]` فرانت‌اند (`dispatch.ts`): حذف مقداردهی `filters['show_counted']` و `filters['show_counting']` در تابع `loadItems`.
- `[x]` فرانت‌اند (`dispatch.ts`): حذف کدهای وابسته به این متغیرها از تابع `exportData`.

</div>
