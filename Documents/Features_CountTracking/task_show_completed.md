<div dir="rtl" align="right">

# لیست وظایف: نمایش کالاهای پایان‌یافته

## ۱. لاجیک بک‌اند
- `[x]` دریافت پارامتر `show_completed` از ریکوئست در متد `get_queryset` فایل `views.py`.
- `[x]` فیلتر نکردن وضعیت `FINAL_APPROVED` در صورتی که `show_completed=true` باشد.

## ۲. رابط کاربری (فرانت‌اند)
- `[x]` تعریف متغیر و تابع تغییر وضعیت در فایل `count-tracking.ts`.
- `[x]` اضافه کردن سوئیچ/دکمه تغییر وضعیت در فایل `count-tracking.html`.
- `[x]` ارسال پارامتر `show_completed` به سرور هنگام دریافت داده‌ها.

</div>
