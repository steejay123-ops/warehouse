<div dir="rtl" align="right">

# لیست وظایف پیاده‌سازی تخصیص مدیر

## ۱. پایگاه داده و بک‌اند
- `[x]` **مدل `CountTask`:** افزودن فیلد `assigned_manager` به فایل `models.py`.
- `[x]` **مایگریشن:** اجرای دستورات `makemigrations` و `migrate`.
- `[x]` **متد `bulk_assign`:** دریافت `manager_assignee` از کلاینت و اختصاص آن در زمان ساخت تسک (`views.py`).
- `[x]` **متد `get_queryset`:** افزودن شرط دیدن استخر مشترک (`assigned_manager__isnull=True`) برای مدیران.

## ۲. رابط کاربری (فرانت‌اند)
- `[x]` **ویو `dispatch.ts`:** تعریف متغیر `managers`، استخراج آن‌ها از لیست کاربران، و مدیریت `selectedManager`.
- `[x]` **ویو `dispatch.html`:** اضافه کردن منوی کشویی انتخاب مدیر در فرم ارجاع میدانی.

## ۳. بررسی و تست
- `[x]` اطمینان از صحت عملکرد سرور بک‌اند پس از مایگریشن.
- `[x]` بررسی لاگ‌های کامپایل فرانت‌اند.

</div>
