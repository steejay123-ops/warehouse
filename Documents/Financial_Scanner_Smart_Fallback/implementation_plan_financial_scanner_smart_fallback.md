# طرح اجرایی: ارتقای هوش تابع اسکنر و پیاده‌سازی فالبک هوشمند جستجو در کارتابل مالی

<div dir="rtl" align="right">

## ۱. هدف و خلاصه طرح (Goal & Summary)
هدف این طرح، ارتقای منطق پردازش اسکنر بارکدخوان (`onBarcodeScanned` و `CustomsScannerParser`) است تا:
1. تشخیص ساختار چندسطری/جدولی از تک‌بارکد با دقت بالا و بدون خطا انجام شود (جلوگیری از تشخیص اشتباه متن‌های دارای کاما یا خط تیره).
2. در روال تک‌بارکد، یک استراتژی سه‌مرحله‌ای (تطبیق مستقیم -> بررسی استخر -> فالبک به جستجوی همه‌جانبه روی تمام فیلدها) اعمال گردد تا کاربر هرگز با بن‌بست خطای قرمز مواجه نشود و بتواند با اسکن شرح یا مشخصات نیز کالا را پیدا کند.

---

## ۲. بررسی نیازمندی‌های کاربر و تصمیمات نهایی (Design Decisions)
- **روال هیبریدی و چندمرحله‌ای اسکن:**
  1. اولویت ۱: باز کردن مستقیم فرم در صورت تطبیق تک‌کالا با کد یکتا، PO، تگ یا فاکتور.
  2. اولویت ۲: بر عهده گرفتن خودکار در صورت وجود در استخر اقلام (Pool).
  3. اولویت ۳: قرار دادن خودکار مقدار در کادر جستجوی سراسری و فیلتر کردن تمام فیلدها (شرح، آدرس، نام انبار، فیلدهای پویا و یادداشت‌ها).
- **الزام وجود ساختار جدولی واقعی:** تشخیص چندسطری فقط در صورت وجود کاراکترهای کنترلی (`Chr 30/31`)، فرمت تب/اینتر، یا جفت جداکننده سطر و ستون، یا حداقل ۲ پایپ فعال می‌شود.
- **بازخورد هوشمند به کاربر:** نمایش تعداد نتایج فیلترشده و فوکوس خودکار به کادر جستجو در صورت عدم وجود تطبیق.

---

## ۳. تغییرات پیشنهادی در فایل‌ها (Proposed Changes)

### فرانت‌اند (`warehouse-front`)

#### [MODIFY] [customs-scanner-parser.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/customs/customs-scanner-parser.ts)
- بازنویسی و تقویت تابع `isMultiRowOrStructured` جهت الزام به وجود همزمان جداکننده سطر و ستون یا کاراکترهای کنترلی اسکنر.
- افزودن تگ (`tag`) و پارت نامبر به کلیدهای نگاشت هدر در `mapHeaderToKey`.

#### [MODIFY] [customs.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/customs/customs.ts)
- ارتقای متد `onBarcodeScanned` با پیاده‌سازی گام‌های سه‌گانه:
  1. جستجوی دقیق در `tasks` کارشناس (بر پایه `fa_unic_code`, `en_unic_code`, `id`, `po`, `inv_rti_number`, `tag`).
  2. در صورت تطبیق ۱ مورد: باز کردن فوری با `openDetail`.
  3. در صورت تطبیق چند مورد: قرار دادن در `searchQuery` و نمایش توست تعداد یافته‌ها.
  4. در صورت عدم تطبیق در کارتابل: بررسی `poolTasks` و اجرای روال Claim در صورت تطبیق.
  5. در صورت عدم تطبیق در استخر: فالبک خودکار به `this.searchQuery = code; this.applyFilters();` و اعلان هوشمند نتایج.

---

## ۴. برنامه راستی‌آزمایی و آزمون‌ها (Verification Plan)

### تست‌های دستی و خودکار:
1. **تست اسکن کد یکتا:** اسکن کد یکتای معتبر -> باز شدن فوری فرم جزئیات کالا.
2. **تست اسکن PO مشترک:** اسکن شماره سفارش خریدی که چند کالا دارد -> فیلتر شدن جدول و نمایش تعداد.
3. **تست اسکن متن بدون جداکننده (مثلاً بخشی از شرح کالا یا فیلد پویا):** اسکن متن -> قرارگیری در کادر جستجو و فیلتر شدن ردیف‌ها.
4. **تست اسکن کدهای دارای کاما یا خط تیره:** اطمینان از عدم باز شدن اشتباه مودال اسکن دسته‌ای.
5. **تست بیلد Angular:** اجرای `npm run build` برای تضمین عدم وجود خطای کامپایل.

</div>
