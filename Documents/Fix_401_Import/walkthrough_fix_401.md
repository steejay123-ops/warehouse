<div dir="rtl" align="right">

# گزارش اجرا: رفع خطای ۴۰۱ در مدیریت کالا

مطابق با تایید، کدهای بخش مدیریت اسناد که به صورت مستقیم و غیراستاندارد به `localStorage` متصل بودند، اصلاح شدند تا با سیستم هوشمند «مرا به خاطر بسپار» همگام شوند.

### تغییرات اعمال شده

<table dir="rtl" align="right" border="1" style="width: 100%; text-align: right;">
  <thead>
    <tr>
      <th>بخش سیستم</th>
      <th>فایل اصلاح شده</th>
      <th>شرح اقدامات انجام شده</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><b>سرویس API مدیریت کالا</b></td>
      <td><code>item-api.service.ts</code></td>
      <td>
        سرویس <code>AuthService</code> به داخل کلاس تزریق شد. کدی که توکن را به شکل استاتیک از لوکال استوریج برای آپلود فایل‌های اکسل برمی‌داشت حذف شده و با <code>auth.getAccessToken()</code> جایگزین گردید.
      </td>
    </tr>
    <tr>
      <td><b>بخش دانلود لاگ فایل</b></td>
      <td><code>docs.ts</code></td>
      <td>
        سرویس <code>AuthService</code> به کامپوننت اصلی تزریق شد و ساختار ساخت URL دانلود فایل لاگ آپلود از <code>localStorage</code> به استفاده از سرویس استاندارد تغییر پیدا کرد.
      </td>
    </tr>
  </tbody>
</table>

### نتیجه نهایی

- تغییرات در تایپ‌اسکریپت به درستی کامپایل شدند.
- هم‌اکنون سیستم آپلود فایل و دانلود لاگ بدون توجه به اینکه آیا تیک "مرا به خاطر بسپار" را زده‌اید یا خیر، توکن صحیح را پیدا کرده و دیگر خطای ۴۰۱ تولید نخواهد کرد.

</div>
