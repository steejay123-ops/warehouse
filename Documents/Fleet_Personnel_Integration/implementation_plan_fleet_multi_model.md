# طرح جامع ارتقای سیستم ثبت کارکرد ماشین‌آلات و ناوگان (Multi-Model Fleet Engine)

این طرح بر اساس توافقات انجام‌شده در مصاحبه تخصصی، سیستم ثبت کارکرد ماشین‌آلات را به یک پلتفرم جامع ۴ مدله با ایجنت‌های نگهبان سخت‌گیر ارتقا می‌دهد.

---

## ۱. مدل‌های قراردادی ۴ گانه (Contract Pricing Models)

| مدل قرارداد | کد مدل (`pricing_model`) | منطق کارکرد و نرخ پایه | فرمول محاسبه تسویه مالی | کاربرد تجاری |
| :--- | :---: | :--- | :--- | :--- |
| **ماشین‌آلات ساعتی** | `hourly` | پایه **۱۰ ساعت در روز** + ساعت اضافه‌کار با نرخ ساعتی توافقی | $(\text{ساعت عادی} \times \text{نرخ ساعت}) + (\text{ساعت اضافه} \times \text{نرخ اضافه})$ | لودر، بیل مکانیکی، لیفتراک پیمانکاری، جرثقیل، گریدر |
| **ناوگان سرویسی** | `trip` | تعداد سرویس حمل بار + شماره بارنامه / حواله | $\text{تعداد سرویس} \times \text{نرخ مصوب هر سرویس}$ | نیسان باربری، وانت، خاورهای حمل بار موردی |
| **خودروهای روزمزد** | `daily` | تعداد روزهای استقرار و فعالیت (شیفت کامل / نیمه‌شیفت) | $\text{تعداد روزهای کارکرد} \times \text{نرخ روزانه ثابت}$ | خودروهای گشت، سواری‌های در اختیار مدیریت، ون‌های پرسنلی |
| **ناوگان شرکتی** | `company` | ثبت تردد و ساعات بدون تسویه ریالی | **همواره ۰ تومان** (فاقد تسویه کرایه‌ای) | کامیون‌ها و لیفتراک‌های ملکی شرکت |

---

## ۲. نگهبان‌های سخت‌گیر داده‌ها (5 Strict Guard Agents & Invariants)

> [!IMPORTANT]
> **گارد ۱ (سقف ساعات روزانه):** مجموع ساعات عادی و اضافه‌کار هر ماشین در هر روز نمی‌تواند از ۲۴ ساعت تجاوز کند ($\text{ساعت موثر} + \text{اضافه‌کار} \le 24$).
> 
> **گارد ۲ (قفل دوره ماهانه):** در صورت قفل بودن دوره (`LOCKED` یا `FINALIZED`)، هیچ تغییری در کارکرد روزانه یا شیت ۳۱ روزه ثبت نمی‌شود مگر با پارامتر امنیتی `is_override=True` توسط سوپریوزر.
> 
> **گارد ۳ (ایمن‌سازی مبالغ شرکتی):** کلیه مبالغ تسویه، کرایه و خروجی دیسکت بانکی برای خودروهای شرکتی به صورت تضمین‌شده در سطح دیتابیس و کد `0` محاسبه می‌شوند.
> 
> **گارد ۴ (ایزولاسیون وب‌سوکت و تداخل تب‌ها):** تغییرات ارسالی از سایر تب‌ها یا سایر کلاینت‌ها با تطابق `client_tab_id` فیلتر شده و بدون بازنشانی کل جدول به صورت درجا (In-place Patch) اعمال می‌شوند.
> 
> **گارد ۵ (صحت‌سنجی شبا و دیسکت بانکی):** کلیه مبالغ تومان در دیسکت بانک ملی دقیقاً در ۱۰ ضرب شده (ریال) و شماره شبا با ساختار استاندارد ۲۶ کاراکتری `IR...` اعتبارسنجی می‌شود.

---

## ۳. تغییرات پیشنهادی در لایه‌ها (Proposed Changes)

### 🔹 لایه بک‌اند (Backend - Django & Database)

#### [MODIFY] [models.py](file:///e:/warehouse%20project/warehouse-backend/personnel/models.py)
- افزودن فیلد `pricing_model` با مقادیر `('hourly', 'trip', 'daily', 'company')` به `VehicleDriverProfile`.
- افزودن فیلدهای `hourly_rate` و `overtime_rate` به `VehicleDriverProfile`.
- افزودن فیلدهای `effective_hours`، `overtime_hours` و `is_present` به `VehicleTripLog` برای ثبت دقیق کارکرد ساعتی و روزمزد در کنار سرویسی.

#### [MODIFY] [views.py](file:///e:/warehouse%20project/warehouse-backend/personnel/views.py)
- ارتقای متد `get_matrix` و `bulk_save` در `VehicleTripViewSet` جهت پشتیبانی از ثبت چندحالته (ساعتی، سرویسی، روزمزد).
- ارتقای متد `get_monthly_grid` و `bulk_save_monthly_grid` برای تجمیع ساعات، سرویس‌ها و روزهای کارکرد در تقویم ۳۱ روزه.
- ارتقای موتور تسویه مالی ناوگان `fleet_settlement_engine.py` جهت محاسبه ترکیبی ۴ مدل.

---

### 🔹 لایه فرانت‌اند (Frontend - Angular & UI/UX)

#### [MODIFY] [personnel.model.ts](file:///e:/warehouse%20project/warehouse-front/src/app/core/models/personnel.model.ts)
- به‌روزرسانی اینترفیس‌های `VehicleDriverProfile`، `VehicleMatrixRow` و `VehicleMonthlyGridRow` با فیلدهای مدل‌های جدید.

#### [MODIFY] [warehouse-attendance.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/personnel/warehouse-attendance/warehouse-attendance.html)
- افزودن تب‌های فیلتر سریع در بالای جدول: «همه ناوگان»، «ماشین‌آلات ساعتی»، «ناوگان سرویسی»، «روزمزد و شرکتی».
- طراحی سلول‌های تطبیقی (Adaptive Row Cells):
  - ردیف ساعتی: فیلدهای ساعت موثر (پیش‌فرض ۱۰س) و اضافه‌کار.
  - ردیف سرویسی: فیلد تعداد سرویس و شماره بارنامه.
  - ردیف روزمزد: بج وضعیت حضور (حاضر / غایب / نیمه‌شیفت).
- ارتقای شیت تقویم ۳۱ روزه، مودال ویرایش روز و برگه چاپی رسمی A4.

#### [MODIFY] [warehouse-attendance.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/personnel/warehouse-attendance/warehouse-attendance.ts)
- مدیریت وضعیت تب‌های فیلتر سریع و ذخیره چندحالته مقادیر ردیف‌ها در پی‌لود سرور.

---

## ۴. برنامه اعتبارسنجی و تست‌ها (Verification Plan)

### Automated Tests
1. **تست‌های بک‌اند:** نوشتن تست‌های جدید در `personnel/test_fleet_multi_model_pricing.py` برای سناریوهای:
   - ثبت کارکرد ۱۰ ساعته + ۳ ساعت اضافه‌کار برای ماشین‌آلات سنگین.
   - ثبت تعداد سرویس برای خودروهای سرویسی.
   - ثبت حضور برای روزمزدها.
   - اعمال گارد سقف ۲۴ ساعت و گارد قفل دوره.
   - صدور دیسکت بانک ملی با محاسبه دقیق مبالغ.
2. **تست‌های سریع فرانت‌اند (Vitest):** اجرای تست‌های `personnel-fleet.spec.ts` و `warehouse-attendance.spec.ts`.
3. **بیلد پروداکشن:** اعتبارسنجی موفقیت‌آمیز `npm run build`.
