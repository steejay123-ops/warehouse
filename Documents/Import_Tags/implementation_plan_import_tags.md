<div dir="rtl" align="right">

# برنامه‌ی پیاده‌سازی: اصلاح و بهبود تگ‌گذاری کالا در آپلود اکسل

در این بخش، ۴ درخواست مرتبط با سیستم تگ‌گذاری (Tag-on-Import) مورد بررسی و حل قرار می‌گیرد. 

## موارد و تغییرات پیشنهادی (Proposed Changes)

| جزء سیستم | تغییرات برنامه‌ریزی شده | فایل‌های مرتبط |
| :--- | :--- | :--- |
| **بک‌اند (Backend)** | ۱. رفع باگ `split` در جداسازی کامای فارسی و انگلیسی با جایگزینی `replace(',', '،')`<br>۲. پیوست کردن (Append) تگ‌های جدید به تگ‌های موجود در صورت آپدیت کالا. | `inventory/views.py` |
| **فرانت‌اند (UI)** | اضافه کردن یک راهنمای متنی کوچک در زیر اینپوت تگ‌گذاری گروهی (مثال: نحوه وارد کردن چند تگ) | `docs.html` |
| **فرانت‌اند (Logic)** | نمایش مودال هشدار/تأییدیه پیش از شروع آپلود، در صورتی که فیلد تگ خالی باشد | `docs.ts` / `docs.html` |


### بخش بک‌اند

#### [MODIFY] [views.py](file:///e:/warehouse%20project/warehouse-backend/inventory/views.py)
> [!NOTE]
> - اصلاح منطق برش استرینگ: قبل از جدا کردن تگ‌ها، تمام `,` ها به `،` تبدیل خواهند شد.
> - در مرحله یافتن کالای از پیش موجود (`existing_item`)، اگر کالا تگی از قبل داشته باشد، تگ‌های قدیمی نیز همراه با تگ‌های جدید در لیست `final_tags` ریخته شده و سپس `set` (یونیک) می‌شوند.

### بخش فرانت‌اند

#### [MODIFY] [docs.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/docs/docs.html)
> [!TIP]
> - افزودن راهنمای متنی کوچک در زیر input تگ.
> - افزودن کد مودالِ «هشدار خالی بودن تگ» با دو دکمه «ادامه بدون تگ» و «بازگشت».

#### [MODIFY] [docs.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/docs/docs.ts)
> [!IMPORTANT]
> - در متد `simulateImportProcess` بررسی می‌شود که آیا `importTag` خالی است یا خیر.
> - در صورت خالی بودن، فِلگ مربوط به مودال هشدار `showEmptyTagWarning` برابر `true` می‌شود و فرایند متوقف می‌گردد تا کاربر تأیید کند.

## برنامه تست و تأیید (Verification Plan)

### تست‌های دستی
- کلیک روی دکمه شروع آپلود با اینپوت تگ خالی -> باید مودال هشدار باز شود.
- کلیک روی «ادامه بدون تگ» در مودال -> باید آپلود شروع شود.
- درج تگ به صورت `تگ۱,تگ۲،تگ۳` در اکسل -> باید در بک‌اند به درستی سه تگ مجزا پردازش شود.
- وارد کردن فایل اکسل برای کالایی که از قبل تگ دارد -> تگ‌های قبلی نباید پاک شوند، بلکه تگ جدید به انتهای آن‌ها اضافه گردد.

## نیاز به تایید شما (User Review Required)
> [!WARNING]
> لطفاً این طرح را مرور کنید و در صورت تأیید با نوشتن واژه‌ی **«تایید»** یا **«شروع»** اجازه دهید تا کدها را اصلاح کنم.

</div>
