<div dir="rtl" align="right">

# لیست وظایف پیاده‌سازی (Task List) - سیستم داده‌های اولیه

- `[x]` **بخش ۱: مایگریشن داده برای نقش‌ها**
  - `[x]` ایجاد فایل Data Migration خالی در اپلیکیشن `accounts`.
  - `[x]` نوشتن تابع `RunPython` برای استخراج دسترسی‌ها و ساخت خودکار گروه‌ها.

- `[x]` **بخش ۲: اسکریپت راه‌اندازی (setup_project)**
  - `[x]` ایجاد ساختار دایرکتوری `management/commands` در پروژه (ترجیحاً در اپلیکیشن `accounts` یا `core`).
  - `[x]` نوشتن اسکریپت `setup_project.py` برای اجرای دستور `migrate` از داخل کد.
  - `[x]` پیاده‌سازی کدهای ساخت ادمین اصلی (Superuser).
  - `[x]` پیاده‌سازی کدهای ساخت انبار پیش‌فرض.
  - `[x]` پیاده‌سازی کدهای ساخت کاربران تستی (طبق درخواست کاربر) و انتساب آن‌ها به گروه‌ها.

- `[x]` **بخش ۳: تایید و اعتبارسنجی**
  - `[x]` اجرای `python manage.py makemigrations`.
  - `[x]` اجرای `python manage.py setup_project` در محیط فعلی و بررسی خطاها.
  - `[x]` کنترل خروجی ترمینال و اطمینان از صحت انجام مراحل.

</div>
