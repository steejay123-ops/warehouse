<div dir="rtl" align="right">

# لیست وظایف: پیاده‌سازی انبارگردانی

- `[x]` **مرحله ۱: توسعه بک‌اند (Django)**
  - `[x]` ایجاد/بروزرسانی Serializer برای مدل `CountTask`.
  - `[x]` ایجاد API برای دریافت لیست کالاهای اختصاص‌یافته به شمارشگر فعلی.
  - `[x]` ایجاد API برای ذخیره محلی (Draft) و ارسال نهایی (Bulk Submit) توسط شمارشگر.
  - `[x]` ایجاد API برای کارتابل سرپرست (دریافت کالاهای `COUNTED` و `SUPERVISOR_REJECTED`).
  - `[x]` ایجاد API برای تایید/رد توسط سرپرست.

- `[x]` **مرحله ۲: توسعه فرانت‌اند (Angular) - سرویس‌ها**
  - `[x]` ایجاد اینترفیس `CountTask` در فرانت‌اند.
  - `[x]` اضافه‌کردن متدهای ارتباط با API به سرویس `counts.service.ts` (یا `inventory`).

- `[x]` **مرحله ۳: توسعه فرانت‌اند - رابط کاربری شمارشگر**
  - `[x]` ایجاد کامپوننت `CounterComponent` (نمایش لیست کارت‌ها).
  - `[x]` پیاده‌سازی حالت Master-Detail (ورود به جزئیات با کلیک روی کارت).
  - `[x]` اضافه‌کردن فیلد برای وارد کردن `counted_balance` به صورت Blind.
  - `[x]` اضافه‌کردن دکمه ارسال گروهی (Bulk Submit) برای شمارشگر.

- `[x]` **مرحله ۴: توسعه فرانت‌اند - رابط کاربری سرپرست**
  - `[x]` ایجاد کامپوننت `SupervisorComponent` (نمایش جدولی رکوردهای ارسالی شمارشگر).
  - `[x]` نمایش فیلدهای کالا، شمارشگر، و مقادیر شمرده شده.
  - `[x]` اضافه‌کردن عملیات `Approve` (تایید نهایی به مدیر).
  - `[x]` اضافه‌کردن عملیات `Reject` با امکان ثبت دلیل (ارجاع به شمارشگر برای بازشماری).
  - `[x]` اضافه‌کردن امکان Bulk Approve (تایید گروهی).

- `[ ]` **مرحله ۵: نهایی‌سازی**
  - `[ ]` افزودن مسیرها به `app.routes.ts`.
  - `[ ]` اضافه کردن لینک‌های ورود به منوی ناوبری.
  - `[ ]` کامپایل و تست.

</div>
