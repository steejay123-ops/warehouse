<div dir="rtl" align="right">

- `[ ]` ۱. به روزرسانی `parse_headers` در بک‌اند
  - حذف حساسیت به بزرگی و کوچکی حروف در هدرها
  - بررسی وجود فیلدهای حساس با استفاده از `get_setting('SENSITIVE_EXCEL_FIELDS')`
  - ارسال پیغام هشدار برای فرانت‌اند اگر کاربر عادی است
- `[ ]` ۲. بروزرسانی `import_excel` در بک‌اند
  - حذف حساسیت به حروف در هدرها
  - نادیده گرفتن قطعی ۴ فیلد سیستمی `created_by`, `modified_by`, `created_at`, `updated_at`
  - نادیده گرفتن رکورد در صورت مغایرت بین `id` و `fa_unic_code`
  - نادیده گرفتن فیلدهای حساس اگر کاربر عادی است
- `[ ]` ۳. به روزرسانی فرانت‌اند در کامپوننت `docs.ts` و `docs.html`
  - دریافت پرچم هشدار از `parse_headers`
  - پیاده‌سازی ساختار اولیه یک مودال هشدار برای نمایش به کاربر (طراحی اصلی توسط کاربر انجام خواهد شد)
- `[ ]` ۴. تست و بررسی کامپایل

</div>
