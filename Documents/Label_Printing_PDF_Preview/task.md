<div dir="rtl" align="right">

# برنامه‌ی انجام کار: جایگزینی بوم با PDF Preview

- `[x]` **فاز ۱: تغییرات بک‌اند (Backend)**
  - `[x]` افزودن فیلد ویژه `__custom_remark__` به لیست فیلدهای در دسترس (`available_fields`) در `label_views.py`.
  - `[x]` تغییر متد `generate_pdf` در `label_views.py` برای پذیرش آرایه‌ی `items_config` (شامل آیدی و تعداد) و `custom_remark`.
  - `[x]` تغییر `label_pdf_generator.py` جهت پشتیبانی از چاپ به تعداد خواسته شده (Quantity) و مقداردهی به فیلد توضیحات لحظه‌ای.
- `[x]` **فاز ۲: تغییرات فرانت‌اند (Frontend)**
  - `[x]` به‌روزرسانی سرویس `label-api.service.ts` برای ارسال داده‌های جدید به API.
  - `[x]` تغییر ظاهر بخش چاپ (`label-designer.html`) و ایجاد پنل تنظیمات چاپ (لیست آیتم‌ها با Input تعداد + فیلد توضیحات).
  - `[x]` افزودن قاب `iframe` برای نمایش زنده (Live Preview) فایل PDF در سمت راست مودال.
  - `[x]` متصل کردن دکمه "پیش‌نمایش" به متد جدید در `label-designer.ts` برای فراخوانی API و رندر PDF دریافتی (Blob) به کمک `URL.createObjectURL`.
- `[ ]` **فاز ۳: تست و صحت‌سنجی**
  - `[ ]` اطمینان از عملکرد صحیح چاپ تعداد چندگانه و نمایش توضیحات سفارشی.

</div>
