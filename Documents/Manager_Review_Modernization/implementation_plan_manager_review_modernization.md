# برنامه جامع پیاده‌سازی همگام‌سازی و نوسازی کارتابل بررسی نهایی مدیر (`Manager Review Modernization Implementation Plan`)

<div dir="rtl" align="right">

این مستند نقشه راه جامع و مهندسی شده برای نوسازی ساختار تب «بررسی نهایی مدیر» (`Manager Review`) بر پایه آخرین استانداردها، ساختار ماژولار و ۲۴ ویژگی پیشرفته پیاده‌شده در «کارتابل سرپرست» (`Supervisor Cartable`) را تشریح می‌کند.

---

## 🎯 اهداف کلیدی و رویکرد طراحی

1. **کپی شاسی و ساختار مدرن (`Cloning Modern UI Framework`):** انتقال تمام کامپوننت‌های مدرن، لایه‌بندی بصری، تقویم شمسی جلالی (`PersianDatePipe`)، پایداری وضعیت در `LocalStorage` و `URL State`، چیپ‌های لوکیشن، دراپ‌دان سورت هوشمند و بارکد اسکنر.
2. **بومی‌سازی و حفظ منطق تجاری مدیر (`Manager Domain Logic Adaptation`):** حفظ دقیق وضعیت‌های فرآیندی (`MANAGER_REVIEW`, `FINAL_APPROVED`, `MANAGER_REJECTED`)، اندپوینت‌های API مدیر (`bulk_manager_approve`, `bulk_manager_reject`)، اکشن‌های استخر مدیر (`as_role='manager'`) و مجوزهای امنیتی مربوطه.
3. **ارزیابی ایجنت نگهبان مستقل در هر فاز (`Independent Guardian Agent Reviews`):** بررسی سلامت ساختار در پایان هر فاز قبل از ورود به فاز بعدی.

---

## 📋 تقسیم‌بندی فازهای اجرایی (Project Phases)

| فاز | حوزه تخصصی | شرح اقدامات کلیدی |
| :--- | :--- | :--- |
| **فاز ۱** | شاسی‌سازی و زیرساخت فرانت | انتقال معماری تایپ‌اسکریپت، ایمپورت لوله‌های تاریخ شمسی (`PersianDatePipe`)، ماژول‌های تاچ، بارکد اسکنر، و ساختار پایداری وضعیت. |
| **فاز ۲** | تطبیق منطق دامین مدیر | بازنویسی متدهای بارگذاری تسک‌ها با نقش مدیر، اتصال اندپوینت‌های تایید نهایی و رد مدیر، هندلینگ وب‌سوکت و کشینگ زنده SWR. |
| **فاز ۳** | تمپلیت HTML و تعاملات بصری | پیاده‌سازی کارت‌های مدرن، نوار ابزار شناور شیشه‌ای، مودال‌های غنی جزئیات شمارش و اسناد، بج‌های وضعیت نهایی و فرمت‌های ریالی. |
| **فاز ۴** | تست‌های بک‌اند و کامپایل | اجرای تست‌های واحد جنگو، کامپایل و اعتبارسنجی تایپ‌اسکریپت و استرس‌تست ایجنت نگهبان. |
| **فاز ۵** | تست تعاملی مرورگر و گزارش | تست خودکار در مرورگر واقعی (`Browser Subagent`)، ارزیابی تمامی تب‌ها، فیلترها و مودال‌ها و ثبت گزارش DUAL-SAVE. |

---

## 🔍 تغییرات تفکیکی فایل‌ها (Proposed File Changes)

### فرانت‌اند (`warehouse-front`)

#### [MODIFY] [`manager-review.ts`](file:///e:/warehouse%20project/warehouse-front/src/app/components/manager-review/manager-review.ts)
- به‌روزرسانی ایمپورت‌ها (`PersianDatePipe`, `BarcodeScannerComponent`, `OfflinePendingBadgeComponent`, `WarehouseSelectorComponent`).
- استقرار متغیرهای وضعیت: فیلتر لوکیشن، دراپ‌دان سورت هوشمند، پایداری دوگانه `LocalStorage` و `URL State` با پارامترهای تفکیکی زیرتب‌ها.
- تطبیق کامل متدهای بارگذاری `loadTasks`، `loadPoolTasks`، `loadDocTasks` و `loadDocPoolTasks` برای وضعیت‌های `MANAGER_REVIEW` و `DOC_MANAGER_REVIEW`.
- بازنویسی متدهای تایید نهایی `confirmApprove` و `confirmDocApprove` به همراه ثبت یادداشت اختیاری مدیر و بروزرسانی بلادرنگ لیست.
- بازنویسی متدهای رد مدیر `confirmBulkReject` و `confirmDocReject` جهت صدور دستور بازشماری یا ارجاع به سرپرست.
- ارتقای متد `applyFilters` با موتور جستجوی یکپارچه و نرمال‌ساز الفبای فارسی/عربی.

#### [MODIFY] [`manager-review.html`](file:///e:/warehouse%20project/warehouse-front/src/app/components/manager-review/manager-review.html)
- پیاده‌سازی هدر استاندارد با سلکتور انبار، دکمه بارکد اسکنر، خروجی اکسل پیشرفته و دکمه رفرش آنی.
- تب‌های تفکیک‌شده «انبارگردانی (میدانی)» و «مالی / اسناد» با شمارنده‌های زنده.
- نوار فیلتر چسبان (`Sticky Filters`) شامل چیپ‌های لوکیشن هوشمند، چیپ‌های بازه زمانی و دکمه مرتب‌سازی هوشمند.
- رندر کارت‌های اقلام با طراحی کارت‌های نوسازی‌شده، مقایسه اعشاری مغایرت، بج‌های وضعیت نهایی (`تایید نهایی`, `در انتظار تایید مدیر`, `رد مدیر`).
- نوار ابزار شناور شیشه‌ای (`Floating Action Bar`) با افکت‌های انیمیشنی، دکمه‌های تایید نهایی دسته‌جمعی و رد سریع.
- مودال‌های غنی جزئیات شمارش میدانی و جزئیات اسناد مالی با پشتیبانی از فیلدهای سفارشی پویا و تاریخ شمسی.

#### [MODIFY] [`manager-review.css`](file:///e:/warehouse%20project/warehouse-front/src/app/components/manager-review/manager-review.css)
- تضمین وجود انیمیشن‌های نرم باز و بسته شدن مودال‌ها (`slideInModal`, `slideUpFloating`) و استایل اسکرول‌بار مخفی.

---

## 🛡️ پروتکل بازبینی ایجنت نگهبان مستقل (Guardian Protocol)

در پایان هر یک از فازها، ایجنت نگهبان به طور مستقل موارد زیر را ارزیابی خواهد کرد:
1. عدم تخریب یا حذف ناخواسته کدهای مرتبط با سایر بخش‌ها.
2. تطابق ۱۰۰٪ وضعیت‌ها، مجوزها و اندپوینت‌های مدیر.
3. عدم وجود خطاهای کامپایل تایپ‌اسکریپت و کدهای شکسته در تمپلیت.
4. سلامت تست‌های بک‌اند.

---

## 🧪 برنامه راستی‌آزمایی و اعتبارسنجی (Verification Plan)

### ۱. تست‌های خودکار بک‌اند (`Django Backend Tests`)
```powershell
python manage.py test inventory.tests inventory.tests_docs
```

### ۲. اعتبارسنجی کامپایل فرانت‌اند (`Frontend Type & Template Check`)
```powershell
npm run build -- --no-watch
```

### ۳. تست تعاملی مرورگر (`Browser Subagent Verification`)
- بارگذاری مسیر `http://localhost:4200/manager-review`
- تست سوئیچ بین بخش انبارگردانی و مالی/اسناد
- بررسی اعمال فیلترهای زمانی، چیپ‌های لوکیشن و منوی مرتب‌سازی هوشمند
- تست باز شدن مودال جزئیات شمارش و جزئیات اسناد مالی
- بررسی دکمه انتخاب دسته‌جمعی و نوار ابزار شناور عملیات نهایی

</div>
