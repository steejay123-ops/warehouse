# فهرست تسک‌های اجرایی و رفع ایرادات ۱۶‌گانه سیستم ماژولار

<div dir="rtl" align="right">

> [!IMPORTANT]
> این سند برنامه جامع اجرایی و صحت‌سنجی ۱۶ ایراد فنی و منطقی سیستم ماژولار است که بر اساس متدولوژی مرحله‌به‌مرحله و تست‌های واقعی اجرا خواهد شد.

### فاز ۱: اصلاحات بحرانی و امنیتی بک‌اند (Backend Critical & Security)
- [x] <!-- id: task-01 --> **تسک ۱ (ایراد ۱):** اصلاح کرش رانتایم متد `to_journal_lines` در `personnel/models.py` (اصلاح نام فیلدها و اضافه کردن سطر کسر مساعده جهت تراز دوبل)
- [x] <!-- id: task-02 --> **تسک ۲ (ایراد ۲):** رفع خطای ۵۰۰ در ایمپورت اکسل کاربران در پروفایل حسابداری‌تنها در `accounts/views.py` (گارد ایمن روی `assigned_warehouses`)
- [x] <!-- id: task-03 --> **تسک ۳ (ایراد ۳):** رفع خطای ۵۰۰ اندپوینت لاگ‌های ممیزی در حالت حسابداری‌تنها در `accounts/views.py` (بررسی `hasattr` برای کاربران غیرسوپریوزر)
- [x] <!-- id: task-04 --> **تسک ۴ (ایراد ۷):** جلوگیری از نشت کلیم و جعل نقش در توکن JWT در `accounts/middleware.py` (عدم فالبک و برگرداندن آرایه خالی برای ماژول غایب)
- [x] <!-- id: task-05 --> **تسک ۵ (ایراد ۸):** اصلاح فیلتر لاگ‌های ممیزی مالی برای کاربران غیرسوپریوزر در `accounts/views.py` (پذیرش رکوردهای با `warehouse_id__isnull=True`)

### فاز ۲: خزانه‌داری، رخدادهای مالی، پکیجینگ و جامعیت داده‌ها (Data Integrity, Outbox & Packaging)
- [x] <!-- id: task-06 --> **تسک ۶ (ایراد ۶):** اتصال `emit_accounting_event` به رویدادهای قطعی‌سازی حقوق، صورت‌وضعیت‌ها و پرداخت خزانه‌داری در `personnel/views.py`
- [x] <!-- id: task-07 --> **تسک ۷ (ایراد ۱۲):** اصلاح کانفیگ پکیج‌های `packages/wh-*` و فراخوانی کشف خودکار ماژول‌ها (`discover_entry_point_modules`) در `PlatformCoreConfig.ready()`
- [x] <!-- id: task-08 --> **تسک ۸ (ایراد ۱۳):** تفکیک اپ `reports` به لایه مشترک پلتفرم و ثبت موجودیت‌های مالی و پرسنلی در موتور کوئری
- [x] <!-- id: task-09 --> **تسک ۹ (ایراد ۱۴):** پیشگیری از یتیم ماندن شناسه‌های انبار در پرسنلی و چت در سیگنال `post_delete` انبار (`warehouses/signals.py`)

### فاز ۳: معماری فرانت‌اند، ایزولاسیون باندل و تفکیک نقش‌ها (Frontend Architecture, Bundles & SoD)
- [x] <!-- id: task-10 --> **تسک ۱۰ (ایراد ۴):** اصلاح هدایت پروکسی `offlineDb` برای جدول `docTasks` از دیتابیس مالی به دیتابیس انبارداری (`core/services/offline-db.ts`)
- [x] <!-- id: task-11 --> **تسک ۱۱ (ایراد ۱۱):** تفکیک ثوابت `IS_WAREHOUSE_INSTALLED` در فایلی سبک (`module-flags.ts`) جهت حفظ ایزولاسیون باندل و کارکرد واقعی Lazy Loading
- [ ] <!-- id: task-12 --> **تسک ۱۲ (ایراد ۹):** پیاده‌سازی گارد ماژولار (`ModuleInstalledGuard` / `canMatch`) در `app.routes.ts` جهت جلوگیری از کرش روتهای غایب
- [ ] <!-- id: task-13 --> **تسک ۱۳ (ایراد ۱۰):** اصلاح تفکیک وظایف (SoD) در `AppPersonaService` و حذف دسترسی سرپرست انبار از سرپرست پرسنلی (`app-persona.service.ts:251`)
- [ ] <!-- id: task-14 --> **تسک ۱۴ (ایراد ۱۵):** مخفی‌سازی منوهای انبار در نصب حسابداری و هدایت هوشمند در `exitWarehouseMode()` (`layout.ts`)

### فاز ۴: همگام‌سازی آفلاین و مدیریت یکپارچه پرسنل (Sync & User Management)
- [ ] <!-- id: task-15 --> **تسک ۱۵ (ایراد ۵):** تفکیک متد سینک دلتای سرور بر اساس قلمرو (`scopeKind`) و رفع نشت کرسر در `sync-pull.service.ts`
- [ ] <!-- id: task-16 --> **تسک ۱۶ (ایراد ۱۶):** اصلاح قالب اکسل نمونه کاربران به ۱۴ ستون، پشتیبانی از `assigned_warehouses` در متد `update` کاربر، و اتصال پروژه‌های مالی به فرم کاربران در غیاب انبار

</div>
