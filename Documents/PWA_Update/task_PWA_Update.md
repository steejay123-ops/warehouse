<div dir="rtl" align="right">

# لیست وظایف: پیاده‌سازی مکانیزم اطلاع‌رسانی بروزرسانی PWA

- [x] افزودن `SwUpdate` و `ConfirmDialogService` به سازنده `app.ts`.
- [x] پیاده‌سازی منطق گوش دادن به رویداد `VERSION_READY` و نمایش دیالوگ.
- [x] پیاده‌سازی منطق `activateUpdate` و رفرش صفحه پس از تایید کاربر.
- [x] تنظیم تایمر برای بررسی نسخه‌های جدید هر یک ساعت یک بار (`setInterval`).
- [x] اجرای `npm run build` جهت اطمینان از صحت تغییرات.

</div>
