<div dir="rtl" align="right">

# طرح جامع بهینه‌سازی و ارتقای سرعت سیستم (Performance Optimization)

این سند جزئیات فنی و گام‌های پیاده‌سازی جهت رفع کامل کندی سیستم، بهینه‌سازی فرآیند «بروزرسانی عمیق» (Deep Sync)، رفع اختلالات اتصال کلادفلر تونل (Cloudflare Tunnel) و کاهش ترافیک شبکه را تبیین می‌نماید.

---

## ۱. بررسی نیازمندی و تصمیمات کلیدی (User Review Required)

> [!IMPORTANT]
> **اصلاح اساسی کوئری‌های دیتابیس (N+1 Queries Fix):**
> در حال حاضر به ازای هر رکورد در تسک‌های شمارش، چندین مرتبه به جدول تنظیمات دیتابیس کوئری زده می‌شود که با پیاده‌سازی کش لایه سرویس (`services.py`) و پاس دادن کانتکست به سریالایزرها، بیش از ۵,۰۰۰ کوئری اضافی به صفر کاهش خواهد یافت.

> [!TIP]
> **بهینه‌سازی انتقال داده در سرور اکسپرس (`server.js`):**
> فعال‌سازی فشرده‌سازی خودکار (`compression` با الگوریتم Gzip) برای تمام فایل‌های باندل انگولار و پاسخ‌های پروکسی در `server.js` باعث کاهش ۷۰ الی ۸۰ درصدی حجم انتقال اولیه در بستر تونل اینترنت خواهد شد.

---

## ۲. تغییرات پیشنهادی بر تفکیک کامپوننت‌ها (Proposed Changes)

### بخش اول: بک‌اند (Warehouse Backend)

#### [MODIFY] [services.py](file:///e:/warehouse%20project/warehouse-backend/warehouses/services.py)
- پیاده‌سازی مکانیزم کش درون‌حافظه‌ای/جنگو (`django.core.cache`) برای توابع `get_setting` و `get_all_settings`
- ابطال کش هنگام تغییر یا ذخیره تنظیمات سیستم در مدل `SystemSetting`

#### [MODIFY] [serializers.py](file:///e:/warehouse%20project/warehouse-backend/inventory/serializers.py)
- بازنویسی متدهای `get_is_blind` و `get_item_details` در `CountTaskSerializer` جهت خواندن وضعیت از `self.context` بدون کوئری دیتابیس
- پاس دادن کانتکست والد به `ItemSerializer` برای جلوگیری از کوئری‌های تکراری

#### [MODIFY] [sync_views.py](file:///e:/warehouse%20project/warehouse-backend/inventory/sync_views.py)
- تکمیل پیوندهای پیش‌واکشی (`select_related` برای فیلدهای `item__warehouse`، `item__created_by`، `item__modified_by`) در متد `_build_queryset`
- سریالایز دسته‌جمعی به جای حلقه تک‌تک (`many=True`) به همراه ارسال کانتکست `is_blind`

---

### بخش دوم: فرانت‌اند و سرور توزیع (Warehouse Front)

#### [MODIFY] [server.js](file:///e:/warehouse%20project/warehouse-front/server.js)
- نصب پکیج `compression` و اضافه کردن میدل‌ور فشرده‌سازی در ابتدای زنجیره اکسپرس
- تنظیم هدرهای بهینه کش برای فایل‌های استاتیک انگولار

#### [MODIFY] [cmd.txt](file:///e:/warehouse%20project/cmd.txt)
- تثبیت دستور کلادفلر تونل با سویچ صریح `--protocol http2` جهت جلوگیری از افت پکت و قطعی مکرر

---

## ۳. جدول مقایسه‌ای وضعیت قبل و بعد (Architecture & Metrics Comparison)

| شاخص | وضعیت فعلی (قبل از بهینه‌سازی) | وضعیت هدف (پس از بهینه‌سازی) |
| :--- | :--- | :--- |
| **تعداد کوئری SQL در هر صفحه همگام‌سازی** | بیش از ۵,۰۰۰ کوئری جداگانه | کمتر از ۵ کوئری بهینه‌شده |
| **حجم انتقال فایل جاوااسکریپت در تونل** | ~ ۲.۵ مگابایت (خام و فشرده‌نشده) | ~ ۶۰۰ کیلوبایت (با فشرده‌سازی Gzip) |
| **پروتکل ارتباطی تونل کلادفلر** | QUIC / UDP (دچار افت پکت در ایران) | HTTP/2 / TCP (پایدار و با سرعت ثابت) |
| **زمان اجرای بروزرسانی عمیق** | طولانی و با افت فریم | چند ثانیه (آنی و سبک) |

---

## ۴. برنامه اعتبارسنجی و تست (Verification Plan)

### تست‌های خودکار و دستی:
1. اجرای بیلد فرانت‌اند و تست صحت کارکرد `server.js` با `compression`.
2. اجرای درخواست `GET /api/inventory/sync/pull/?warehouse_id=...` و بررسی تعداد کوئری‌ها و سرعت پاسخ‌دهی در ترمینال بک‌اند.
3. آزمایش فرآیند بروزرسانی عمیق در رابط کاربری و بررسی زمان تکمیل و عدم وجود خطای همگام‌سازی.

</div>
