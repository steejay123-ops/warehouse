<div dir="rtl" align="right">

# وظایف — لاگین دائمی (Persistent Login)

- `[x]` ۱. بک‌اند: تغییر `SIMPLE_JWT` در `settings.py`
- `[x]` ۲. فرانت‌اند: حذف چک‌باکس «مرا به خاطر بسپار» از `login.html`
- `[x]` ۳. فرانت‌اند: حذف متغیر `rememberMe` از `login.ts`
- `[x]` ۴. فرانت‌اند: ساده‌سازی `auth.service.ts` — فقط `localStorage`، ذخیره Refresh Token جدید
- `[x]` ۵. فرانت‌اند: پیاده‌سازی صف/قفل Refresh در `auth.interceptor.ts`
- `[x]` ۶. فرانت‌اند: به‌روزرسانی `tryRefreshToken` در `offline-sync.service.ts`
- `[x]` ۷. بررسی کامپایل و عدم خطا

> [!NOTE]
> تمام وظایف با موفقیت انجام شد. خطای بیلد مربوط به `manager-review.html` بوده و ارتباطی با تغییرات این تسک ندارد.

</div>
