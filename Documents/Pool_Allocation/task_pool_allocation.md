<div dir="rtl" align="right">

# برنامه‌ی تسک‌ها: پیاده‌سازی استخر (Pool) تخصیص کالا

- `[x]` **مرحله ۱: به‌روزرسانی مدل دیتابیس**
  - `[x]` امکان‌پذیر کردن فیلد `counter` به صورت Nullable در `CountTask` در فایل `inventory/models.py`.
  - `[x]` ساخت مایگریشن (Makemigrations).
  - `[x]` اعمال مایگریشن روی دیتابیس (Migrate).

- `[x]` **مرحله ۲: اصلاح بخش تخصیص کالا (مدیریت)**
  - `[x]` تغییر کدهای فرانت‌اند (`dispatch.ts`): حذف اجباری بودن انتخاب فیلدهای تخصیص‌گیرنده و اصلاح متن دیالوگ ارجاع (نمایش «استخر عمومی» در صورت عدم انتخاب فرد).
  - `[x]` تغییر کدهای بک‌اند (`inventory/views.py` متد `bulk_assign`): ایجاد `CountTask` حتی در صورت ارسال `field_assignee_id = None`.

- `[x]` **مرحله ۳: توسعه API کارتابل و Claim**
  - `[x]` ایجاد اندپوینت `pool_tasks` در `CountTaskViewSet` برای برگرداندن تسک‌های استخر. (دریافت تسک‌های بدون انبارگردان برای انبارگردان‌ها و تسک‌های بدون سرپرست برای سرپرستان).
  - `[x]` ایجاد اکشن `claim_tasks` در `CountTaskViewSet` برای اختصاص دادن کاربر جاری به لیست تسک‌های انتخاب‌شده.

- `[x]` **مرحله ۴: توسعه داشبورد انبارگردان و سرپرست**
  - `[x]` افزودن تب «استخر کالاها» به رابط کاربری انبارگردان (`counter-dashboard.html / ts`).
  - `[x]` فراخوانی لیست استخر و قرار دادن چک‌باکس و دکمه «به عهده گرفتن» (Claim).
  - `[x]` افزودن تب «استخر کالاها» به رابط کاربری سرپرست (`supervisor-dashboard.html / ts`).
  - `[x]` پیاده‌سازی سوییچ خودکار به تب «تسک‌های من» پس از Claim موفقیت‌آمیز.

- `[x]` **مرحله ۵: کامپایل، تست و اعتبارسنجی**
  - `[x]` اطمینان از صحت بیلد فرانت‌اند.
  - `[x]` رفع خطاهای احتمالی ترمینال.

</div>
