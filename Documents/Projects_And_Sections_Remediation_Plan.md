# طرح جامع اصلاح، بهینه‌سازی و ارتقای صفحه پروژه‌ها، بخش‌ها و انتساب‌های سازمانی
# (`/app/finance/projects-and-sections`)

> **نسخه:** ۲.۰ (بازبینی‌شده، جامع و نهایی با ممیزی ۴۱ نقص فنی و معماری)  
> **وضعیت سند:** تدوین‌شده / آماده تایید و اجرا  
> **مسیر فایل:** `E:\warehouse project\Documents\Projects_And_Sections_Remediation_Plan.md`  
> **محدوده اثر:** بک‌اند (`warehouse-backend/personnel/`) و فرانت‌اند (`warehouse-front/src/app/components/organization/projects-and-sections/`)  
> **اصل بنیادین:** حفظ ۱۰۰٪ داده‌ها (هیچ داده‌ای نباید از بین برود)، صفر بازنویسی مخرب، امنیت سخت‌گیرانه RBAC، پایداری پشت تونل Cloudflare، و بهینه‌سازی رندرینگ DOM  

---

## ۱. خلاصه‌وضعیت و فلسفه طرح

صفحه مدیریت ساختار سازمانی (`/app/finance/projects-and-sections`) به عنوان ستون فقرات سامانه در حوزه تفکیک مراکز هزینه، گردش‌کار تاییدات اسناد مالی، کارکرد پرسنل و ناوگان عملیاتی عمل می‌کند. در بازبینی موشکافانه و فوق‌العاده عمیق انجام‌شده، مشخص گردید که فراتر از ایرادات اولیه، در مجموع **۴۱ نقص و خلأ جدی** در ۴ لایه (بک‌اند، فرانت‌اند، موتور اکسل و یکپارچگی داده دیتابیس) وجود دارد.

برخی از بارزترین خطرات کشف‌شده عبارتند از:
۱. **خطر نابودی اسناد مالی و کارکردها:** کلیدهای خارجی `CASCADE` در فاکتورها، تنظیمات مالیاتی سالانه و `SET_NULL` شدن پروژه‌ها در کارکردها و ترددها.
۲. **باگ پیش‌نمایش اکسل:** اجرای دوگانه و ذخیره زودهنگام فایل در مرحله صرفاً پیش‌نمایش به دلیل عدم پشتیبانی بک‌اند از `dry_run`.
۳. **قفل شدن رندرینگ انگولار:** فراخوانی توابع فیلتر در تمپلیت که تا ۲۵,۰۰۰ بار در هر چرخه Change Detection اجرا شده و موجب افت شدید فریم (Lag) می‌شود.
۴. **باگ نمایش اشتباه هشدار:** فعال شدن هشدار چشمک‌زن قرمز «⚠️ فاقد مدیر» روی تمامی بخش‌ها در صورت فیلتر کردن سمت‌ها (مانند جستجوی حسابداران).
۵. **ناهماهنگی کش SWR زنده:** نادیده گرفته شدن به‌روزرسانی زنده وب‌سوکت برای بخش‌ها به دلیل وجود دو آرایه موازی ناهمگام (`projectSections` و `allProjectSections`).
۶. **خروجی اکسل اشتباه:** در تب انتساب‌ها، خروجی اکسل به جای فیلتر جاری پروژه انتساب‌ها، فیلتر تب بخش‌ها را ارسال می‌کند.

این سند تمامی این ۴۱ مورد را در یک ماتریس شفاف آورده و در قالب **۶ فاز تفکیک‌شده همراه با لیست تسک‌های شناسه‌دار**، نقشه راه اجرای گام‌به‌گام را بدون ایجاد رگرسیون تبیین می‌نماید.

---

## ۲. جدول ماتریس جامع ۴۱ ایراد کشف‌شده (بک‌اند، فرانت‌اند، موتور اکسل و لاجیک)

| ردیف | لایه سیستم | سطح شدت | عنوان نقص فنی / لاجیک | شرح دقیق مشکل و پیامد عملیاتی | راه‌حل مهندسی در طرح |
| :---: | :---: | :---: | :--- | :--- | :--- |
| **۱** | بک‌اند / امنیت | **بسیار بحرانی** | نبود کنترل سطح دسترسی در تمامی ViewSetهای سازمانی | کلاس‌های `FinancialProjectViewSet`، `ProjectSectionViewSet`، `UserSectionAssignmentViewSet` و `CounterpartyViewSet` صرفاً از `IsAuthenticated` استفاده می‌کنند؛ هر کاربری لاگین کند می‌تواند پروژه، بخش، یا انتساب مدیران و خزانه‌داران را دستکاری یا حذف کند. | ایجاد و اعمال کلاس Permission اختصاصی مبتنی بر نقش (`IsSystemAdminOrFinanceManager`) برای متدهای موتاسیون. |
| **۲** | بک‌اند / داده | **بسیار بحرانی** | حذف زنجیره‌ای فاکتورها با `CASCADE` در دیتابیس | در مدل `ExpenseInvoice` روابط `section` و `counterparty` با `on_delete=models.CASCADE` تعریف شده‌اند؛ حذف تصادفی یا عمدی یک بخش یا شخص، تمام فاکتورهای ثبت‌شده آن را برای همیشه پاک می‌کند. | تغییر رفتار به `on_delete=models.PROTECT` در مدل و ممانعت از حذف در `perform_destroy` در صورت وجود اسناد مالی. |
| **۳** | بک‌اند / داده | **بسیار بحرانی** | حذف زنجیره‌ای تنظیمات حقوق و مالیات با حذف پروژه | در مدل `PayrollYearlySettings` رابطه `project` دارای `on_delete=models.CASCADE` است؛ حذف یک پروژه تمامی جداول مالیاتی، ضرایب بیمه و رده‌های شغلی پروژه را پاک می‌کند. | تغییر رابطه به `on_delete=models.PROTECT` و جلوگیری از حذف پروژه‌های دارای تنظیمات مالی. |
| **۴** | بک‌اند و فرانت / اکسل | **بسیار بحرانی** | اجرای دوگانه و ذخیره زودهنگام در پیش‌نمایش اکسل | کامپوننت `ExcelImportModal` فایل را با `dryRun=true` برای پیش‌نمایش می‌فرستد؛ اما بک‌اند پارامتر `dry_run` ندارد و همان لحظه پیش‌نمایش داده‌ها را ذخیره می‌کند! با زدن دکمه تایید نهایی، ایمپورت مجدداً اجرا می‌شود. | افزودن پشتیبانی از پارامتر `dry_run` در تمامی متدهای اکسل انجین جهت اعتبارسنجی بدون ذخیره در دیتابیس. |
| **۵** | فرانت‌اند / پرفورمنس | **بحرانی** | حلقه‌های محاسباتی سنگین در قالب HTML ماتریس | توابع `getAvailableSourceSectionsForClone()` و `getSourceSectionAssignmentCount()` در دراپ‌داون قالب HTML صدا زده می‌شوند و در هر چرخه Change Detection تا ۲۵,۰۰۰ بار آرایه‌ها را فیلتر می‌کنند که باعث لگ شدید UI می‌شود. | کش کردن نتایج در متغیرهای کامپوننت و محاسبه آن صرفاً در زمان باز شدن مودال یا تغییر فیلتر. |
| **۶** | فرانت‌اند / پرفورمنس | **بحرانی** | فراخوانی مکرر `getSelectedSourceSectionDetails()` در تمپلیت | این متد در هر رندر تمپلیت بین ۸ تا ۱۵ بار صدا زده می‌شود و هر بار لیست انتساب‌ها را فیلتر و نقش‌ها را تفکیک می‌کند. | نگهداری آمار در شیء کش‌شده `cloneSourceStats` و فراخوانی صرفاً با ایونت تغییر بخش مبدأ. |
| **۷** | بک‌اند / پرفورمنس | **بحرانی** | خطای N+1 Query در شمارش بخش‌های پروژه‌ها | در `FinancialProjectSerializer` فیلد `sections_count = serializers.IntegerField(source='sections.count')` برای هر پروژه یک کوئری مستقل `COUNT` می‌زند. در `export_projects_excel` نیز همین حلقه تکرار شده است. | استفاده از `.annotate(sections_count=Count('sections'))` در کوئری‌ست پایه ویوست و اکسل انجین. |
| **۸** | فرانت‌اند / باگ داده | **بحرانی** | پاک شدن کامل داده‌های صفحه در خطای موقت شبکه | در متدهای `loadAssignments`، `loadCounterparties` و `loadUsers` در بلاک `error`، آرایه‌ها با `this.userAssignments = []` خالی می‌شوند؛ با یک قطعی لحظه‌ای، تمامی اطلاعات روی صفحه محو می‌گردد. | حذف ریست آرایه‌ها در زمان خطا، حفظ کش موجود در صفحه و نمایش توست هشدار شبکه. |
| **۹** | فرانت‌اند / باگ لاجیک | **بحرانی** | آلارم کاذب «⚠️ فاقد مدیر» در صورت فیلتر کردن سمت‌ها | متد `hasManagerAssigned` از آرایه فیلترشده `sGroup.roles` استفاده می‌کند؛ اگر کاربر مثلاً «حسابداران» را فیلتر کند، نقش مدیر از آرایه حذف شده و تمامی بخش‌ها به اشتباه هشدار فاقد مدیر می‌زنند! | ارزیابی وجود مدیر از روی تمامی انتساب‌های خام بخش (`userAssignments`) نه از روی آرایه فیلترشده نقش‌ها. |
| **۱۰** | فرانت‌اند / همگام‌سازی | **بحرانی** | بی‌اثر بودن به‌روزرسانی زنده وب‌سوکت برای بخش‌ها | هنگام دریافت رویداد زنده، `this.projectSections = data` ست می‌شود اما `this.allProjectSections` تغییر نمی‌کند. از آنجا که گترهای قالب ابتدا `allProjectSections` را می‌خوانند، تغییرات زنده هرگز نمایش داده نمی‌شوند. | حذف ساختار دو آرایه‌ای موازی و نگهداری یک آرایه مرجع واحد (`projectSections`) برای جلوگیری از عدم تطابق. |
| **۱۱** | فرانت‌اند / فرم | **مهم** | عدم انطباق نوع داده (`[value]` به جای `[ngValue]`) در دراپ‌داون‌ها | در تمپلیت برای پروژه‌ها و بخش‌ها از `[value]="p.id"` استفاده شده که مقدار عددی را به رشته تبدیل می‌کند (`"5"` به جای `5`) و فرم در حالت ویرایش خالی نمایش داده می‌شود. | اصلاح تمامی دراپ‌داون‌ها به `[ngValue]="p.id"`. |
| **۱۲** | فرانت‌اند / خروجی | **مهم** | ارسال فیلتر اشتباه در خروجی اکسل انتساب‌های پرسنل | در متد `exportExcel` در تب انتساب‌ها، `this.selectedProjectId` (فیلتر تب بخش‌ها) به جای `this.assignmentProjectFilter` ارسال می‌شود و خروجی اکسل پروژه‌ای مغایر با صفحه دانلود می‌کند. | ارسال متغیر صحیح `this.assignmentProjectFilter` به سرویس دانلود اکسل. |
| **۱۳** | بک‌اند / منطق | **مهم** | خطای `UniqueTogether` در فعال‌سازی مجدد کاربر غیرفعال | یکتایی `unique_together = ('user', 'section', 'role')` تعریف شده است؛ اگر کاربری قبلاً غیرفعال شده باشد (`is_active=False`)، انتساب مجدد وی از طریق فرم استاندارد با خطای ۴۰۰ مسدود می‌شود. | بازنویسی لاجیک ذخیره جهت فعال‌سازی مجدد رکورد در صورت وجود (`update_or_create`). |
| **۱۴** | بک‌اند / منطق | **مهم** | عدم فعال‌سازی رکوردهای غیرفعال در کپی چارت سازمانی | در `clone_assignments` بررسی با `exists()` انجام می‌شود؛ اگر کاربری در بخش مقصد رکورد غیرفعال داشته باشد، به عنوان تکراری رد شده و فعال نمی‌شود. | به‌روزرسانی وضعیت رکورد موجود به `is_active=True` در صورت وجود رکورد غیرفعال در مقصد. |
| **۱۵** | بک‌اند / پایداری | **مهم** | کرش ۵۰۰ در `bulk-assign` با ورود `user_id` نامعتبر و نقش غیرمجاز | وجود `user_id` در دیتابیس بررسی نمی‌شود و ورود آیدی نامعتبر باعث خطای ۵۰۰ (`IntegrityError`) می‌شود؛ همچنین رشته نقش با `ROLE_CHOICES` اعتبارسنجی نمی‌شود. | اعتبارسنجی وجود کاربران، اعتبارسنجی نقش و بازگرداندن خطای استاندارد ۴۰۰ در صورت مغایرت. |
| **۱۶** | بک‌اند / پایداری | **مهم** | خطای ۵۰۰ در `clone-assignments` با شناسه مبدأ نامعتبر | در اکشن کپی چارت، `int(source_section_id)` بدون `try/except` قرار دارد و ارسال ورودی نامعتبر باعث کرش ۵۰۰ سرور می‌شود. | کنترل خطای تبدیل رشته به عدد با مدیریت استثناء و بازگرداندن خطای ۴۰۰. |
| **۱۷** | بک‌اند / اکسل | **مهم** | نادیده گرفتن ستون‌های وضعیت و بخش در فایل‌های اکسل | در ایمپورت بخش‌ها و انتساب‌ها، وضعیت فعال روی `True` هاردکد شده و ستون اکسل نادیده گرفته می‌شود؛ در ایمپورت طرف‌حساب‌ها نیز ستون‌های بخش و پروژه نادیده گرفته می‌شوند. | استخراج مقادیر از ستون‌های استاندارد اکسل و اعمال وضعیت و بخش مربوطه. |
| **۱۸** | بک‌اند / اکسل | **مهم** | انتساب تصادفی بخش در صورت خالی بودن کد پروژه در اکسل | اگر کد پروژه در اکسل خالی باشد، بخش اول با کد مشابه بدون توجه به پروژه انتخاب می‌شود که پرسنل را به بخش پروژه دیگری منتسب می‌کند. | اجباری کردن تطبیق کد پروژه و بخش در موتور اکسل. |
| **۱۹** | فرانت‌اند / ترافیک | **مهم** | دانلود کامل و بدون فیلتر تمامی انتساب‌ها در هر بار واکشی | متد `loadAssignments()` همیشه با پارامتر `undefined` صدا زده می‌شود و کل انتساب‌های شرکت را در کلاینت فیلتر می‌کند که بار ترافیکی بالایی تحمیل می‌کند. | ارسال پارامترهای سرور (`project_id` و `role`) به API انتساب‌ها و کش کردن هوشمند. |
| **۲۰** | فرانت‌اند / کاربران | **مهم** | نمایش کاربران غیرفعال/معلق در لیست انتساب سریع | `systemUsers` تمامی کاربران از جمله کاربران غیرفعال (`is_active: false`) را واکشی می‌کند و مدیر می‌تواند افراد معلق را به بخش‌ها منتسب کند. | فیلتر کردن کاربران بر اساس `is_active: true` در متد `availableUsersForQuickAssign`. |
| **۲۱** | فرانت‌اند / داده | **مهم** | باقی ماندن انتساب‌های پروژه یا بخش حذف‌شده در حافظه فرانت | پس از حذف یک پروژه یا بخش، آرایه `this.userAssignments` به‌روزرسانی نمی‌شود و انتساب‌های موارد حذف‌شده تا رفرش دستی در صفحه باقی می‌مانند. | پاکسازی انتساب‌های مرتبط یا فراخوانی مجدد `loadAssignments()` پس از حذف پروژه/بخش. |
| **۲۲** | فرانت‌اند / کد مرده | **متوسط** | وجود متغیرها و متدهای مرده `newAssignment` و `saveAssignment` | در فایل تایپ‌اسکریپت این متغیرها و متدها باقی مانده‌اند؛ در حالی که فرم سنتی انتساب از قالب HTML به طور کامل حذف شده است. | پاکسازی کدهای مرده جهت ارتقای خوانایی و نگهداری‌پذیری. |
| **۲۳** | فرانت‌اند / UX | **متوسط** | نبود امکان ویرایش نقش یا وضعیت فعال انتساب در فرانت‌اند | در هر دو نمای ماتریسی و جدولی، فقط دکمه «لغو انتساب» (حذف فیزیکی) وجود دارد و امکان تغییر سمت فرد یا غیرفعال‌سازی موقت او تعبیه نشده است. | افزودن دکمه ویرایش نقش و سوئیچ تغییر وضعیت فعال/غیرفعال برای انتساب‌ها. |
| **۲۴** | فرانت‌اند / UX | **متوسط** | اسکرول نشدن خودکار صفحه به فرم ویرایش در جداول طولانی | با زدن دکمه ویرایش در پایین جدول، صفحه به فرم بالا اسکرول نمی‌شود و کاربر متوجه بارگذاری رکورد در فرم نمی‌گردد. | افزودن `scrollIntoView({ behavior: 'smooth' })` به توابع ویرایش. |
| **۲۵** | فرانت‌اند / خطا | **متوسط** | پنهان ماندن خطاهای اعتبارسنجی سرور در توست‌ها | توست‌ها فقط `err?.error?.error` را می‌خوانند؛ در حالی که DRF خطاها را به فرم `{ field: [...] }` بازمی‌گرداند و کاربر پیام دقیق خطا را نمی‌بیند. | پیاده‌سازی متد استخراج خطاهای فیلدی از پاسخ DRF و نمایش متن شفاف در توست. |
| **۲۶** | فرانت‌اند / کش | **متوسط** | عدم ابطال کش IndexedDB پس از عملیات پروژه‌ها، بخش‌ها و طرف‌حساب‌ها | متدهای موتاسیون پروژه، بخش و طرف‌حساب کش آفلاین SWR را ابطال نمی‌کنند و در رفرش بعدی، داده‌های کهنه نمایش داده می‌شوند. | فراخوانی `offlineSync.invalidateCache` پس از هر عملیات موفق موتاسیون. |
| **۲۷** | فرانت‌اند / ناوبری | **متوسط** | اختلال در دکمه Back مرورگر به دلیل `replaceUrl: true` | متد `switchSubTab` با `replaceUrl: true` اجرا می‌شود؛ بنابراین تغییر تب در تاریخچه ثبت نشده و دکمه Back کاربر را از سامانه خارج می‌کند. | حذف `replaceUrl: true` در جابجایی تب‌ها و ثبت تاریخچه صحیح ناوبری. |
| **۲۸** | فرانت‌اند / امنیت | **متوسط** | نبود روت `projects-and-sections` در جدول `ROUTE_PERMISSIONS` گارد | در `auth.guard.ts` این روت در `ROUTE_PERMISSIONS` ثبت نشده و بررسی دقیق مجوز RBAC برای آن دور زده می‌شود. | ثبت روت در `ROUTE_PERMISSIONS` با مجوزهای `view_sys_projects` و `admin_all`. |
| **۲۹** | بک‌اند / اعتبارسنجی | **متوسط** | نبود اعتبارسنجی شبا و کد ملی در سریالایزر طرف‌حساب | `CounterpartySerializer` شماره شبا و کد ملی را بدون بررسی الگوریتم Mod 97 و طول استاندارد ذخیره می‌کند. | افزودن اعتبارسنجی سخت‌گیرانه شبا و کد ملی در سریالایزر طرف‌حساب. |
| **۳۰** | فرانت‌اند / فرم | **متوسط** | عدم نرمال‌سازی ارقام فارسی در کدهای پروژه، بخش، تلفن و کد ملی | در فرم‌های پروژه، بخش و طرف‌حساب، ارقام فارسی تایپ‌شده به انگلیسی تبدیل نمی‌شوند و در دیتابیس با ارقام فارسی ذخیره می‌گردند که سرچ دیتابیس را مختل می‌کند. | افزودن نرمال‌سازی ارقام فارسی به انگلیسی قبل از ارسال به سرور. |
| **۳۱** | فرانت‌اند / فرم | **متوسط** | نبود فیلد وضعیت فعال (`is_active`) برای طرف‌حساب‌ها | در فرم و جدول طرف‌حساب‌ها فیلد `is_active` غایب است و امکان مشاهده طرف‌حساب‌های غیرفعال یا تغییر وضعیت آنها وجود ندارد. | افزودن چک‌باکس فعال در فرم و ستون وضعیت در جدول طرف‌حساب‌ها. |
| **۳۲** | فرانت‌اند / مدل | **متوسط** | نادیده گرفته شدن کد حساب تفصیلی (`account_code`) در طرف‌حساب‌ها | فیلد `account_code` در مدل بک‌اند برای اتصال به دفتر کل رزرو شده اما در فرانت‌اند و اکسل کاملاً نادیده گرفته شده است. | افزودن فیلد کد حساب تفصیلی به فرم، جدول و اکسل طرف‌حساب‌ها. |
| **۳۳** | فرانت‌اند / UX | **متوسط** | بسته شدن ناگهانی مودال اکسل بدون نمایش گزارش نتیجه | به محض اتمام ایمپورت، `closeExcelModal()` صدا زده می‌شود و کاربر فرصت مشاهده تعداد موفق، صرف‌نظرشده و خطاهای سطور را پیدا نمی‌کند. | باز نگه داشتن مودال در مرحله نتیجه (`step = 'result'`) و بستن صرفاً با دکمه کاربر. |
| **۳۴** | بک‌اند / کارایی | **متوسط** | غیرفعال بودن کامل صفحه‌بندی (`pagination_class = None`) | هر ۴ ویوست فاقد صفحه‌بندی هستند و در دیتابیس‌های سازمانی با هزاران رکورد، کل حجم به صورت یکجا بارگذاری می‌شود. | آماده‌سازی معماری برای صفحه‌بندی و بهینه‌سازی بارگذاری رکوردهای حجیم. |
| **۳۵** | بک‌اند / داده | **متوسط** | تهی شدن (`SET_NULL`) پروژه‌ها در کارکرد و ترددها با حذف پروژه/بخش | در `DailyAttendance` و `VehicleTripLog` روابط پروژه و بخش با `SET_NULL` تعریف شده‌اند؛ حذف پروژه یا بخش تاریخچه محل کار پرسنل و ناوگان را پاک می‌کند. | مسدودسازی حذف پروژه‌ها و بخش‌های دارای سابقه تردد یا کارکرد پرسنل. |
| **۳۶** | بک‌اند / لاجیک | **متوسط** | نمایش بخش‌های متعلق به پروژه‌های غیرفعال در `my-sections` | اکشن `my_sections` فعال بودن پروژه مادر (`section__project__is_active=True`) را بررسی نمی‌کند. | افزودن شرط فعال بودن پروژه در کوئری اکشن `my_sections`. |
| **۳۷** | بک‌اند / پایداری | **متوسط** | خطای ۵۰۰ در کوئری پارامترهای غیرعددی ویوست‌ها | ارسال مقادیر غیرعددی مانند `project_id=abc` در ویوست‌ها باعث پرتاب `ValueError` و خطای ۵۰۰ سرور می‌شود. | ایمن‌سازی فیلترهای کوئری‌ست با بررسی عددی بودن پارامترها قبل از فیلتر. |
| **۳۸** | بک‌اند / اکسل | **متوسط** | نبود محافظت `try/except` در بارگذاری فایل در اکسل انجین | در صورت بارگذاری فایل خراب، غیر اکسل یا دارای پسوند اشتباه، `openpyxl` کرش ۵۰۰ می‌دهد. | قرار دادن فرآیند باز کردن فایل در بلاک محافظت و بازگرداندن خطای فارسی کاربرپسند. |
| **۳۹** | فرانت‌اند / استایل | **جزئی** | وجود کلاس‌های بلااستفاده CSS در `projects-and-sections.css` | کلاس‌های `.subtab-btn` و `.active-subtab` از نسخه‌های قدیمی باقی مانده و بلااستفاده هستند. | پاکسازی کلاس‌های مرده فایل استایل. |
| **۴۰** | فرانت‌اند / چرخه حیات | **جزئی** | فراخوانی تکراری و موازی `loadSections()` در مقداردهی اولیه | در `ngOnInit` متدهای `loadAllData()` و `setupRouteQuerySync()` همزمان بخش‌ها را واکشی می‌کنند. | تجمیع جریان بارگذاری اولیه در یک کانال منسجم. |
| **۴۱** | فرانت‌اند / ذخیره‌سازی | **جزئی** | باقی ماندن شناسه‌های بخش‌های حذف‌شده در `localStorage` | در `loadCollapsedSectionsState` شناسه‌ها بدون اعتبارسنجی نگهداری شده و کارت‌های منقضی در حافظه می‌مانند. | پالایش شناسه‌های ذخیره‌شده با لیست بخش‌های فعال واقعی. |

---

## ۳. طرح فازبندی‌شده اصلاح و ارتقا (Implementation Roadmap)

این طرح در قالب **۶ فاز مهندسی‌شده و متوالی** اجرا خواهد شد:

```
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۱: امن‌سازی، حفاظت از داده‌ها، قوانین عدم حذف و دسترسی‌های RBAC   │
│ (حل موارد ۱، ۲، ۳، ۲۸، ۳۵)                                            │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۲: اصلاح موتور اکسل، پیش‌نمایش واقعی (Dry Run) و مدیریت خطاها      │
│ (حل موارد ۴، ۱۷، ۱۸، ۳۳، ۳۸)                                           │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۳: بهینه‌سازی کوئری‌های بک‌اند، پایداری API و یکپارچگی منطق دیتابیس │
│ (حل موارد ۷، ۱۳، ۱۴، ۱۵، ۱۶، ۲۹، ۳۴، ۳۶، ۳۷)                           │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۴: بهینه‌سازی عملکرد فرانت‌اند، رفع Lag ماتریس و همگام‌سازی زنده   │
│ (حل موارد ۵، ۶، ۸، ۹، ۱۰، ۲۱، ۲۲، ۳۹، ۴۰)                              │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۵: اصلاح فرم‌ها، اعتبارسنجی‌ها، تعاملات UX و کش آفلاین             │
│ (حل موارد ۱۱، ۱۲، ۱۹، ۲۰، ۲۳، ۲۴، ۲۵، ۲۶، ۲۷، ۳۰، ۳۱، ۳۲، ۴۱)          │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ فاز ۶: تست‌های رگرسیون، ارتقای ایجنت نگهبان ساختار و گواهی نهایی      │
│ (راستی‌آزمایی ۴۱ گانه، بیلد فرانت‌اند و پاس شدن نگهبان ۲۹ و نگهبان پروژه)│
└────────────────────────────────────────────────────────────────────────┘
```

---

## ۴. لیست تفصیلی تسک‌های اجرایی شناسه‌دار

<div dir="rtl" align="right">

### فاز ۱: امن‌سازی، حفاظت از داده‌ها، قوانین عدم حذف و دسترسی‌های RBAC
- [x] <!-- id: 101 --> **تسک ۱.۱ (بک‌اند):** اصلاح رفتار `on_delete` در مدل `ExpenseInvoice` از `CASCADE` به `PROTECT` برای فیلدهای `section` و `counterparty` (تضمین حفظ ابدی اسناد مالی).
- [x] <!-- id: 102 --> **تسک ۱.۲ (بک‌اند):** اصلاح رفتار `on_delete` در مدل `PayrollYearlySettings` برای رابطه `project` از `CASCADE` به `PROTECT` (جلوگیری از پاک شدن ضرایب مالیاتی و رده‌های شغلی پروژه).
- [x] <!-- id: 103 --> **تسک ۱.۳ (بک‌اند):** ایمن‌سازی متدهای `perform_destroy` در `FinancialProjectViewSet`، `ProjectSectionViewSet` و `CounterpartyViewSet`: بررسی وجود اسناد وابسته (فاکتورها، کارکرد روزانه، ترددها)؛ در صورت وجود سابقه، مسدود کردن حذف فیزیکی و بازگرداندن خطای شفاف یا سوئیچ خودکار به غیرفعال‌سازی نرم (`is_active=False`).
- [x] <!-- id: 104 --> **تسک ۱.۴ (بک‌اند):** اصلاح متد `perform_destroy` در `UserSectionAssignmentViewSet`: تغییر رفتار حذف فیزیکی به غیرفعال‌سازی نرم (`is_active=False`) برای نگهداری ردپای حسابرسی و انتساب‌های تاریخی.
- [x] <!-- id: 105 --> **تسک ۱.۵ (بک‌اند):** ایجاد کلاس Permission اختصاصی در `personnel/permissions.py` جهت کنترل دسترسی‌های ایجاد، ویرایش و حذف ساختار سازمانی صرفاً برای نقش‌های مجاز مالی/ادمین.
- [x] <!-- id: 106 --> **تسک ۱.۶ (فرانت‌اند):** ثبت مسیر `projects-and-sections` در جدول `ROUTE_PERMISSIONS` در `auth.guard.ts` با مجوزهای `view_sys_projects` و `admin_all`.

---

### فاز ۲: اصلاح موتور اکسل، پیش‌نمایش واقعی (Dry Run) و مدیریت خطاها
- [x] <!-- id: 201 --> **تسک ۲.۱ (بک‌اند):** افزودن پارامتر `dry_run=False` به توابع چهارگانه ایمپورت در `org_excel_engine.py` و ویوست‌ها، به طوری که در مرحله اعتبارسنجی اولیه (پیش‌نمایش مودال)، هیچ رکوردی Commit نشود و صرفاً خلاصه آماری سطور جدید/ویرایشی/خطاها برگردد.
- [x] <!-- id: 202 --> **تسک ۲.۲ (بک‌اند):** اصلاح `import_sections_from_excel`: خواندن ستون وضعیت فعال (`is_active`) از اکسل به جای هاردکد کردن `True`.
- [x] <!-- id: 203 --> **تسک ۲.۳ (بک‌اند):** اصلاح `import_assignments_from_excel`: اجباری کردن تطبیق کد پروژه و بخش، و خواندن ستون وضعیت فعال از فایل.
- [x] <!-- id: 204 --> **تسک ۲.۴ (بک‌اند):** اصلاح `import_counterparties_from_excel`: خواندن ستون‌های بخش و پروژه و منتسب کردن صحیح طرف‌حساب‌ها به بخش مربوطه.
- [x] <!-- id: 205 --> **تسک ۲.۵ (بک‌اند):** قرار دادن فرآیند باز کردن فایل در `org_excel_engine.py` در بلاک `try/except` و بازگرداندن پیام خطای فارسی واضح در صورت بارگذاری فایل خراب یا غیراکسل.
- [x] <!-- id: 206 --> **تسک ۲.۶ (فرانت‌اند):** هماهنگ‌سازی فراخوانی‌های `importFn` در `projects-and-sections.ts` جهت ارسال فلگ `dryRun` به API و بستن مودال صرفاً با تایید کاربر در مرحله نمایش نتایج (`step = 'result'`).

---

### فاز ۳: بهینه‌سازی کوئری‌های بک‌اند، پایداری API و یکپارچگی منطق دیتابیس
- [x] <!-- id: 301 --> **تسک ۳.۱ (بک‌اند):** رفع خطای N+1 Query در `FinancialProjectViewSet` و اکسل انجین با اضافه کردن `.annotate(sections_count=Count('sections'))` به کوئری‌ست پایه.
- [x] <!-- id: 302 --> **تسک ۳.۲ (بک‌اند):** اصلاح لاجیک ایجاد انتساب در `UserSectionAssignmentViewSet`: در صورت تلاش برای ایجاد انتسابی که قبلاً با `is_active=False` وجود داشته، رکورد موجود به جای خطای یکتایی فعال گردد (`is_active=True`).
- [x] <!-- id: 303 --> **تسک ۳.۳ (بک‌اند):** اصلاح اکشن `clone_assignments`: فعال‌سازی مجدد رکوردهای غیرفعال در بخش مقصد در صورت تشابه نقش و کاربر.
- [x] <!-- id: 304 --> **تسک ۳.۴ (بک‌اند):** اعتبارسنجی ورودی‌ها در `bulk-assign`: بررسی وجود شناسه‌های کاربران در `auth_user` و معتبر بودن رشته نقش با `ROLE_CHOICES`.
- [x] <!-- id: 305 --> **تسک ۳.۵ (بک‌اند):** اصلاح تبدیل عددی در `clone_assignments` با کنترل خطای تبدیل رشته به عدد.
- [x] <!-- id: 306 --> **تسک ۳.۶ (بک‌اند):** اصلاح اکشن `my_sections` جهت بررسی فعال بودن پروژه والد (`section__project__is_active=True`).
- [x] <!-- id: 307 --> **تسک ۳.۷ (بک‌اند):** افزودن اعتبارسنجی ۲۴ رقمی و الگوریتم Mod 97 شبا و طول کد ملی در `CounterpartySerializer`.
- [x] <!-- id: 308 --> **تسک ۳.۸ (بک‌اند):** ایمن‌سازی فیلترهای کوئری‌ست ویوست‌ها در برابر پارامترهای غیرعددی نامعتبر (`project_id`, `section_id`, `user_id`).

---

### فاز ۴: بهینه‌سازی عملکرد فرانت‌اند، رفع Lag ماتریس و همگام‌سازی زنده
- [x] <!-- id: 401 --> **تسک ۴.۱ (فرانت‌اند):** حذف فراخوانی توابع فیلتر از تمپلیت HTML در مودال کپی چارت و کش کردن نتایج در آرایه `availableCloneSourceSections`.
- [x] <!-- id: 402 --> **تسک ۴.۲ (فرانت‌اند):** حذف فراخوانی مکرر `getSelectedSourceSectionDetails()` در تمپلیت و جایگزینی با شیء کش‌شده `cloneSourceStats` در زمان تغییر بخش مبدأ.
- [x] <!-- id: 403 --> **تسک ۴.۳ (فرانت‌اند):** اصلاح باگ منطقی متد `hasManagerAssigned`: ارزیابی حضور مدیر مستقیماً از روی انتساب‌های خام بخش (`userAssignments`) تا در صورت فیلتر نقش‌ها آلارم کاذب داده نشود.
- [x] <!-- id: 404 --> **تسک ۴.۴ (فرانت‌اند):** رفع باگ به‌روزرسانی زنده وب‌سوکت برای بخش‌ها با حذف آرایه موازی `allProjectSections` و نگهداری یک منبع واحد حقیقت (`projectSections`).
- [x] <!-- id: 405 --> **تسک ۴.۵ (فرانت‌اند):** اصلاح باگ خالی شدن آرایه‌های داده در خطای شبکه (`error` callbacks) و حفظ داده‌های موجود روی صفحه.
- [x] <!-- id: 406 --> **تسک ۴.۶ (فرانت‌اند):** پاکسازی انتساب‌های مرتبط در حافظه فرانت پس از حذف موفق پروژه یا بخش سازمانی.
- [x] <!-- id: 407 --> **تسک ۴.۷ (فرانت‌اند):** پاکسازی کدهای مرده `newAssignment` و `saveAssignment` و استایل‌های بلااستفاده در `projects-and-sections.css`.

---

### فاز ۵: اصلاح فرم‌ها، اعتبارسنجی‌ها، تعاملات UX و کش آفلاین
- [x] <!-- id: 501 --> **تسک ۵.۱ (فرانت‌اند):** اصلاح تمامی دراپ‌داون‌های پروژه‌ها و بخش‌ها در قالب HTML از `[value]="p.id"` به `[ngValue]="p.id"`.
- [x] <!-- id: 502 --> **تسک ۵.۲ (فرانت‌اند):** اصلاح متغیر ارسالی در دانلود اکسل انتساب‌ها از `this.selectedProjectId` به `this.assignmentProjectFilter`.
- [x] <!-- id: 503 --> **تسک ۵.۳ (فرانت‌اند):** فیلتر کردن کاربران فعال (`is_active: true`) در مودال انتساب سریع جهت جلوگیری از انتساب کاربران معلق.
- [x] <!-- id: 504 --> **تسک ۵.۴ (فرانت‌اند):** افزودن قابلیت ویرایش نقش و تغییر وضعیت فعال/غیرفعال برای انتساب‌ها در نمای ماتریسی و نمای جدولی.
- [x] <!-- id: 505 --> **تسک ۵.۵ (فرانت‌اند):** افزودن اسکرول نرم خودکار (`scrollIntoView`) به ابتدای فرم هنگام کلیک روی دکمه ویرایش پروژه، بخش یا طرف‌حساب.
- [x] <!-- id: 506 --> **تسک ۵.۶ (فرانت‌اند):** بهبود پیام‌های خطا در اشتراک‌های API جهت استخراج خطاهای فیلدی جنگو (`err.error`) و نمایش پیام شفاف در توست.
- [x] <!-- id: 507 --> **تسک ۵.۷ (فرانت‌اند):** فراخوانی `offlineSync.invalidateCache` پس از ایجاد، ویرایش و حذف پروژه‌ها، بخش‌ها و طرف‌حساب‌ها جهت تضمین هماهنگی کش SWR.
- [x] <!-- id: 508 --> **تسک ۵.۸ (فرانت‌اند):** اصلاح مدیریت تاریخچه مرورگر: حذف `replaceUrl: true` هنگام تغییر ساب‌تب‌ها جهت حفظ کارکرد صحیح دکمه Back.
- [x] <!-- id: 509 --> **تسک ۵.۹ (فرانت‌اند):** افزودن فیلد وضعیت فعال (`is_active`) و کد حساب تفصیلی (`account_code`) به فرم و جدول طرف‌حساب‌های مالی.
- [x] <!-- id: 510 --> **تسک ۵.۱۰ (فرانت‌اند):** تبدیل خودکار ارقام فارسی و عربی به انگلیسی در فیلدهای کد پروژه، کد بخش، تلفن و کد ملی طرف‌حساب‌ها.
- [x] <!-- id: 511 --> **تسک ۵.۱۱ (فرانت‌اند):** پالایش شناسه‌های ذخیره‌شده بخش‌های باز/بسته در `localStorage` با لیست بخش‌های فعال واکشی‌شده.

---

### فاز ۶: تست‌های رگرسیون، ارتقای ایجنت نگهبان ساختار و تایید نهایی
- [x] <!-- id: 601 --> **تسک ۶.۱ (آزمون):** گسترش سوئیت آزمون سخت‌گیرانه در `section_guardian.py` برای سنجش خودکار موارد زیر:
  - مسدود بودن حذف پروژه‌ها و بخش‌های دارای سند مالی یا کارکرد
  - صحت عملکرد پیش‌نمایش اکسل (`dry_run=True` بدون تغییر دیتابیس)
  - فعال‌سازی مجدد انتساب‌های غیرفعال بدون خطای یکتایی
  - عدم وجود N+1 Query در شمارش بخش‌ها و اکسل پروژه‌ها
  - صحت اعتبارسنجی شماره شبا و کد ملی
- [x] <!-- id: 602 --> **تسک ۶.۲ (آزمون):** اجرای تست‌های بک‌اند با `./venv/Scripts/python.exe manage.py test personnel` و `./venv/Scripts/python.exe manage.py check`.
- [x] <!-- id: 603 --> **تسک ۶.۳ (آزمون):** تست بیلد کامل فرانت‌اند و بررسی سلامت کامپوننت با `npm run build`.
- [x] <!-- id: 604 --> **تسک ۶.۴ (مستندسازی):** تهیه سند گزارش کارکرد نهایی (`walkthrough`) و ثبت دستاوردها در تاریخچه پروژه.

</div>

---

## ۵. ماتریس مهار ریسک‌های معماری

| ریسک احتمالی | ضریب اهمیت | تمهیدات مهار ریسک در طرح |
| :--- | :---: | :--- |
| **شکستن اسناد مالی با تغییر `CASCADE` به `PROTECT`** | بسیار بحرانی | تغییر رابطه از `CASCADE` به `PROTECT` هیچ تأثیری روی داده‌های موجود دیتابیس ندارد و صرفاً مانع حذف‌های تخریبی آینده می‌شود. مایگریشن جنگو بدون دستکاری داده‌های قبلی اعمال می‌شود. |
| **ایجاد تاخیر یا Timeout پشت Cloudflare Tunnel** | مهم | با افزودن `dry_run`، محاسبات سنگین پیش‌نمایش در حافظه انجام شده و تراکنش دیتابیسی باز نمی‌ماند؛ در کنار آن با رفع N+1 Query و بهینه‌سازی کوئری‌ها، زمان پاسخ‌دهی به زیر ۳۰۰ میلی‌ثانیه می‌رسد. |
| **ناهماهنگی کش آفلاین کلاینت با سرور** | متوسط | با ابطال صریح کلیدهای مسیر (`invalidateCache`) پس از هر تغییر موفق، کش IndexedDB فوراً با داده‌های معتبر تازه‌سازی می‌شود. |

---

## ۶. وضعیت نهایی

این طرح به طور کامل بازبینی، غنی‌سازی و با **۴۱ تسک جامع** بازنویسی شد. طبق دستور کاربر، **هیچ خط کدی در سورس‌کد پروژه تغییر نیافته است** و سامانه آماده است تا پس از تایید کاربر، فازهای اجرایی را آغاز نماید.
