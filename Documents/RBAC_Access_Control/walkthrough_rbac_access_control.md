<div dir="rtl" align="right">

# گزارش و مستندات معماری سطوح دسترسی و تفکیک وظایف (RBAC & SoD Walkthrough)

این سند گزارش فنی و آزمون‌های معماری طراحی، استقرار و تثبیت سطوح دسترسی مبتنی بر نقش (`RBAC`) و ایزولاسیون کامل نقش‌ها (`Segregation of Duties - SoD`) را ثبت می‌کند.

---

## ۱. خلاصه اقدامات و اصلاحات کلیدی انجام‌شده
1. **ایزولاسیون صددرصدی منوها بر پایه نقش فعال (`Strict Sidebar Role Isolation`):**
   - در فایل `warehouse-front/src/app/components/layout/layout.ts`، تمام فال‌بک‌های عمومی پرسنلی (`view_sys_personnel`, `view_sys_projects`) حذف شدند.
   - سیگنال‌های ناوبری پنج‌گانه (`employeeNavItems`, `supervisorNavItems`, `accountantNavItems`, `managerNavItems`, `treasurerNavItems`) مشروط به `activeRole === 'role_code' || isAdmin` شدند.
   - منوی قدیمی و عمومی «کارکرد و حسابداری» (`accountingNavItems`) برای کاربران تک‌نقشه پنهان شد تا از نشت صفحات و شلوغی سایدبار جلوگیری شود.
   - کارت تغییر انبار در پایین سایدبار منحصراً به کاربران دارای مجوز انبارداری (`hasWarehouseAccess`) مقید گردید.
2. **سرویس هدایت هوشمند و گارد نقش در سرویس پرسونای برنامه (`AppPersonaService`):**
   - اصلاح تابع `availableRoles` جهت جلوگیری از انتساب ناخواسته نقش کارمند به کاربر حسابدار در حضور مجوزهای پرسنلی.
   - افزودن متد `getDefaultRouteForRole(roleCode?: string)` جهت هدایت کاربر به پورتال تخصصی خودش (`accountant` -> `/app/finance/accountant-payroll`).
   - ارتقای `canAccessRoute` برای اعتبارسنجی دوطرفه نقش و مسیر جاری و مسدودسازی دسترسی‌های غیرمجاز.
3. **گارد مسیرهای اختصاصی فرانت‌اند (`auth.guard.ts`):**
   - تزریق `AppPersonaService` و اعتبارسنجی `canAccessRoute(state.url)`. در صورت عدم انطباق با نقش فعال، کاربر بی‌درنگ به صفحه پیش‌فرض مجاز خود ریدایرکت می‌شود.
   - پالایش کامل جدول `ROUTE_PERMISSIONS` و حذف مجوزهای عمومی از روت‌های اختصاصی هر نقش.
4. **تنظیم ریدایرکت‌های ریشه ماژول‌ها (`app.routes.ts` و `accounting.routes.ts`):**
   - در هنگام ورود کاربر به ریشه سامانه (`/app` یا `/app/finance`)، کاربر بر اساس نقش فعال مستقیماً به داشبورد تخصصی خود هدایت می‌شود.
5. **استانداردسازی پرزت‌های نقش در مدال مدیریت کاربران (`users.ts`):**
   - پالایش و تفکیک دقیق مجوزهای انتساب با یک کلیک برای کارمند، سرپرست، حسابدار، مدیر و خزانه‌دار و حذف مجوزهای انبار از نقش‌های مالی.

---

## ۲. تفکیک صددرصدی مجوزهای انبارداری و حسابداری (Zero Shared Permissions)
- **جداسازی کامل پرمیشن‌های سرپرستی:**
  - مجوز `view_sys_supervisor` که پیشتر میان انبار و پرسنلی مشترک بود، تفکیک شد:
    - انبارداری: `accounts.can_act_as_wh_supervisor`
    - کارگاه/پرسنلی: `accounts.can_act_as_workshop_supervisor`
- **جداسازی کامل پرمیشن‌های مدیریتی:**
  - مجوز `view_sys_reports` که میان مدیر شرکت و مدیر انبار مشترک بود، تفکیک شد:
    - مدیر انبارداری: `accounts.can_act_as_wh_manager`
    - مدیر کل شرکت (حسابداری): `accounts.can_act_as_company_manager`
- **مایگریشن رو به جلو:** مایگریشن `accounts.0004_alter_customuser_options.py` با موفقیت تولید و بر روی پایگاه داده اعمال شد.
- **کاتالوگ ماژول‌ها و میان‌افزار (`middleware.py`):** استخراج `allowed_apps` و `role_scopes` به گونه‌ای بازنویسی شد که هیچ کاربری با مجوزهای مالی اجازه دسترسی به اپلیکیشن انبارداری (`warehouse_counting`) نداشته باشد و بالعکس.

---

## ۳. بازنشانی گذرواژه کلیه کاربران (Password Reset to 1234567)
گذرواژه تمامی ۱۲ کاربر ثبت‌شده در پایگاه داده با موفقیت به مقدار `1234567` بازنشانی گردید و فلگ الزام به تغییر پسورد (`requires_password_change`) برای همه بر روی `False` تنظیم شد:
1. `admin` (مدیر شرکت - Superuser) ➔ `1234567`
2. `h.alishvandi` (حبیب عالیشوندی - مدیر شرکت) ➔ `1234567`
3. `a.haghshenas` (امیر حق‌شناس - مدیر پروژه‌ها) ➔ `1234567`
4. `m.bagheri` (مهری باقری - حسابدار ارشد) ➔ `1234567`
5. `e.taghavi` (عنایت تقوی - خزانه‌دار دالان و پارسیان) ➔ `1234567`
6. `n.beyrami` (نسرین بیرمی - خزانه‌دار انبارداری) ➔ `1234567`
7. `h.akbari` (حسین اکبری - سرپرست کارگاه انبارداری) ➔ `1234567`
8. `r.hoseeni` (رزاق حسینی - سرپرست کارگاه دالان) ➔ `1234567`
9. `m.amiri` (محمد امیری - سرپرست کارگاه پارسیان) ➔ `1234567`
10. `a.mohammadi` (امین محمدی - کارمند ثبت انبارداری) ➔ `1234567`
11. `gh.alishvandi` (قاسم عالیشوندی - کارمند ثبت دالان) ➔ `1234567`
12. `a.ghashghaee` (علی قشقایی - کارمند ثبت) ➔ `1234567`

---

## ۴. نتایج آزمون سریع مرورگر و DOM برای تمامی کاربران واقعی (DOM Unit Test Results)
آزمون سریع در بستر Vitest + jsdom در فایل `src/app/core/auth/all-users-permissions-dom.spec.ts` برای تک‌تک ۱۲ کاربر واقعی دیتابیس اجرا شد:

| ردیف | نام کاربری | عنوان و نقش سازمانی | پروژه‌ها و بخش‌های تخصیص‌یافته | دسترسی انبارداری؟ | وضعیت در ساختار DOM سایدبار | نتیجه تست DOM |
|:---:|:---|:---|:---|:---:|:---|:---:|
| ۱ | `admin` | مدیر شرکت (Superuser) | سراسری سیستم (تمام پروژه‌ها و بخش‌ها) | ✅ بله | دسترسی کامل به همه پورتال‌ها و تعویض انبار در DOM | ✅ قبولی کامل (Pass) |
| ۲ | `h.alishvandi` | مدیر عامل / شرکت (`manager`) | دالان (۲ بخش)، پارسیان (۲ بخش)، انبارداری (۱ بخش) | ❌ خیر | فقط پورتال مدیریت کلان در DOM (۵ لینک)؛ سایر نقش‌ها و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۳ | `a.haghshenas` | مدیر پروژه‌ها (`manager`) | دالان (۲ بخش)، پارسیان (۲ بخش) | ❌ خیر | فقط پورتال مدیریت کلان در DOM (۵ لینک)؛ سایر نقش‌ها و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۴ | `m.bagheri` | حسابدار ارشد (`accountant`) | دالان (۲ بخش)، پارسیان (۲ بخش)، انبارداری (۱ بخش) | ❌ خیر | فقط پورتال ۶‌گانه حسابداری در DOM (۶ لینک)؛ سایر نقش‌ها و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۵ | `e.taghavi` | خزانه‌دار (`treasury`) | دالان (۲ بخش)، پارسیان (۲ بخش) | ❌ خیر | فقط پورتال خزانه‌داری در DOM (۵ لینک)؛ سایر نقش‌ها و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۶ | `n.beyrami` | خزانه‌دار (`treasury`) | انبارداری (۱ بخش) | ❌ خیر | فقط پورتال خزانه‌داری در DOM (۵ لینک)؛ سایر نقش‌ها و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۷ | `h.akbari` | سرپرست کارگاه (`supervisor`) | انبارداری (۱ بخش) | ❌ خیر | فقط پورتال سرپرستی در DOM (۶ لینک)؛ منوی کارمند، حسابدار، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۸ | `r.hoseeni` | سرپرست کارگاه (`supervisor`) | دالان (۱ بخش) | ❌ خیر | فقط پورتال سرپرستی در DOM (۶ لینک)؛ منوی کارمند، حسابدار، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۹ | `m.amiri` | سرپرست کارگاه (`supervisor`) | پارسیان (۱ بخش) | ❌ خیر | فقط پورتال سرپرستی در DOM (۶ لینک)؛ منوی کارمند، حسابدار، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۱۰ | `a.mohammadi` | کارمند / اپراتور (`operator`) | انبارداری (۱ بخش) | ❌ خیر | فقط منوی ثبت کارمند در DOM (۶ لینک)؛ تاییدات سرپرستی، حسابداری، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۱۱ | `gh.alishvandi` | کارمند / اپراتور (`operator`) | دالان (۱ بخش) | ❌ خیر | فقط منوی ثبت کارمند در DOM (۶ لینک)؛ تاییدات سرپرستی، حسابداری، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |
| ۱۲ | `a.ghashghaee` | کارمند / اپراتور (`operator`) | بدون بخش در دیتابیس (آماده تخصیص) | ❌ خیر | فقط منوی ثبت کارمند در DOM (۶ لینک)؛ تاییدات سرپرستی، حسابداری، مدیر و کارت انبار محو | ✅ قبولی کامل (Pass) |

**خلاصه آزمون Vitest:**
- تعداد تست‌ها: ۱۲ از ۱۲ پاس شد (`12 passed`)
- زمان اجرای تست‌های DOM: ۵۶ میلی‌ثانیه
- خروج: بدون کوچک‌ترین خطای تایپ‌اسکریپت یا تداخل در رندرهای DOM

---

</div>
