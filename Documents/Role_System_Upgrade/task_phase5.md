<div dir="rtl" align="right">

# وظایف اجرایی — فاز ۵: معماری مبتنی بر دسترسی (RBAC)

## ۱. بک‌اند (تنظیمات دسترسی‌ها)
- [x] ۱.۱ — آپدیت فایل `seed_permissions.py` برای افزودن دسترسی‌های جدید عملیاتی (`can_act_as_...`).
- [x] ۱.۲ — اجرای `seed_permissions` برای ثبت دسترسی‌ها در دیتابیس.
- [x] ۱.۳ — اختصاص دسترسی‌های جدید به ۸ نقش پایه در دیتابیس (بروزرسانی نقش‌های فعلی).

## ۲. بک‌اند (تغییرات API و منطق تجاری)
- [x] ۲.۱ — افزودن فیلتر `?has_perm=` به `UserViewSet` (در `accounts/views.py`).
- [x] ۲.۲ — جایگزینی چک‌های `user.groups.filter(name=...)` با `user.has_perm(...)` در متدهای `CountTaskViewSet` (`inventory/views.py`).
- [x] ۲.۳ — جایگزینی چک‌های `user.groups.filter(name=...)` با `user.has_perm(...)` در متدهای `DocTaskViewSet` (`inventory/views.py`).

## ۳. فرانت‌اند (تطبیق UI و درخواست‌ها)
- [x] ۳.۱ — اصلاح `dispatch.ts`: دریافت لیست کاربران مجاز به جای فیلتر کردن هاردکد (`fetchUsersByPerm`).
- [x] ۳.۲ — اصلاح پنل مدیریت کاربران: اطمینان از نمایش و امکان انتخاب دسترسی‌های جدید در مدال ویرایش نقش.

## ۴. تست و تأیید
- [x] تست کامل کامپایل و اجرای موفق.
- [x] تست ارجاع یک تسک و اطمینان از صحت کنترل دسترسی بدون وابستگی به نام نقش.

</div>
