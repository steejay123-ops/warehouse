<div dir="rtl" align="right">

- `[/]` 1. **برعکس کردن چیدمان (UI)**
  - `[ ]` 1.1. تغییر ترتیب `<h4>` و دکمه‌ها در `users.html` تا دکمه‌ها در سمت راست باشند.
  - `[ ]` 1.2. تغییر ترتیب دکمه‌های سفارشی و سیستمی تا سفارشی اول باشد.
- `[ ]` 2. **تغییر حالت پیش‌فرض (Logic)**
  - `[ ]` 2.1. مقدار اولیه `activeRoleTab` در `users.ts` به `custom` تغییر یابد.
- `[ ]` 3. **راستی‌آزمایی و بروزرسانی مستندات**

</div>
