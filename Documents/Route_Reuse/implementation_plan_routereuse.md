<div dir="rtl" align="right">

# برنامه‌ریزی: پیاده‌سازی RouteReuseStrategy برای کش‌کردن تمامی تب‌های اصلی

در این طرح، بخش پیچیده تنظیمات کاربری حذف شده و هدف این است که تمام داشبوردهای اصلی نرم‌افزار (انبارگردان، سرپرست، مدیر و تخصیص) همواره به صورت اتوماتیک در پس‌زمینه زنده بمانند تا سرعت جابجایی بین آن‌ها به حداکثر برسد.

## تغییرات پیشنهادی

### ۱. ایجاد فایل استراتژی ثابت
#### [NEW] `src/app/core/strategies/custom-route-reuse-strategy.ts`
یک کلاس جدید پیاده‌سازی می‌شود که از `RouteReuseStrategy` ارث‌بری می‌کند. این کلاس منطق زیر را خواهد داشت:
- بررسی می‌کند آیا روتی که کاربر از آن خارج می‌شود مقدار `data: { reuse: true }` را دارد یا خیر.
- در صورت مثبت بودن، کامپوننت و تمام وضعیت آن را در یک `Map` (کش مموری) ذخیره می‌کند.
- هنگام برگشت کاربر به همان روت، کامپوننتِ فریز شده را دقیقاً با همان شکل و اسکرول بدون نیاز به ساخت مجدد بازیابی می‌کند.

### ۲. اعمال تنظیمات در پروایدرهای سراسری
#### [MODIFY] [app.config.ts](file:///e:/warehouse%20project/warehouse-front/src/app/app.config.ts)
این استراتژی سفارشی به عنوان پروایدرِ `RouteReuseStrategy` به کل نرم‌افزار تزریق (Provide) می‌شود.

### ۳. مشخص کردن مسیرهای اصلی برای کش شدن
#### [MODIFY] [app.routes.ts](file:///e:/warehouse%20project/warehouse-front/src/app/app.routes.ts)
فقط به مسیرهای اصلی یعنی `counter`، `supervisor`، `manager` و `dispatch` فلگِ `data: { reuse: true }` اضافه می‌شود.
**نکته مهم:** صفحاتی مثل فرم‌های لاگین یا ویرایش مشخصات به عمد این تیک را نخواهند داشت تا کش نشوند و مشکل تداخل اطلاعات به وجود نیاید.

---

## ⚠️ User Review Required

> [!IMPORTANT]
> <div dir="rtl" align="right">
> طبق بررسی‌ها، این روش بهینه‌ترین و بی‌دردسرترین روش برای ایجاد تجربه کاربری عالی (شبیه اینستاگرام) است و به دلیل ثابت بودن، ریسک باگ و نشت حافظه در آن صفر است.
> 
> 🔴 **مطابق دستور صریح شما هیچ کدی تا زمان دریافت تایید اجرا نخواهد شد.** لطفاً پس از تایید طرح، کلمه **«شروع»** را تایپ کنید یا روی گزینه **Proceed** کلیک کنید.
> </div>

## برنامه راستی‌آزمایی (Verification Plan)
- اعمال تغییرات و ذخیره فایل‌ها.
- پرش بین تب‌های مختلف سیستم. هیچ‌کدام از تب‌ها نباید لودینگ داشته باشند و بلافاصله باز می‌شوند در حالی که اسکرول صفحه کاملاً حفظ شده است.

</div>
