<div dir="rtl" align="right">

# طرح جامع رفع ریشه‌ای مشکل صفحه سیاه دوربین و تطبیق هوشمند اسکنر در کارتابل‌های مالی و انبارگردان

این طرح به منظور **حل قطعی مشکل سیاه بودن تصویر وب‌کم/دوربین اسکنر، افزودن امکان سوییچ بین دوربین‌ها، رفع خطاهای پالیسی پخش ویدیو و ارتقای موتور تطبیق بارکد در کارتابل مالی (`customs`) و انبارگردان (`counter-dashboard`)** تدوین شده است.

---

## ۱. تحلیل علل ریشه‌ای (Root Cause Analysis)

| ردیف | مشکل شناسایی‌شده | علت ریشه‌ای در کد | راهکار مهندسی |
| :---: | :--- | :--- | :--- |
| **۱** | **سیاه بودن تصویر وب‌کم در ویندوز/لپ‌تاپ** | قید اجباری `facingMode: 'environment'` که باعث اتصال مرورگر به سنسور نامرئی مادون‌قرمز (IR Camera / Windows Hello) به عنوان دوربین دوم می‌شود. | تغییر به قید ترجیحی `{ ideal: 'environment' }` با قابلیت بازگشت به وب‌کم پیش‌فرض (`Fallback to { video: true }`). |
| **۲** | **عدم پخش استریم یا توقف فریم در مرورگرها** | عدم مقداردهی مستقیم خصیصه‌های `muted = true`، `playsInline = true` و `autoplay = true` روی شیء DOM ویدیو قبل از `play()`. | مقداردهی صریح ویژگی‌های DOM و کنترل خطاهای `play()`. |
| **۳** | **فقدان امکان انتخاب/تغییر دوربین** | نبود متد تشخیص چند دوربینه و نبود دکمه چرخش/تعویض دوربین در صفحه اسکنر. | پیاده‌سازی `detectAvailableCameras()` با فیلتر وب‌کم‌های مجازی و افزودن دکمه تعاملی «چرخش دوربین» (Switch Camera). |
| **۴** | **شناسایی نشدن بارکد در کارتابل مالی (`customs`)** | تطبیق انحصاری فقط با `fa_unic_code` و عدم نرمال‌سازی ارقام فارسی/عربی در متد `onBarcodeScanned`. | گسترش تطبیق به `fa_unic_code`، `en_unic_code`، `id`، `po`، `inv_rti_number` و تبدیل ارقام فارسی/عربی با `normalizeDigits`. |
| **۵** | **عدم بازگشت فوکوس پس از اسکن در حالت Headless** | نادیده‌گرفتن متد `focusInput()` در حالت `headless` بدون اطلاع‌رسانی به کامپوننت فراخواننده. | تنظیم فوکوس خودکار روی اینپوت فعال پس از اتمام اسکن. |

---

## ۲. نیازمند تایید کاربر (User Review Required)

> [!IMPORTANT]
> با اعمال این تغییرات، در صورت اتصال سیستم به چند دوربین یا وب‌کم (مانند وب‌کم اکسترنال، دوربین اصلی و دوربین سلفی)، دکمه «چرخش دوربین» به صورت خودکار در بالای صفحه اسکنر فعال خواهد شد و با یک کلیک می‌توان بین لنزها سوییچ کرد.

---

## ۳. تغییرات پیشنهادی در فایل‌ها (Proposed Changes)

### 📦 کامپوننت مشترک اسکنر (`src/app/shared/components/barcode-scanner`)

#### [MODIFY] [barcode-scanner.component.ts](file:///e:/warehouse%20project/warehouse-front/src/app/shared/components/barcode-scanner/barcode-scanner.component.ts)
- افزودن متغیرهای `availableCameras: MediaDeviceInfo[]` و `selectedCameraId: string | null`.
- پیاده‌سازی متد `detectAvailableCameras()` جهت دریافت و فیلتر کردن وب‌کم‌های موجود.
- پیاده‌سازی متد `switchCamera()` برای چرخش بین دوربین‌ها و اتصال مجدد استریم.
- اصلاح قیدهای `getUserMedia` با مکانیسم تلاش مجدد (Fallback به `{ video: true }`).
- تنظیم صریح خصوصیات DOM ویدیو (`video.muted = true; video.playsInline = true; video.autoplay = true;`).
- افزودن دکمه «چرخش دوربین» در هدر اورلی دوربین زمانی که بیش از یک دوربین در دسترس باشد.

---

### 💼 کارتابل مالی و اسناد (`src/app/components/customs`)

#### [MODIFY] [customs.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/customs/customs.ts)
- ارتقای متد `onBarcodeScanned` جهت پشتیبانی از:
  - تبدیل ارقام فارسی و عربی به انگلیسی (`normalizeDigits`).
  - تطبیق چندگانه بر روی `fa_unic_code`, `en_unic_code`, `id`, `po`, `inv_rti_number`.
  - جستجو در اقلام کارتابل شخصی و اقلام استخر کالاها.

---

### 📦 کارتابل انبارگردان (`src/app/components/counter/counter-dashboard`)

#### [MODIFY] [counter-dashboard.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/counter/counter-dashboard/counter-dashboard.ts)
- اطمینان از عملکرد صحیح اسکن بارکد، هماهنگی با دکمه تعویض دوربین و فوکوس بلادرنگ.

---

## ۴. فازبندی اجرایی (Execution Phases)

### ⚡ فاز ۱: بازنویسی لایه راه‌اندازی دوربین و تعویض لنز در `BarcodeScannerComponent`
- اصلاح قیود استریم و افزودن Fallback به تنظیمات ساده ویدیو در صورت بروز خطای سخت‌افزاری.
- پیاده‌سازی منطق فیلتر و چرخش دوربین‌ها (`switchCamera`).
- اضافه کردن دکمه چرخش دوربین به رابط کاربری بالای صفحه اسکنر.

### 💼 فاز ۲: ارتقای موتور تطبیق اسکن در کارتابل مالی (`customs.ts`)
- پیاده‌سازی `normalizeDigits` و بررسی چندگانه شناسه‌های کالا در کارتابل مالی.
- بهبود بازخوردها و پیام‌های خطای عدم یافتن کالا.

### 🧪 فاز ۳: کامپایل، تست بیلد و راستی‌آزمایی جامع
- اجرای `npm run build` و بررسی عدم وجود خطای کامپایل تایپ‌اسکریپت و انگولار.
- آزمایش سناریوهای باز شدن دوربین، چرخش لنز، بستن با Escape و اسکن کدهای چندگانه.

---

## ۵. برنامه راستی‌آزمایی (Verification Plan)

### تست‌های خودکار:
1. **تست بیلد کامل فرانت‌اند:** اجرای `npm run build` در پوشه `warehouse-front` و اطمینان از خروجی سالم بدون خطای کامپایل.

### تست‌های دستی و میدانی:
1. باز کردن کارتابل انبارگردان و کارتابل مالی و کلیک روی آیکون دوربین.
2. بررسی لود شدن تصویر زنده وب‌کم/دوربین و عدم نمایش صفحه سیاه.
3. در صورت وجود بیش از یک دوربین، کلیک روی دکمه «چرخش دوربین» و بررسی سوییچ استریم تصویر.
4. اسکن یا وارد کردن دستی بارکد با ارقام فارسی یا شماره PO و بررسی باز شدن مستقیم فرم کالا در هر دو کارتابل.

</div>
