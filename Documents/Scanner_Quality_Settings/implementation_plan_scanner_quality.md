<div dir="rtl" align="right">

# طرح پیاده‌سازی تنظیمات کیفیت دوربین و موتور سوییچ خودکار هوشمند اسکنر (Adaptive Camera Scanner Quality & Settings)

این سند معماری و طرح جامع پیاده‌سازی سیستم مدیریت کیفیت دوربین اسکنر بارکد، پشتیبانی از قوی‌ترین حالت‌های فعلی (Ultra HD / 2K)، تنظیمات تفصیلی و موتور هوشمند تشخیص افت فریم و سوییچ خودکار (Adaptive Auto-Degrade) را تبیین می‌کند.

---

## ۱. اهداف و نیازمندی‌های کلیدی

1. **حفظ کامل بالاترین کیفیت فعلی به عنوان یکی از حالت‌های اصلی:**
   - حالت فوق‌العاده / حداکثر دقت (Ultra HD / 2K): رزولوشن 1080p تا 1440p/2K، کراپ متمرکز ۸۵۰×۸۵۰، فرکانس ۶۰ms و پردازش دوگانه عمیق با `TRY_HARDER` برای بارکدهای فوق‌العاده ریز و متراکم گمرکی/دارویی.
2. **پشتیبانی از ۴ پروفایل آماده (Presets) + حالت سفارشی پیشرفته:**
   - ⚡ **هوشمند خودکار (Adaptive - پیش‌فرض پیشنهادی):** شروع از حداکثر کیفیت و سوییچ نامحسوس و پله‌ای به کیفیت‌های پایین‌تر در صورت عدم کشش گوشی.
   - 🎯 **فوق‌العاده / حداکثر دقت (Ultra HD / 2K):** قوی‌ترین حالت کنونی سیستم.
   - 💎 **کیفیت بالا (High HD - 1080p):** عملکرد عالی برای گوشی‌های میان‌رده به بالا.
   - ⚖️ **متعادل و روان (Balanced - 720p):** سرعت بالا و مصرف بهینه پردازنده.
   - 🔋 **سبک و اقتصادی (Lite / Battery Saver - 480p):** مناسب برای گوشی‌های بسیار ضعیف و بارکدهای ساده.
   - 🛠️ **حالت پیشرفته سفارشی (Custom Advanced):** امکان تنظیم دستی رزولوشن، اندازه کراپ، فرکانس اسکن و موتورهای دیکود.
3. **ساختار دوگانه تنظیمات (دیتابیس سرور + حافظه محلی گوشی):**
   - تعریف پیش‌فرض سراسری در تنظیمات کلان سیستم (`settings`) و انبار (`wh-settings`).
   - امکان انتخاب آنی پروفایل در هدر صفحه دوربین اسکنر و ذخیره در `localStorage` همان دستگاه.
   - گزینه «بازگشت به پیش‌فرض انبار/سیستم» جهت پاک‌سازی اورراید محلی و تبعیت از دیتابیس سرور.
4. **موتور سوییچ هوشمند خودکار (Adaptive Auto-Degrade):**
   - مانیتورینگ بلادرنگ زمان پردازش فریم‌ها و نرخ فریم واقعی (`Real FPS`).
   - در صورت کندی گوشی (افت به زیر ۶ فریم/ثانیه یا تاخیر بالای ۱۲۰ms برای ۲.۵ ثانیه متوالی)، کاهش نامحسوس ابعاد کراپ و فرکانس و سپس رزولوشن دوربین.
   - نمایش یک اعلان کوتاه و ملایم (Toast / Badge) به کاربر و ذخیره پروفایل پایدار کشف‌شده برای اسکن‌های بعدی آن گوشی.

---

## ۲. مشخصات پروفایل‌های کیفیت اسکنر

| ویژگی / پروفایل | 🔋 سبک (Lite) | ⚖️ متعادل (Balanced) | 💎 بالا (High) | 🎯 فوق‌العاده (Ultra HD - فعلی) | ⚡ هوشمند (Adaptive) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **رزولوشن تصویر** | `640×480` | `1280×720` | `1920×1080` | `1920×1080` تا `2560×1440` | پویا (شروع از Ultra) |
| **ابعاد کانواس هدف (ROI)** | `400×400` px | `600×600` px | `700×700` px | `850×850` px | پویا بر اساس FPS |
| **فاصله زمانی اسکن** | ۱۵۰ میلی‌ثانیه | ۱۰۰ میلی‌ثانیه | ۸۰ میلی‌ثانیه | ۶۰ میلی‌ثانیه | تنظیم خودکار |
| **موتور `TRY_HARDER`** | غیرفعال | غیرفعال | فعال (فقط QR) | فعال (همه فرمت‌ها) | پویا بر اساس لود پردازنده |
| **هدف کاربری** | گوشی‌های قدیمی/ضعیف | مصرف کم باتری | گوشی‌های میان‌رده | بارکدهای ریز و متراکم | تمام دستگاه‌ها (توصیه‌شده) |

---

## ۳. تغییرات پیشنهادی در فایل‌ها

### الف) لایه بک‌اند (Django)
#### [MODIFY] [warehouses/services.py](file:///e:/warehouse%20project/warehouse-backend/warehouses/services.py)
- افزودن کلیدهای پیش‌فرض تنظیمات اسکنر به `DEFAULT_SETTINGS`:
  - `'scanner_camera_preset': 'adaptive'`
  - `'scanner_custom_resolution': '1080p'`
  - `'scanner_custom_interval_ms': 60`
  - `'scanner_custom_roi_size': 850`
  - `'scanner_custom_try_harder': True`

### ب) لایه کامپوننت اسکنر بارکد (Frontend Shared)
#### [MODIFY] [barcode-scanner.component.ts](file:///e:/warehouse%20project/warehouse-front/src/app/shared/components/barcode-scanner/barcode-scanner.component.ts)
- پیاده‌سازی مدل‌های `ScannerQualityPreset` و پیکربندی‌های متناظر.
- بارگذاری تنظیم پیش‌فرض از تنظیمات انبار/سیستم و خواندن اورراید محلی از `localStorage`.
- افزودن مکانیزم ناظر عملکرد (Performance Observer) داخل `startPrecisionScanLoop`:
  - محاسبه میانگین زمان پردازش فریم‌ها و تعداد فریم‌های موفق بر ثانیه.
  - اجرای تابع `triggerAdaptiveDowngrade()` در صورت تشخیص لگ بدون متوقف شدن ویدیو.
- طراحی منوی انتخاب کیفیت سریع در هدر دوربین (دکمه با آیکون کیفیت و نشانگر وضعیت فعال).
- افزودن گزینه «پیش‌فرض سرور / سیستم» با قابلیت ریست به تنظیمات دیتابیس.
- نمایش اعلان ملایم `⚡ کیفیت اسکنر بهینه شد` هنگام سوییچ خودکار.

### ج) لایه صفحات تنظیمات سیستم و انبار
#### [MODIFY] [settings.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/settings/settings.ts) و [settings.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/settings/settings.html)
- افزودن بخش کارت "تنظیمات کیفیت و بهینه‌سازی دوربین اسکنر" در تب قوانین عملیاتی (`operations`).
- پشتیبانی از انتخابگر پروفایل‌های ۴گانه + هوشمند + بخش تفصیلی تنظیمات پیشرفته (رزولوشن، فرکانس، کراپ و TRY_HARDER).
- ذخیره کلیدهای جدید در آبجکت `settings`.

#### [MODIFY] [wh-settings.ts](file:///e:/warehouse%20project/warehouse-front/src/app/components/wh-settings/wh-settings.ts) و [wh-settings.html](file:///e:/warehouse%20project/warehouse-front/src/app/components/wh-settings/wh-settings.html)
- افزودن قابلیت اختصاصی‌سازی تنظیمات کیفیت دوربین برای هر انبار به همراه دکمه بازنشانی اختصاصی (`resetSetting`).

---

## ۴. برنامه فازبندی اجرا (Task Breakdown)

1. **فاز ۱ (Backend & Data Contracts):** تعریف کلیدهای تنظیمات دوربین در `services.py` و سرویس کش تنظیمات.
2. **فاز ۲ (Scanner Core & Presets Engine):** پیاده‌سازی کنترلر پروفایل‌های کیفیت و کانستریت‌های متغیر در `barcode-scanner.component.ts`.
3. **فاز ۳ (Adaptive Auto-Degrade Logic):** پیاده‌سازی حلقه پایش تاخیر و لگ فریم‌ها و تابع سوییچ روان بدون قطعی استریم.
4. **فاز ۴ (Camera Overlay UI & Local Persistence):** افزودن دکمه و منوی انتخابگر کیفیت در هدر دوربین به همراه گزینه بازگشت به پیش‌فرض دیتابیس و ذخیره در `localStorage`.
5. **فاز ۵ (Settings Pages Integration):** افزودن رابط کاربری انتخاب پروفایل و تنظیمات پیشرفته در `settings` و `wh-settings`.
6. **فاز ۶ (Verification & Testing):** راستی‌آزمایی باز شدن دوربین در تمام پروفایل‌ها، تست شبیه‌سازی لگ و سوییچ خودکار، و تست ذخیره در دیتابیس و لوکال استوریج.

---

## ۵. طرح راستی‌آزمایی (Verification Plan)

### تست‌های دستی و سناریوهای آزمون
1. **تست انتخاب پروفایل Ultra HD:** اطمینان از اعمال رزولوشن 1080p-2K، کراپ ۸۵۰ و فرکانس ۶۰ms و خوانده شدن سریع بارکدهای ریز DataMatrix.
2. **تست پروفایل‌های Balanced و Lite:** بررسی کاهش مصرف CPU و تغییر رزولوشن ویدیو در پنل دیباگ HUD.
3. **تست سوییچ خودکار (Adaptive Fallback):** شبیه‌سازی افت فریم و اطمینان از سوییچ پله‌ای به پروفایل پایین‌تر و نمایش نوتیفیکیشن.
4. **تست اولویت دیتابیس و حافظه گوشی:** تغییر تنظیم در انبار -> بررسی اعمال در گوشی؛ سپس تغییر دستی در گوشی -> اطمینان از ذخیره محلی؛ و در نهایت زدن دکمه «بازگشت به پیش‌فرض سیستم» و بازگشت به تنظیمات دیتابیس.

</div>
