# چک‌لیست جامع و مرجع پایش پیشرفت — پایدارسازی و اصلاح صفحه تنظیمات (Settings Hardening)

این سند چک‌لیست مرکزی پیشرفت اصلاحات صفحه تنظیمات، ماژول بکاپ، کنترل دسترسی فیلدها و رفتار آفلاین سیستم است.

---

## 🗺️ ساختار بسته‌های اجرایی (۴ بسته)

```
E:\warehouse project\Documents\SettingsProblem\
├── implementation_plan.md                                       ◄── طرح جامع فنی و معماری
├── task_settings_hardening.md                                   ◄── چک‌لیست کلی (این سند)
├── package_1_database_and_backup_armor.md                       ◄── راهنمای بسته ۱ (ایمن‌سازی دیتابیس و بکاپ)
├── package_2_offline_pwa_and_data_integrity.md                  ◄── راهنمای بسته ۲ (آفلاین، PWA و حفاظت از داده)
├── package_3_permissions_and_dead_code.md                       ◄── راهنمای بسته ۳ (مجوزها، اعتبارسنجی و اسکنر)
└── package_4_accessibility_performance_and_modularization.md    ◄── راهنمای بسته ۴ (دسترس‌پذیری، کارایی و ماژولارکردن)
```

---

## 📦 بسته ۱: زیرساخت تست و ایمن‌سازی دیتابیس و بکاپ (Database & Backup Armor)
> **فازهای پوشش‌داده:** فاز ۰ (زیرساخت تست) و فاز ۱ (ایمن‌سازی بازیابی دیتابیس)  
> **ایرادات هدف:** ۲-۳، ۲-۴، ۱-۶، ۱-۷، ۱-۸، ۱-۹  
> **سند راهنما:** `package_1_database_and_backup_armor.md`

- [x] **فاز ۰ (زیرساخت تست‌های پایه):**
  - [x] ساخت `warehouse-backend/warehouses/tests_settings.py` و Mockهای پایه
  - [x] ساخت `warehouse-backend/config/tests_backup.py` با شبیه‌سازی امن Mock برای `pg_dump`/`pg_restore`
  - [x] اصلاح و سبزسازی `settings.spec.ts` در فرانت با Providerهای تست
- [x] **فاز ۱ (ایمن‌سازی بازیابی و بکاپ):**
  - [x] اصلاح باگ حذف فایل نجات در پایتون ۳ با فلگ صریح `restore_failed` (ایراد ۲-۳)
  - [x] تفکیک حالت‌های رول‌بک و ارسال وضعیت واقعی `rollback_state` به اپراتور (ایراد ۲-۴)
  - [x] الزام متن تأیید متنی صریح `RESTORE_DATABASE_CONFIRM` در فرانت و بک‌اند (ایراد ۱-۶)
  - [x] ثبت رکوردهای `AuditLog` سطح Critical و Warning برای ایجاد و بازیابی بکاپ (ایراد ۱-۷)
  - [x] اعمال سقف حجم آپلود `BACKUP_MAX_UPLOAD_MB` پیش از `.read()` در رم (ایراد ۱-۹)
  - [x] اعتبارسنجی حداقل طول رمز بکاپ (حداقل ۱۲ کاراکتر) در ساخت بکاپ (ایراد ۱-۸)
  - [x] افزودن فیلد `requireText` به `ConfirmDialogComponent` جهت پشتیبانی از تأیید متنی (ایراد ۱-۶، ۵-۷)

---

## 📦 بسته ۲: یکپارچگی داده، آفلاین PWA و گارد خروج (Offline & Data Integrity)
> **فازهای پوشش‌داده:** فاز ۲ (آفلاین و PWA) و فاز ۳ (محافظت از داده در ذخیره)  
> **ایرادات هدف:** ۳-۱ تا ۳-۵، ۲-۱، ۲-۲، ۲-۵، ۲-۶، ۲-۷  
> **سند راهنما:** `package_2_offline_pwa_and_data_integrity.md`

- [ ] **فاز ۲ (رفتار آفلاین و کش PWA):**
  - [ ] افزودن توکن `SKIP_OFFLINE` به `saveGlobalSettings`، `downloadBackup` و `restoreBackup` (ایرادات ۳-۱، ۳-۲، ۳-۳)
  - [ ] ساخت تابع کمکی `clearServerDerivedCaches()` در `offline-db.ts` و فراخوانی در ریستور موفق (ایراد ۳-۴)
  - [ ] بررسی خالی بودن `syncQueue` پیش از باز کردن دیالوگ بازیابی دیتابیس (حفاظت داده کاربر)
  - [ ] صفر کردن `isBackupLoading` در `finalize()` و پاکسازی کش تنظمیات در خروج (ایرادات ۳-۱، ۳-۵)
- [ ] **فاز ۳ (حفاظت داده در ذخیره‌سازی):**
  - [ ] مسدودسازی دکمه ذخیره در صورت شکست واکشی فیلدهای پویا (`dyn_*`) با بنر هشدار (ایراد ۲-۱)
  - [ ] نگه‌داری نقشه خام کلیدها و جلوگیری از حذف ناخواسته کلیدهای ناشناس در ذخیره (ایراد ۲-۲)
  - [ ] ارسال دلتای کلیدهای تغییریافته (`Sparse Update`) به جای کل آبجکت (ایرادات ۲-۵، ۲-۷)
  - [ ] پیاده‌سازی گارد خروج `settingsLeaveGuard` برای جلوگیری از خروج با فرم کثیف (ایراد ۲-۶)

---

## 📦 بسته ۳: امنیت، کنترل دسترسی، اعتبارسنجی و اسکنر (Permissions & Scanner)
> **فازهای پوشش‌داده:** فاز ۴ (مجوزها و اعتبارسنجی)، فاز ۵ (اعمال سمت سرور مجوز فیلدها) و فاز ۶ (تنظیمات اسکنر و کدهای مرده)  
> **ایرادات هدف:** ۱-۱، ۱-۲، ۱-۳، ۱-۴، ۱-۵، ۴، ۴-۱، ۴-۲  
> **سند راهنما:** `package_3_permissions_and_dead_code.md`

- [ ] **فاز ۴ (مجوزها و اعتبارسنجی ورودی):**
  - [ ] گیت‌کردن دکمه ذخیره با مجوز `perm_sys_settings` و نمایش بنر فقط‌خواندنی (ایراد ۱-۱)
  - [ ] استخراج و نمایش پیام دقیق خطای سرور از `err?.error?.error` (ایراد ۱-۲)
  - [ ] محدودسازی دسترسی `GET /api/settings/global/` به نقشه فیلدهای لازم برای کاربران عادی (ایراد ۱-۳)
  - [ ] اعتبارسنجی سخت‌گیرانه کلیدها و تایپ‌ها با Serializer در `POST /api/settings/global/` (ایراد ۱-۴)
- [ ] **فاز ۵ (اعمال سمت سرور مجوز فیلدهای کالا):**
  - [ ] پیاده‌سازی متد `get_editable_item_fields(role, warehouse_id)` در `warehouses/services.py`
  - [ ] اعمال کنترل دسترسی فیلدها در `ItemViewSet.partial_update` در `inventory/views.py` و رد درخواست با ۴۰۰ (ایراد ۱-۵)
  - [ ] ثبت `AuditLog` برای تلاش‌های غیرمجاز ویرایش فیلدهای قفل‌شده
- [ ] **فاز ۶ (منطق اسکنر و پاکسازی کدهای مرده):**
  - [ ] ذخیره کاراکترهای اسکی واقعی (`\x1E`, `\t`) و تابع سازگاری `decodeDelimiter` (ایراد ۴-۱)
  - [ ] بایند کردن `@Input() serverPreset` در تمام کامپوننت‌های `<app-barcode-scanner>` (ایراد ۴)
  - [ ] حذف `<select>` منسوخ `manager_approval_mode` از تمپلیت فرانت (ایراد ۴)
  - [ ] تمیزکاری سوئیچ‌های چت و حذف شکنندگی `!== false` (ایراد ۴-۲)

---

## 📦 بسته ۴: دسترس‌پذیری، کارایی، تایپ‌استریکت و ماژولارسازی (UX & Refactoring)
> **فازهای پوشش‌داده:** فاز ۷ (دسترس‌پذیری و UX) و فاز ۸ (کیفیت کد و تفکیک قالب)  
> **ایرادات هدف:** ۵-۱ تا ۵-۱۳  
> **سند راهنما:** `package_4_accessibility_performance_and_modularization.md`

- [ ] **فاز ۷ (دسترس‌پذیری و ارتقای UX):**
  - [ ] ساخت کامپوننت مشترک `<app-toggle-switch>` با پشتیبانی کیبورد (Space/Enter) و ARIA معتبر (ایرادات ۵-۱، ۵-۲)
  - [ ] اصلاح رفتار یا برچسب دکمه‌های «نمایش همه / مخفی‌سازی همه» (ایراد ۵-۳)
  - [ ] افزودن دیالوگ تأییدیه قبل از بازنشانی کلی فیلدها (ایراد ۵-۴)
  - [ ] اصلاح دانلود بلاب و تعویق `revokeObjectURL` در فایرفاکس (ایراد ۵-۵)
  - [ ] مدیریت امن فیلدهای پسورد و افزودن `autocomplete="new-password"` (ایراد ۵-۶)
  - [ ] تکمیل استانداردهای Focus Trap و Escape در مودال‌ها (ایراد ۵-۷)
- [ ] **فاز ۸ (کارایی، تایپ‌استریکت و تفکیک کامپوننت):**
  - [ ] مدیریت اشتراک با `takeUntilDestroyed()` و حذف `detectChanges` در `ngOnInit` (ایراد ۵-۸)
  - [ ] تعریف اینترفیس سخت‌گیر `SystemSettingsConfig` و حذف `settings: any` (ایراد ۵-۹)
  - [ ] تبدیل گترهای فیلتر جدول به سیگنال‌های محاسباتی `computed()` (ایراد ۵-۱۰)
  - [ ] پاکسازی کدهای مرده و حذف رفرنس به `settings.css` خالی (ایراد ۵-۱۱)
  - [ ] تفکیک قالب ۱۰۱۹ خطی `settings.html` به ۵ زیرکامپوننت بر اساس تب‌ها زیر سقف ۵۰۰ خط (ایراد ۵-۱۳)
  - [ ] تکمیل تست‌های نهایی و اطمینان از بیلد سبز فرانت و بک‌اند (ایراد ۵-۱۲)
