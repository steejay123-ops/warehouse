<div dir="rtl" align="right">

# تسک‌ها: قابلیت دور زدن سرپرست برای کالاهای خاص

- `[x]` **گام ۱: آماده‌سازی تسک‌ها**
  - `[x]` ساخت مستند Planning و اخذ تایید.
  - `[x]` ایجاد و ذخیره Task در `Documents`.
- `[x]` **گام ۲: تغییرات بک‌اند (مدل و پایگاه داده)**
  - `[x]` افزودن فیلد `skip_supervisor` به مدل `CountTask` در `models.py`.
  - `[x]` اجرای Migrationها.
- `[x]` **گام ۳: تغییرات بک‌اند (نماها - Views)**
  - `[x]` تغییر در تابع `bulk_assign` برای دریافت و ثبت `'skip'`.
  - `[x]` تغییر در تابع `bulk_submit` برای پردازش حلقه تسک‌ها و استفاده از `bulk_update`.
- `[x]` **گام ۴: تغییرات فرانت‌اند (HTML و TS)**
  - `[x]` حذف `*ngIf="requireSupervisor"` و ایجاد دو `<option>` برای استخر و بدون سرپرست در `dispatch.html`.
  - `[x]` تنظیم اولیه متغیر در `dispatch.ts` و تغییر پیام تایید.
- `[/]` **گام ۵: تاییدیه نهایی**
  - `[x]` اطمینان از صحت کامپایل کدها در ترمینال.
  - `[ ]` تولید فایل Walkthrough برای مشاهده کاربر.

</div>
