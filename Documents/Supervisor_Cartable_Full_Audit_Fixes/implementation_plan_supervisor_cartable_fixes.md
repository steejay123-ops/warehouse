# [طرح جامع اصلاح ۸ ایراد ساختاری و منطقی کارتابل سرپرست]

<div dir="rtl" align="right">

## مقدمه و هدف
این سند راهنمای گام‌به‌گام و تفصیلی جهت رفع هشت نقص عملکردی شناسایی‌شده در جریان ارزیابی چندعاملی (Multi-Agent Audit) صفحه کارتابل سرپرست ([`SupervisorDashboard`](file:///e:/warehouse%20project/warehouse-front/src/app/components/supervisor/supervisor-dashboard/supervisor-dashboard.ts)) می‌باشد.

---

## جدول تفصیلی ایرادات و راه‌حل‌های مهندسی

| ردیف | عنوان نقص | ریشه بروز مشکل | راه‌حل مهندسی پیاده‌سازی |
| :--- | :--- | :--- | :--- |
| **۱** | فراخوانی هاردکدشدهٔ `loadTasks()` در رفرش و سلکتور انبار | در `supervisor-dashboard.html` خطوط ۱۲ و ۲۶ مستقیماً `loadTasks()` فراخوانی شده که فقط تب کارهای من را لود می‌کند. | جایگزینی با متد هوشمند [`refreshCurrentTab()`](file:///e:/warehouse%20project/warehouse-front/src/app/components/supervisor/supervisor-dashboard/supervisor-dashboard.ts#L294) تا تب فعال کاربر بازخوانی شود. |
| **۲** | نشت تسک‌های بدون سرپرستِ استخر به تب کارهای من در وب‌سوکت | شرط `(!taskData.supervisor && this.currentTab === 'my-tasks')` در خط ۳۴۰ و ۳۹۱ تسک‌های استخر را در تب فعال تزریق می‌کرد. | تفکیک قاطع `isMyTask` و `isPool` و حذف شرط نشت داده. |
| **۳** | فیلتر ناصحیح و مفقود شدن اسناد مالی ردشده مدیر | خطوط ۳۷۵ و ۷۷۵ در `supervisor-dashboard.ts` فقط وضعیت `DOC_PROCESSED` را دریافت می‌کردند و `DOC_MANAGER_REJECTED` را حذف می‌نمودند. | اضافه کردن وضعیت `DOC_MANAGER_REJECTED` به شروط دریافت داده و فیلتر وب‌سوکت و نمایش بج اختصاصی در تمپلیت. |
| **۴** | خنثی شدن هوش استعلام زنده SWR پس‌زمینه | در اینترسپتور آفلاین مقدار `req.url` بدون کوئری‌پارامتر پاس داده می‌شد و شرط `as_role=supervisor` هرگز برقرار نمی‌شد. | ارسال `req.urlWithParams` در اینترسپتور و تطابق منعطف آدرس در لیسنر کامپوننت. |
| **۵** | بلاک کد ناتمام و عدم پشتیبانی اکسپورت در تب استخر | خطوط ۱۰۲۴-۱۰۳۰ شامل کامنت‌های خام و عدم تنظیم صحیح پارامترهای استخر برای اندپوینت اکسل بود. | پاک‌سازی کامل کدهای مازاد و ارسال پارامتر متناسب با تب جاری جهت اکسپورت دقیق تسک‌های استخر. |
| **۶** | عدم پوشش کلید Escape برای مدال جزئیات سند و تداخل کلید A | لیسنر کیبورد در خطوط ۱۵۲ و ۲۰۱ مدال `selectedDocDetailTask` و تفاوت عملیات تب‌های استخر را مدیریت نمی‌کرد. | افزودن هندلر Escape برای مدال جزئیات سند و انطباق شورت‌کات `A` با عملیات تب جاری. |
| **۷** | عدم وجود تایم‌لاین سوابق برای اسناد و عنوان انگلیسی وضعیت‌ها | در کارت‌های اسناد مالی دکمه یا مدال سوابق وجود نداشت و فیلتر `titlecase` نام‌های انگلیسی خام با آندرلاین تولید می‌کرد. | اضافه کردن متد ترجمه اختصاصی `getActionTypeLabel()` و نمایش سوابق در مدال جزئیات سند مالی. |
| **۸** | پنهان ماندن بج‌های شمارنده تا زمان کلیک روی تب | آرایه‌های `poolTasks` و `docTasks` در بدو ورود کاربر لود نمی‌شدند و بج‌ها عدد صفر را نشان می‌دادند. | پیاده‌سازی متد `preloadTabCounts()` در پس‌زمینه با بار محاسباتی سبک بدون بلاک کردن UI. |

---

## فازبندی اجرا (Execution Phases)

### فاز ۱: تصحیح منطق داده‌ای، سلکتور انبار و وب‌سوکت
- اصلاح ارجاع رویداد تغییر انبار و دکمه رفرش به `refreshCurrentTab()`.
- بازنویسی متدهای `updateCountTaskInPlace` و `updateDocTaskInPlace` جهت جلوگیری قطعی از نشت تسک‌های استخر به کارهای من.
- هماهنگ‌سازی اینترسپتور آفلاین و اشتراک SWR.

### فاز ۲: پشتیبانی کامل از گردش کار اسناد مالی ردشده
- اضافه کردن وضعیت `DOC_MANAGER_REJECTED` به `loadDocTasks` و `updateDocTaskInPlace`.
- به‌روزرسانی کارت‌های سند و تمپلیت HTML جهت نمایش بج قرمز، علت رد مدیر و یادداشت‌ها.

### فاز ۳: بهینه‌سازی اکسپورت، تعاملات کیبورد و تایم‌لاین سوابق
- تکمیل متد `executeExport()` برای تب‌های استخر.
- تکمیل رویدادهای کیبورد (Escape و کلید A).
- ایجاد متد ترجمه فارسی اکشن‌ها (`getActionTypeLabel`) و نمایش سوابق اسناد.
- راه‌اندازی متد `preloadTabCounts()`.

---

## بررسی اثرات جانبی و سناریوهای مرزی (Edge Cases & Safety)

1. **تداخل‌های چندکاربره:** با رفع باگ وب‌سوکت، اگر کاربری در انبار دیگری تسکی را ثبت کند یا کاربری تسکی را به عهده بگیرد، تسک به درستی از استخر حذف شده و در صورت عدم انتساب به سرپرست فعلی وارد کارهای او نمی‌شود.
2. **پایداری در حالت آفلاین:** متد `refreshCurrentTab(false, true)` استیت‌های انتخابی کاربر (`selectedTasks`) را در هنگام به‌روزرسانی‌های پس‌زمینه حفظ می‌کند.

</div>
