<div dir="rtl" align="right">

# لیست وظایف (Task List): فیلتر با کلیک روی تگ‌ها

- `[ ]` **فایل `dispatch.html`**
  - `[ ]` افزودن قابلیت کلیک و ظاهر دکمه‌ای به تگ‌های موجود در جدول.
  - `[ ]` افزودن ویژگی `[filters]="state.appState.dispatchSettings.filters"` به کامپوننت جدول.

- `[ ]` **فایل `dispatch.ts`**
  - `[ ]` پیاده‌سازی متد `filterByTag` جهت ذخیره فیلتر و رفرش شدن داده‌ها.

- `[ ]` **تست و بررسی**
  - `[ ]` بررسی صحت اعمال فیلتر و به‌روزرسانی جدول پس از کلیک.

</div>
