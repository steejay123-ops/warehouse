<div dir="rtl" align="right">

# لیست وظایف: بهبودهای ظاهری سیستم انبار

- `[ ]` **۱. منوی Sidebar قابل باز/بسته شدن در دسکتاپ**
  - `[ ]` اضافه کردن متغیر وضعیت و ذخیره در localStorage در `layout.ts`
  - `[ ]` اضافه کردن دکمه Toggle و کلاس‌های شرطی در `layout.html`
  - `[ ]` تنظیم استایل‌ها و انیمیشن‌ها در `layout.css`

- `[ ]` **۲. نمایش داده واقعی شمارش کور در تخصیص کالا**
  - `[ ]` خواندن مقدار `blind_counting` از تنظیمات در `dispatch.ts`
  - `[ ]` به‌روزرسانی قالب `dispatch.html` برای نمایش وضعیت واقعی

- `[ ]` **۳. اعتبارسنجی نام سیستمی فیلد داینامیک**
  - `[ ]` اضافه کردن اعتبارسنجی Regex در `dynamic-fields.ts` (افزودن و ویرایش)
  - `[ ]` اضافه کردن پترن و پیغام خطا در `dynamic-fields.html`

- `[ ]` **۴. جایگزینی `confirm()` در حذف فیلد داینامیک**
  - `[ ]` تزریق و استفاده از `ConfirmDialogService` در `dynamic-fields.ts`

- `[ ]` **۵. اصلاح عنوان صفحه تنظیمات انبار**
  - `[ ]` افزودن عنوان مناسب در `layout.ts`

- `[ ]` **۶. پیشنهادات ظاهری اضافی**
  - `[ ]` اضافه کردن آیکون `activity` در `layout.ts`
  - `[ ]` تکمیل عناوین صفحات در `layout.ts`
  - `[ ]` نمایش داده واقعی شمارش کور در `field.html` و `field.ts`

- `[ ]` **۷. بررسی و تأیید نهایی**
  - `[ ]` اطمینان از عملکرد صحیح تمامی موارد پیاده‌سازی شده

</div>
