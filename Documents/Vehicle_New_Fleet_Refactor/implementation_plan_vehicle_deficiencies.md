<div dir="rtl" align="right">

# طرح جامع تکمیل نواقص و بازطراحی ناوگان جدید (Vehicle New Fleet Deficiencies Remedial Plan)
## گزارش ممیزی تطبیقی کد با سند `implementation_plan_vehicle_refactor.md` و برنامه اقدام اصلاحی

> [!IMPORTANT]
> **وضعیت سند:** تدوین طرح تکمیلی و برنامه اقدام اصلاحی (Deficiencies Remedial Plan).
> این سند پس از ممیزی عمیق و خط به خط کلیه تغییرات کد در بک‌اند، فرانت‌اند و تست‌ها در مقایسه با جدول ۲۴ گانه سند مرجع تدوین شده است و اقدامات تکمیلی لازم جهت دستیابی به تطابق ۱۰۰٪ را ترسیم می‌نماید.

---

## ۱. خلاصه نتیجه ممیزی تطبیقی (Executive Audit Summary)

بررسی دقیق مخزن کد نشان می‌دهد که بخش‌های بنیادین ساختاری و امنیتی طرح اولیه با موفقیت پیاده‌سازی شده‌اند؛ از جمله:
- **رفع باگ کشنده تنزل رتبه خودرو در تایید مدیر** با استفاده از `excluded_fields` در متد `approve_manager`.
- **اعمال گارد قلمرو و مقابله با BOLA/IDOR** با فیلتر `UserSectionAssignment` در `get_queryset` و `perform_create`.
- **حفاظت در برابر حذف آبشاری فیزیکی (Cascade Delete)** در متد `destroy` با بررسی `VehicleTripLog`.
- **سیستم ورود دسته‌جمعی از اکسل (Excel Import Modal)** با پشتیبانی از dry-run و قالب استاندارد ۲ ردیفه.
- **ویجت ۴ تکه پلاک ملی استاندارد ایران** به همراه پیش‌نمایش گرافیکی پلاک ملی.
- **فرمت‌بندی پویا و سه رقمی نرخ پایه سرویس** و محاسبه زنده معادل تومان با حفظ موقعیت کرسر.
- **اعتبارسنجی الگوریتم ۱۰ رقمی کد ملی (Mod 11)** و ساختار شماره شبا با استخراج خودکار نام بانک و شماره حساب.
- **همگام‌سازی بلادرنگ وب‌سوکت** و بازخوانی خودکار اطلاعات.

با این وجود، ارزیابی نشان داد که پروژه در وضعیت فعلی **حدود ۸۰٪ منطبق** است و **۹ نقص ساختاری و عملکردی مشخص** نسبت به الزامات سند طرح اولیه و الگوی مرجع `employee-new-personnel` دارد که در ادامه به تفصیل آمده است.

---

## ۲. جدول ممیزی تفصیلی ۲۴ بند طرح اولیه (Audit Verification Matrix)

| ردیف | موضوع و محور سند اولیه | وضعیت در کد فعلی | انطباق | شرح نواقص و شکاف‌های کشف‌شده |
| :---: | :--- | :---: | :---: | :--- |
| **۱** | **نحوه نمایش فرم تعریف** | پیاده‌سازی شده اما فاقد Bottom Sheet موبایل | ⚠️ ناقص | مودال در موبایل به پایین صفحه متصل نمی‌شود (`items-end` وجود ندارد) و با کلیک روی پس‌زمینه (Backdrop Click) بسته نمی‌شود. |
| **۲** | **تفکیک پیش‌نویس و ارسال به سرپرست** | پیاده‌سازی شده | ✅ کامل | دو دکمه در مودال و دکمه موشک در سطر پیش‌نویس جدول فعال است. |
| **۳** | **امکان ویرایش رکوردهای مصوب** | پیاده‌سازی شده اما مودال Diff ناقص است | ⚠️ ناقص | مودال مشاهده تغییرات معلق مقادیر قبلی (`previous_values`) را نمایش نمی‌دهد، کلیدها به فارسی ترجمه نشده‌اند و فیلد ثبت علت تغییرات وجود ندارد. |
| **۴** | **باگ تنزل رتبه خودرو در تایید مدیر** | رفع شده با `excluded_fields` | ✅ کامل | تست بک‌اند عدم تنزل رتبه به تصویب رسیده و فعال است. |
| **۵** | **حفره امنیتی قلمرو پروژه/بخش (BOLA)** | محدودسازی بر مبنای `UserSectionAssignment` | ✅ کامل | در `get_queryset` و `perform_create` فیلتر قلمرو اعمال شده است. |
| **۶** | **ریسک حذف فیزیکی (Cascade Delete)** | تبدیل به آرشیو نرم در صورت وجود تردد | ✅ کامل | در صورت وجود `VehicleTripLog` به `is_active = False` تبدیل می‌شود. |
| **۷** | **همگام‌سازی بلادرنگ وب‌سوکت** | متد `broadcast_vehicle_update` فعال است | ✅ کامل | در کلیه اکشن‌های کارتابل وب‌سوکت ارسال و فرانت‌اند دریافت می‌کند. |
| **۸** | **گردش کار موارد نیازمند بازنگری** | تب وجود دارد اما شرط حذف دارد | ⚠️ ناقص | تب عودت در صورت ۰ بودن مخفی می‌شود؛ دکمه ارسال مجدد در مودال و اکشن سطر جدول تفکیک نشده است. |
| **۹** | **ایمپورت اکسل ناوگان** | مودال و اندپوینت بک‌اند پیاده شده | ✅ کامل | دانلود تمپلیت ۲ ردیفه و درون‌ریزی با dry-run با موفقیت کار می‌کند. |
| **۱۰** | **متدهای وب‌سرویس در فرانت‌اند** | کلیه متدها در `PersonnelApiService` اضافه شده | ✅ کامل | متدهای دانلود، ایمپورت، اکشن‌های کارتابل و تغییرات معلق موجود است. |
| **۱۱** | **تاخیر در جستجو (Debounce)** | پیاده‌سازی با RxJS Subject و تاخیر ۳۰۰ms | ✅ کامل | جستجوی بهینه و همگام با Query Params فعال است. |
| **۱۲** | **ارگونومی ورود شماره شبا** | پیش‌وند LTR، تشخیص خودکار بانک و کپی | ✅ کامل | ارگونومی LTR، فرمت‌بندی مونو و کپی سریع در دسترس است. |
| **۱۳** | **فرمت‌بندی پویای مبالغ (کرایه/نرخ)** | فرمت ۳ رقمی درجا با حفظ کرسر و تومان | ✅ کامل | عملکرد روان و تبدیل تومان فعال است. |
| **۱۴** | **پلاک‌خوان و قالب ۴ تکه استاندارد** | کنترلر ۴ بخشی و پیش‌نمایش گرافیکی | ✅ کامل | تجزیه، ترکیب و نمایش استاندارد پلاک ایران انجام شده است. |
| **۱۵** | **انتخاب سریع نوع خودرو و کاربری** | لیست ساده بدون گروه‌بندی | ⚠️ ناقص | فهرست کشویی فاقد گروه‌بندی (`optgroup`) است و دسته‌بندی تجهیزات انبار و لیفتراک لحاظ نشده است. |
| **۱۶** | **اعتبارسنجی کدملی مالک و راننده** | اعتبارسنجی Mod 11 فعال، تطابق پرسنل غایب | ⚠️ ناقص | الگوریتم ۱۰ رقمی پیاده شده، اما تطابق با پرسنل موجود و تفکیک کدملی مالک پیاده‌سازی نشده است. |
| **۱۷** | **هدر شیشه‌ای چسبان (Sticky Header)** | هدر شیشه‌ای فعال اما دکمه افزودن نامتقارن | ⚠️ ناقص | هدر شیشه‌ای و ۳ دکمه آیکونی مستقرند، اما دکمه چهارم هدر به جای دکمه آیکونی استاندارد (`w-9 h-9`) دکمه متنی عریض است. |
| **۱۸** | **تب‌های فیلتر با شمارنده پویا** | کپسول سگمنت با شمارنده‌ها پیاده شده | ⚠️ ناقص | تب‌های «نیازمند اصلاح» و «رد شده» دارای `*ngIf` هستند و در شمارنده صفر ناپدید می‌شوند. |
| **۱۹** | **همگام‌سازی فیلترها با URL** | اتصال تب و سرچ به Query Params | ✅ کامل | پارامترهای `section_id`، `status_filter` و `search` در آدرس مرورگر سینک هستند. |
| **۲۰** | **کارت‌های ریسپانسیو موبایل** | نمای کارتی برای زیر ۷۶۸ پیکسل | ✅ کامل | در موبایل کارت‌های بدون اسکرول افقی با اکشن‌های سریع فعال است. |
| **۲۱** | **تفکیک مالک و راننده در پرونده ناوگان** | **کاملاً نادیده گرفته شده است** | ❌ اجرا نشده | فیلدهای مالک (نام، کدملی، شماره تماس، وضعیت مالکیت) در مدل و فرانت‌اند وجود ندارد و با تفکیک شبا جایگزین شده بود. |
| **۲۲** | **بج‌های وضعیت دوحالته (Toggleable)** | کلیک مجدد وضعیت را لغو نمی‌کند | ❌ اجرا نشده | کلیک مجدد روی تب فعال وضعیت را به 'all' ریست نمی‌کند و منطق تاگل Rule 7 غایب است. |
| **۲۳** | **تست‌های واحد کامپوننت و DOM** | ۲۳ تست Vitest و ۶ تست جنگو | ✅ کامل | تست‌های جامع نوشته شده و پاس هستند. |
| **۲۴** | **کنترل دسترسی نقشی (RBAC) در اکشن‌ها** | عدم وجود حالت Read-Only برای رکوردهای معلق | ⚠️ ناقص | رکوردهای در حال بررسی کارتابل دکمه «ویرایش» دارند و مودال ویرایش‌پذیر باز می‌شود که باعث خطای ۴۰۰ می‌گردد. |

---

## ۳. فهرست جامع ۹ نقص کشف‌شده (Deficiencies Analysis)

### نقص ۱: فقدان تفکیک مالک و راننده در مدل و فرم (نقض بند ۲۱ و ۱۶)
- **شرح مشکل:** در بسیاری از ناوگان استیجاری شرکت، مالک خودرو فردی غیر از راننده است (مثلاً خودرو به نام پدر، همسر یا شرکت ثالث است اما راننده دیگری کار می‌کند). عدم ثبت مجزای مشخصات مالک و واریز کرایه به حساب راننده باعث مشکلات جدی حقوقی، مالیاتی و عدم انطباق با دیسکت پایا می‌شود. در طرح اولیه بند ۲۱ مشخصاً خواستار تفکیک مشخصات هویتی/بانکی مالک با چک‌باکس «مالک شخص راننده است» شده بود اما در اجرا کاملاً حذف گردید.
- **راهکار اصلاحی:**
  1. افزودن فیلدهای `is_driver_owner` (پیش‌فرض True)، `owner_name`، `owner_national_code` و `owner_phone` به مدل `VehicleDriverProfile`.
  2. ایجاد مایگریشن در جنگو.
  3. به‌روزرسانی سریالایزر و اعتبارسنجی کدملی ۱۰ رقمی مالک.
  4. افزودن چک‌باکس تعاملی در مودال ثبت خودرو: در صورت برداشتن تیک، بخش مشخصات هویتی و بانکی مالک ظاهر شود.

### نقص ۲: نبود حالت فقط‌خواندنی (`isReadOnlyMode`) و باز بودن دکمه ویرایش برای رکوردهای در حال بررسی (نقض بند ۲۴ و ۳)
- **شرح مشکل:** در صفحه `employee-new-personnel`، رکوردهایی که در وضعیت‌های `pending_supervisor`، `pending_accountant`، `pending_manager` یا `rejected` هستند، دکمه آیکونی مشاهده (`👁️ مشاهده پرونده`) دارند و مودال به صورت کاملاً غیرفعال (`[disabled]="isReadOnlyMode"`) فقط برای مشاهده باز می‌شود. اما در `employee-new-vehicle`، دکمه «✏️ ویرایش» برای همه سطرها فعال است! با کلیک روی آن، مودال با اینپوت‌های فعال باز می‌شود و فشردن دکمه ثبت منجر به خطای ۴۰۰ بک‌اند می‌گردد.
- **راهکار اصلاحی:**
  1. پیاده‌سازی گترهای `isReadOnlyMode`، `modalHeaderTitle` و `modalHeaderBadge` در کامپوننت.
  2. در صورت `isReadOnlyMode`، تمامی کنترل‌های فرم مودال باید `[disabled]="isReadOnlyMode"` شوند و در فوتر فقط دکمه «بستن» نمایش یابد.
  3. در جدول و کارت‌های موبایل، برای رکوردهای در حال بررسی آیکون مشاهده (`👁️`) نمایش یابد.

### نقص ۳: عدم پیاده‌سازی رفتار دوحالته (Toggle) در انتخاب تب‌های وضعیت (نقض بند ۲۲)
- **شرح مشکل:** مطابق بند ۲۲، کاربر باید بتواند با کلیک مجدد روی تب فعال وضعیت، فیلتر را لغو کرده و به حالت 'all' بازگردد. در حال حاضر تابع `setStatusFilter(filter)` مقدار فیلتر را مستقیماً ست می‌کند و کلیک مجدد هیچ تاثیری ندارد.
- **راهکار اصلاحی:**
  اصلاح تابع `setStatusFilter`:
  ```typescript
  setStatusFilter(filter: VehicleStatusFilter): void {
    const nextFilter: VehicleStatusFilter = (this.statusFilter === filter && filter !== 'all') ? 'all' : filter;
    this.statusFilter = nextFilter;
    this.router.navigate([], {
      relativeTo: this.route,
      queryParams: { status_filter: nextFilter === 'all' ? null : nextFilter },
      queryParamsHandling: 'merge'
    });
  }
  ```

### نقص ۴: عدم رعایت استاندارد Bottom Sheet موبایل و فقدان خروج با Backdrop Click (نقض بند ۱)
- **شرح مشکل:** مودال فعلی در ابعاد موبایل از وسط صفحه شناور است و در نمایشگرهای کوچک اسکرول داخلی دشوار دارد. همچنین کلیک روی پس‌زمینه تیره (Backdrop) مودال را نمی‌بندد که موجب نارضایتی کاربری در گوشی‌های هوشمند می‌شود.
- **راهکار اصلاحی:**
  1. اصلاح کانتینر مودال طبق استاندارد `employee-new-personnel`:
     `class="fixed inset-0 z-50 flex items-end sm:items-center justify-center p-0 sm:p-4 bg-slate-900/60 backdrop-blur-xs overflow-hidden" (click)="closeNewVehicleModal()"`
  2. کادر داخلی:
     `class="bg-white rounded-t-3xl sm:rounded-3xl max-w-2xl w-full border border-slate-200 shadow-2xl overflow-hidden animate-in slide-in-from-bottom-6 sm:zoom-in-95 duration-200 flex flex-col max-h-[92vh] sm:max-h-[90vh]" (click)="$event.stopPropagation()"`
  3. اعمال همین استاندارد برای مودال مشاهده تغییرات معلق (Diff Modal).

### نقص ۵: نقص در مودال مقایسه تغییرات (Diff Viewer)؛ عدم نمایش مقادیر قبلی و عدم ترجمه کلیدها (نقض بند ۳)
- **شرح مشکل:** مودال مقایسه فعلی صرفاً مقادیر جدید `proposed_changes` را در یک ستون با نام انگلیسی ستون‌های پایگاه‌داده (مانند `default_service_rate`) نشان می‌دهد. مقدار قبلی (`previous_values`) با خط‌خورده نمایش داده نمی‌شود و مقایسه بصری دوطرفه شکل نگرفته است.
- **راهکار اصلاحی:**
  1. افزودن جدول نگاشت نام‌های فارسی فیلدها در کامپوننت:
     `{ driver_name: 'نام راننده', default_service_rate: 'نرخ پایه سرویس', sheba_number: 'شماره شبا', bank_name: 'نام بانک', ... }`
  2. پیاده‌سازی مقایسه دو ستونه: نمایش مقدار خط‌خورده قبلی، فلش `◀` و برچسب سبز مقدار جدید پیشنهادی، دقیقاً مشابه `employee-new-personnel`.

### نقص ۶: حذف شدن تب‌های «عودت» و «رد شده» هنگام صفر بودن و عدم تفکیک دکمه ارسال مجدد (نقض بند ۸ و ۱۸)
- **شرح مشکل:** تب‌های فیلتر با `*ngIf="vehicleMetrics.revision > 0"` مشروط شده‌اند. در نتیجه کاربر نمی‌داند چنین دسته‌بندی در سیستم وجود دارد و نمی‌تواند تب خالی را انتخاب کند. همچنین در زمان اصلاح خودروی عودت‌داده‌شده، برچسب دکمه ثبت همچنان «ثبت و ارسال به سرپرست کارگاه» است و تمایزی با ثبت اولیه ندارد.
- **راهکار اصلاحی:**
  1. حذف `*ngIf` از تب‌های `revision_required` و `rejected` و نمایش دائم با شمارنده (حتی صفر).
  2. اصلاح عنوان دکمه ثبت در صورت بازنگری به «اصلاح و ارسال مجدد به سرپرست».
  3. تعبیه دکمه اختصاصی اصلاح در سطر رکوردهای نیازمند بازنگری.

### نقص ۷: عدم گروه‌بندی فهرست انواع خودرو و فقدان تجهیزات انبارداری (نقض بند ۱۵)
- **شرح مشکل:** سلکتور انواع خودرو یک لیست فلت بدون دسته‌بندی است و عناوین متداول در انبارها مانند «لیفتراک»، «جرثقیل کارگاهی» و «وانت بار» گروه‌بندی استانداردی ندارند.
- **راهکار اصلاحی:**
  سازماندهی لیست `vehicleTypes` با ساختار گروه‌بندی (`optgroup`):
  - سبک و باری سبک (وانت نیسان، وانت بار، سواری)
  - سنگین و نیمه‌سنگین (خاور/کامیونت، کامیون تک/جفت، تریلی/کشنده)
  - ماشین‌آلات انبار و کارگاه (لیفتراک، جرثقیل، سایر تجهیزات)

### نقص ۸: عدم انطباق کد ملی راننده با پرسنل ثبت‌شده شرکت (نقض بند ۱۶)
- **شرح مشکل:** رانندگان ناوگان شرکتی یا ملکی پروژه اغلب در سیستم پرسنلی دارای پرونده هستند. وارد کردن کدملی راننده باید به صورت خودکار یا با یک کلیک بتواند مشخصات فرد را از لیست پرسنل موجود واکشی کرده و نام و تلفن را پیش‌پر کند تا از ورود مجدد و ناهماهنگ داده‌ها جلوگیری شود.
- **راهکار اصلاحی:**
  افزودن امکان جستجوی خودکار پرسنل بر اساس کدملی وارد شده و پیشنهاد تکمیل خودکار نام و تلفن راننده از سوابق پرسنلی موجود در بخش/پروژه.

### نقص ۹: عدم تقارن دکمه «ثبت خودرو جدید» در هدر چسبان نسبت به سایر دکمه‌های آیکونی (نقض بند ۱۷)
- **شرح مشکل:** در استاندارد صفحه پرسنل، هر ۴ دکمه هدر به صورت دکمه‌های آیکونی متقارن `w-9 h-9 rounded-xl` با تولتیپ فارسی طراحی شده‌اند؛ اما در ناوگان دکمه ثبت خودرو متنی عریض با `px-3 py-2` است که نظم چیدمان را بر هم می‌زند.
- **راهکار اصلاحی:**
  استانداردسازی دکمه چهارم هدر به فرمت آیکونی شاخص ایندیگو `w-9 h-9 rounded-xl` با آیکون خودرو و علامت مثبت و تولتیپ «معرفی خودرو جدید (پیش‌نویس)».

---

## ۴. برنامه اقدام فازبندی‌شده اصلاحی (Remedial Action Roadmap)

### فاز ۱: پایگاه داده، مدل‌ها و اعتبارسنجی بک‌اند (Backend Data Layer)
- **فایل‌ها:**
  - `warehouse-backend/personnel/models.py`
  - `warehouse-backend/personnel/serializers.py`
  - `warehouse-backend/personnel/migrations/0010_vehicledriverprofile_owner_fields.py`
- **اقدامات:**
  1. افزودن فیلدهای مالک به مدل `VehicleDriverProfile`:
     ```python
     is_driver_owner = models.BooleanField(default=True, verbose_name="مالک شخص راننده است")
     owner_name = models.CharField(max_length=150, blank=True, null=True, verbose_name="نام و نام خانوادگی مالک")
     owner_national_code = models.CharField(max_length=10, blank=True, null=True, verbose_name="کد ملی مالک")
     owner_phone = models.CharField(max_length=20, blank=True, null=True, verbose_name="شماره تماس مالک")
     ```
  2. ایجاد مایگریشن بدون downtime.
  3. افزودن اعتبارسنجی کد ملی ۱۰ رقمی مالک در `VehicleDriverProfileSerializer`.
  4. افزودن فیلدهای مالک به ساختار اکسل خروجی و ورودی.

### فاز ۲: منطق کامپوننت فرانت‌اند و مدیریت حالات (Frontend Component Logic)
- **فایل‌ها:**
  - `warehouse-front/src/app/components/finance/employee/employee-new-vehicle/employee-new-vehicle.ts`
  - `warehouse-front/src/app/core/models/personnel.model.ts`
- **اقدامات:**
  1. به‌روزرسانی اینترفیس `VehicleDriverProfile` با فیلدهای مالک.
  2. پیاده‌سازی منطق تاگل در `setStatusFilter` (بازگشت به 'all' در کلیک مجدد).
  3. پیاده‌سازی گتر `isReadOnlyMode`:
     ```typescript
     get isReadOnlyMode(): boolean {
       if (!this.editingVehicle) return false;
       if (this.isApprovedRecord) return false;
       return this.editingVehicle.approval_status !== 'draft' && this.editingVehicle.approval_status !== 'revision_required';
     }
     ```
  4. پیاده‌سازی دیکشنری ترجمه فارسی فیلدهای Diff (`fieldLabelsMap`).
  5. سازماندهی `vehicleTypes` با دسته‌بندی‌های استاندارد.
  6. افزودن متد واکشی مشخصات از پرسنل بر مبنای کدملی راننده.

### فاز ۳: بازطراحی قالب، مودال‌ها، ارگونومی موبایل و هدر (UI & Template Refactor)
- **فایل‌ها:**
  - `warehouse-front/src/app/components/finance/employee/employee-new-vehicle/employee-new-vehicle.html`
- **اقدامات:**
  1. بازطراحی مودال ثبت/ویرایش با ساختار Bottom Sheet در موبایل و بستن با Backdrop Click.
  2. تفکیک بخش مالک و راننده در فرم با چک‌باکس «مالک شخص راننده است».
  3. اعمال `[disabled]="isReadOnlyMode"` روی تمام فیلدهای فرم در صورت مشاهده پرونده در گردش کار.
  4. بازطراحی ستون عملیات جدول:
     - رکوردهای در جریان: آیکون مشاهده (`👁️`)
     - پیش‌نویس: آیکون ویرایش + آیکون موشک ارسال + حذف
     - عودت‌داده‌شده: آیکون اصلاح مجدد (نارنجی) + حذف
     - مصوب: آیکون ویرایش و پیشنهاد تغییرات
  5. بازطراحی مودال مقایسه تغییرات معلق (Diff) با مقایسه دوطرفه (مقدار قبلی خط‌خورده ◀ مقدار جدید سبز) و عناوین فارسی.
  6. اصلاح تب‌های فیلتر جهت نمایش همیشگی بدون `*ngIf`.
  7. استانداردسازی دکمه ثبت خودرو در هدر به ابعاد `w-9 h-9 rounded-xl`.

### فاز ۴: تست‌های خودکار و تضمین کیفیت (Testing & Verification)
- **فایل‌ها:**
  - `warehouse-front/src/app/components/finance/employee/employee-new-vehicle/employee-new-vehicle.spec.ts`
  - `warehouse-backend/personnel/tests_vehicle_cycle.py`
- **اقدامات:**
  1. افزودن تست‌های واحد برای رفتار تاگل تب‌ها (`setStatusFilter`).
  2. افزودن تست حالت `isReadOnlyMode` در رکوردهای معلق و رد شده.
  3. افزودن تست اعتبارسنجی و ذخیره‌سازی مشخصات مالک.
  4. تست رفتار بستن مودال با Backdrop Click.
  5. اجرای تست‌های Vitest و Django Test و اطمینان از پاس شدن ۱۰۰٪.

---

## ۵. چک‌لیست راستی‌آزمایی و ارزیابی نهایی (Acceptance Checklist)

- [x] مدل `VehicleDriverProfile` دارای فیلدهای ۴ گانه مالک بوده و مایگریشن اجرا شده باشد.
- [x] چک‌باکس «مالک شخص راننده است» در فرم کار کند و در صورت False بودن فیلدهای مالک فعال شود.
- [x] رکوردهای در حال بررسی کارتابل در مودال فقط‌خواندنی (`isReadOnlyMode`) باز شوند و خطای ۴۰۰ رخ ندهد.
- [x] در نمای دسکتاپ و موبایل، برای رکوردهای در جریان، آیکون مشاهده (`👁️`) نمایش یابد.
- [x] کلیک مجدد روی تب فعال وضعیت، فیلتر را به «همه» تغییر دهد (منطق Toggle).
- [x] مودال ثبت و مودال Diff در موبایل به صورت Bottom Sheet لغزنده از پایین باز شوند و با کلیک روی پس‌زمینه بسته شوند.
- [x] مودال Diff مقادیر قبلی را با خط‌خوردگی و مقادیر جدید را با برچسب سبز همراه با عناوین فارسی نمایش دهد.
- [x] تب‌های وضعیت «نیازمند اصلاح» و «رد شده» همیشه با شمارنده نمایش داده شوند و مخفی نشوند.
- [x] هدر چسبان دارای ۴ دکمه آیکونی متقارن `w-9 h-9 rounded-xl` با استاندارد یکپارچه باشد.
- [x] بیلد TypeScript (`npx tsc --noEmit`) با صفر خطا اجرا شود.
- [x] تمام تست‌های فرانت‌اند و بک‌اند با موفقیت سبز شوند.

</div>
