<div dir="rtl" align="right">

# طرح جامع و تفصیلی ارتقا و بازطراحی صفحه ناوگان جدید (Vehicle New Fleet Refactor Plan)
## انطباق خط به خط با دستاوردهای صفحه پرسنل جدید (`employee-new-personnel`) و چت `77db11aa-0120-454d-ab20-37e8908ccdec`

> [!IMPORTANT]
> **وضعیت اجرایی:** صرفاً تدوین طرح و برنامه اقدام (Design & Planning Phase). هیچ کدی در فرانت‌اند یا بک‌اند بدون تایید صریح کاربر تغییر نخواهد کرد.
> کلیه بخش‌های این سند با رعایت کامل استانداردهای معماری سازمانی، سیستم نگهبانان امنیتی و راهبردهای چت پیشین تدوین شده است.

---

## ۱. مقدمه و چرایی بازطراحی

در چت پیشین (`77db11aa-0120-454d-ab20-37e8908ccdec`)، صفحه ثبت پرسنل جدید (`employee-new-personnel`) پس از بررسی‌های عمیق چندمرحله‌ای، از یک صفحه سنتی با فرم درون‌خطی و فاقد چرخه تاییدات استاندارد به یک پرتال مدرن، ایمن و هماهنگ با استانداردهای سازمانی تبدیل شد. 

صفحه متناظر آن در ناوگان (`/app/finance/employee-new-vehicle` و کامپوننت `EmployeeNewVehicle`) دقیقاً همان معماری قدیمی و آسیب‌پذیر را دارد و نیازمند انطباق دقیق است. در این بررسی عمیق، **۲۴ مغایرت، باگ حیاتی و کمبود ساختاری** شناسایی شد که در ۵ لایه مجزا دسته‌بندی و برنامه‌ریزی شده‌اند.

---

## ۲. جدول مقایسه‌ای جامع ۲۴ گانه (Comprehensive Comparison Table)

| ردیف | موضوع و محور | وضعیت در `employee-new-personnel` (حل شده در چت قبل) | وضعیت فعلی در `employee-new-vehicle` (وضعیت جاری) | پیامد فنی و خطر معماری | راهکار و تغییرات خط به خط الزامی |
| :---: | :--- | :--- | :--- | :--- | :--- |
| **۱** | **نحوه نمایش فرم تعریف** | مودال شناور و ریسپانسیو (`isNewModalOpen`) با Bottom Sheet در موبایل | فرم درون‌خطی عریض (Inline) بالای جدول که بیش از ۵۰۰ پیکسل فضا اشغال کرده | اشغال کل صفحه، خارج شدن جدول خودروها از دید کاربر و نقض استاندارد UI | حذف فرم درون‌خطی، ایجاد دکمه آیکونی «افزودن» در هدر و انتقال فرم به مودال شناور |
| **۲** | **تفکیک ثبت پیش‌نویس و ارسال به سرپرست** | دو دکمه مجزا: «ذخیره پیش‌نویس» (`draft`) و «ارسال به سرپرست» (`pending_supervisor`) | یک دکمه واحد با لاجیک هاردکد `approval_status = 'draft'` | راننده/کاربر پس از ثبت راهی برای ارسال رکورد به چرخه تایید کارتابل سرپرست ندارد | افزودن دکمه دوگانه در مودال و آیکون موشک (ارسال سریع) در سطر جدول |
| **۳** | **امکان ویرایش رکوردهای مصوب** | قابلیت درخواست ویرایش (`VehicleChangeRequest`) با ثبت علت تغییرات | مسدودسازی کامل با آیکون قفل (`🔒 مصوب`) بدون هیچ راهکاری برای ویرایش پلاک یا نرخ | تغییر پلاک، راننده یا شماره شبا غیرممکن شده و نیازمند دستکاری دیتابیس است | اتصال به جریان `VehicleChangeRequest` با فرمت تغییرات دو ستونه (Diff) |
| **۴** | **باگ تنزل رتبه خودرو در تایید مدیر** | محافظت‌شده؛ فیلدهای کنترلی از `diff` استخراج و فیلتر شده‌اند | **باگ کشنده در بک‌اند (`views.py:1040-1050`):** بازنویسی کورکورانه کل `diff` روی رکورد خودرو | تایید درخواست تغییرات توسط مدیر، مقدار `approval_status` را به `pending_supervisor` بازمی‌گرداند و رکورد فعال را معلق می‌کند! | افزودن `excluded_fields` و حذف فیلدهای کنترلی و FKها در متد `approve_manager` |
| **۵** | **حفره امنیتی قلمرو پروژه/بخش (BOLA/IDOR)** | فیلتر بر اساس `UserSectionAssignment` در `get_queryset` و `perform_create` | فاقد فیلتر قلمرو بخش؛ کاربر به تمامی خودروهای شرکت و پروژه‌های دیگر دسترسی دارد | نقض شدید حریم خصوصی و امنیت داده‌های مالی شرکت | اعمال فیلتر سفت‌وسخت بر اساس انتساب‌های کاربری کاربر به بخش‌ها در `VehicleDriverProfileViewSet` |
| **۶** | **ریسک حذف فیزیکی (Cascade Delete)** | جلوگیری از حذف سوابق متصل و تبدیل خودکار به غیرفعال‌سازی نرم (`is_active = False`) | متد `destroy` اورراید نشده و حذف فیزیکی در بک‌اند باز است | حذف یک خودرو منجر به حذف آبشاری تمام کارکردها (`VehicleTripLog`) و اسناد تسویه می‌شود! | متد `destroy` سفارشی با بررسی لاگ‌های تردد و تبدیل به `is_active = False` در صورت وجود سابقه |
| **۷** | **همگام‌سازی بلادرنگ وب‌سوکت** | تزریق `WebSocketService` و متد `broadcast_personnel_update` در بک‌اند | وب‌سوکت تزریق نشده و در بک‌اند هیچ رویدادی برای تغییر وضعیت ناوگان برودکست نمی‌شود | عدم اطلاع لحظه‌ای کاربر و سرپرست از تایید، عودت یا اصلاح سوابق ناوگان | پیاده‌سازی `broadcast_vehicle_update` در سیگنال‌های بک‌اند و شنونده وب‌سوکت در فرانت‌اند |
| **۸** | **گردش کار موارد نیازمند بازنگری** | تب اختصاصی «عودت جهت اصلاح» با نمایش باکس زرد دلیل بازنگری و امکان ارسال مجدد | علت بازنگری در جدول پنهان است؛ تب‌های «عودت» و «رد شده» وجود ندارند | کاربر علت عودت سرپرست یا مدیر را نمی‌فهمد و دکمه‌ای برای ارسال اصلاحیه ندارد | تعبیه تب عودت، پاپ‌اور نمایش علت بازنگری و دکمه بازنگری مجدد |
| **۹** | **ایمپورت اکسل ناوگان** | دکمه استاندارد ایمپورت در هدر، مودال نگاشت ستون‌ها و وب‌سرویس بک‌اند | فقط دکمه اکسپورت وجود دارد؛ هیچ ابزاری برای ورود دسته‌ای ناوگان وجود ندارد | ورود صدها خودرو در پروژه‌های جدید به صورت دستی و تک‌تک بسیار زمان‌بر و خطاساز است | تعبیه دکمه ایندیگو در هدر، ساخت مودال ایمپورت و متد بک‌اند `import_excel` |
| **۱۰** | **کمبود متدهای وب‌سرویس در فرانت‌اند** | متدهای ویرایش، دانلود الگو و ایمپورت اکسل در سرویس پیاده‌سازی شده | متدهای `updateVehicleProfile` و `importVehicleExcel` در `PersonnelApiService` تعریف نشده‌اند | خطای کامپایل هنگام اتصال فرانت‌اند به وب‌سرویس‌های ویرایش و بارگذاری اکسل | افزودن تایپ‌ها و متدهای کامل خودرو در `personnel-api.service.ts` |
| **۱۱** | **تاخیر زمانی در جستجو (Debounce)** | استفاده از `debounceTime(300)` روی فیلد جستجوی زنده جدول | فیلتر کردن در هر بار فشردن کلید (`(input)="applyFilters()"`) بدون هیچ تاخیری | افت شدید فریم‌ریت و کندی رابط کاربری در لیست‌های با تعداد ناوگان بالا | استفاده از RxJS `Subject` با تاخیر ۳۰۰ میلی‌ثانیه در جستجوی جدول ناوگان |
| **۱۲** | **ارگونومی ورود شماره شبا** | پیش‌وند ثابت LTR با مقدار `IR`، اعتبارسنجی الگوریتم شبا و دکمه کپی | فیلد متنی ساده بدون پیشوند مجزا و فاقد کپی سریع | خطای کاربری مکرر در ثبت پیشوند IR، معکوس شدن حروف و بروز اشکال در تسویه پایا | بازطراحی فیلد شبا با پیشوند ثابت LTR، فونت مونو و اکشن کپی سریع |
| **۱۳** | **فرمت‌بندی پویای مبالغ (کرایه/نرخ)** | فرمت‌بندی سه رقمی حین تایپ با حفظ مکان‌نما و تبدیل خودکار ریال/تومان | فیلد عددی خام (`type="number"`) بدون کاما و بدون برچسب واحد پولی | خطای دید فاحش کاربر در ورود صفرهای مبالغ کرایه (مثلاً وارد کردن ۲ میلیون به جای ۲۰ میلیون) | پیاده‌سازی فرمت‌بندی سه رقمی درجا، تبدیل حسی به تومان و حفظ موقعیت کرسر |
| **۱۴** | **پلاک‌خوان و قالب بصری استاندارد ایران** | پلاک گرافیکی ۴ تکه استاندارد ایران با زمینه زرد/سفید و تفکیک دو رقم، حرف، سه رقم و کد شهر | فیلد متنی ساده برای پلاک که باعث ثبت قالب‌های نامعتبر می‌شود | ناهمگونی پلاک‌ها در گزارشات بیمه، فاکتورها و تسویه مالی | فرم پلاک استاندارد با ۴ بخش مجزا (ایران - ۳ رقم - حرف - ۲ رقم) |
| **۱۵** | **انتخاب سریع نوع خودرو و کاربری** | سلکتور هوشمند با گروه‌بندی (سواری، وانت، خاور، تریلی، لیفتراک) | دراپ‌داون ساده متنی بدون دسته‌بندی و پیش‌فرض | اتلاف وقت در انتخاب نوع ماشین و امکان ثبت اشتباه کاربری | بهینه‌سازی فهرست کشویی بر اساس استاندارد وزارت راه و انبارداری |
| **۱۶** | **اعتبارسنجی کدملی مالک و راننده** | اعتبارسنجی هوشمند الگوریتم ۱۰ رقمی و تطابق با پرسنل موجود | فاقد اعتبارسنجی الگوریتم ۱۰ رقمی رسمی ثبت‌احوال | ثبت کدهای نامعتبر و بروز اشکال در دیسکت مالیات و بیمه رانندگان | افزودن تابع اعتبارسنجی کدملی ۱۰ رقمی با فرمول باقیمانده بر ۱۱ |
| **۱۷** | **هدر شیشه‌ای چسبان (Sticky Header)** | هدر چسبان شیشه‌ای با استاندارد سه‌دکمه‌ای آیکونی (اکسل، ایمپورت، سینک) | هدر معمولی با دکمه‌های ناهمگون و فاقد استایل یکنواخت پروژه | ناهمگونی بصری با استاندارد یکپارچه پروژه (`Projects & Sections`) | اعمال استایل `sticky top-0 z-30 bg-slate-50/95 backdrop-blur-md` و دکمه‌های ۹x۹ |
| **۱۸** | **تب‌های فیلتر با شمارنده پویا** | سگمنت فیلتر وضعیت‌ها شامل شمارنده‌های عددی فعال برای هر وضعیت | فقط ۴ تب ساده بدون شمارنده و بدون تب عودت | کاربر متوجه وجود رکوردهای جدید نیازمند اقدام نمی‌شود | بازطراحی نوار سگمنت در سمت راست با استایل اختصاصی و بج‌های عددی مونو |
| **۱۹** | **همگام‌سازی فیلترها با آدرس صفحه (URL Sync)** | انعکاس وضعیت تب و جستجو در URL Query Params برای حفظ حالت در Refresh | وضعیت‌ها در آدرس ذخیره نمی‌شوند و با رفرش صفحه از دست می‌روند | نارضایتی کاربر از پاک شدن فیلترها و موقعیت کاری پس از رفرش یا اشتراک‌گذاری لینک | اتصال `activeTab` و فیلترها به `queryParams` با استفاده از Angular Router |
| **۲۰** | **طراحی اختصاصی کارت‌های ریسپانسیو موبایل** | کارت‌های هوشمند لمسی در موبایل بدون اسکرول افقی با اکشن‌های سریع | جدول معمولی که در موبایل اسکرول وحشتناک افقی می‌خورد | غیرقابل استفاده بودن در گوشی‌ها و تبلت‌های انبارداران در محوطه انبار | سوییچ خودکار به نمای کارت ارگونومیک در صفحات زیر ۷۶۸ پیکسل |
| **۲۱** | **تفکیک مالک و راننده در پرونده ناوگان** | امکان تعریف مجزای مشخصات هویتی و بانکی مالک نسبت به راننده | فرم اطلاعات را به هم ریخته و شبا را مستقیماً به نام راننده ثبت می‌کند | اشکال حقوقی در پرداخت کرایه خودروهای استیجاری به حساب غیرمالک | تفکیک فیلدهای حساب بانکی و شبا با چک‌باکس «مالک شخص راننده است» |
| **۲۲** | **بج‌های وضعیت دوحالته (Toggleable Badges)** | کلیک مجدد روی بج فعال در فیلترها وضعیت را لغو (`null`) می‌کند | فیلترها قابلیت لغو با کلیک مجدد را ندارند | کاربر برای دیدن همه رکوردها باید تب «همه» را مجدداً انتخاب کند | پیاده‌سازی منطق تاگل در کلیک روی وضعیت‌ها طبق استاندارد Rule 7 |
| **۲۳** | **تست‌های واحد کامپوننت و DOM (Vitest)** | فایل تست کامل `employee-new-personnel.spec.ts` با تست DOM سریع | فایل تست ناوگان ناقص است یا وجود ندارد | خطر بروز رگرسیون در بیلدهای بعدی بدون پوشش تست خودکار | ایجاد تست‌های جامع با jsdom و Vitest (تست نوع اول طبق قانون پرایمری) |
| **۲۴** | **کنترل دسترسی نقشی (RBAC) در اکشن‌ها** | نمایش دکمه‌ها و دسترسی به ویرایش دقیقاً بر اساس ماتریس RBAC کاربر | دکمه‌ها بر اساس سطح دسترسی محدود نشده‌اند | دسترسی احتمالی کاربران عادی به اکشن‌های تایید یا لغو تاییدات ناوگان | اعمال شرط‌های دقیق `canApprove` و `canEdit` در سطح تمپلیت و منطق کنترلر |

---

## ۳. نقشه راه فازبندی‌شده اجرا (5-Phase Execution Plan)

### فاز ۱: بازطراحی UI فرانت‌اند، هدر چسبان و انتقال فرم به مودال
- **فایل‌ها:** `employee-new-vehicle.html` و `employee-new-vehicle.ts`
- **اقدامات:**
  1. حذف بلوک فرم بزرگ درون‌خطی بالای صفحه.
  2. ایجاد هدر چسبان شیشه‌ای (`sticky top-0 z-30`) مطابق استاندارد `/app/finance/projects-and-sections`.
  3. استقرار دکمه‌های آیکونی سه‌گانه (سبز برای خروجی اکسل، ایندیگو برای ورود اکسل، اسلیت برای ریفرش) به علاوه دکمه آبی آسمانی «افزودن خودرو جدید» (`w-9 h-9 rounded-xl`).
  4. انتقال کامل فرم به مودال شناور (`isNewVehicleModalOpen`) با انیمیشن ورود و پشتیبانی از Bottom Sheet در موبایل.
  5. بازطراحی اینپوت پلاک خودرو به قالب ۴ قسمتی استاندارد ایران.
  6. اصلاح اینپوت شبا با پیشوند ثابت LTR (`IR`) و اعتبارسنجی درجا.

### فاز ۲: چرخه کارتابل، گردش کار بازنگری و امکان ویرایش رکوردهای مصوب
- **فایل‌ها:** `employee-new-vehicle.html`، `employee-new-vehicle.ts`، `personnel-api.service.ts`
- **اقدامات:**
  1. افزودن دو دکمه در مودال ثبت: «ذخیره به عنوان پیش‌نویس» و «ثبت و ارسال به سرپرست».
  2. افزودن آیکون موشک («ارسال به سرپرست») در ستون عملیات جدول برای رکوردهای پیش‌نویس.
  3. باز کردن قفل ویرایش برای رکوردهای مصوب؛ تبدیل دکمه ویرایش به درخواست ثبت تغییرات (`VehicleChangeRequest`).
  4. افزودن تب «عودت داده شده جهت اصلاح» و «رد شده» به نوار تب‌ها با شمارنده‌های پویا.
  5. نمایش پاپ‌اور یا بنر هشدار زردرنگ شامل «علت عودت سرپرست/مدیر» به همراه دکمه ارسال مجدد اصلاحیه.

### فاز ۳: اصلاح باگ تنزل رتبه و ایمن‌سازی امنیتی لایه بک‌اند
- **فایل‌ها:** `warehouse-backend/personnel/views.py` و `models.py`
- **اقدامات:**
  1. **رفع باگ کشنده تنزل رتبه (`views.py:1040-1050`):** در اکشن `approve_manager` مدل `VehicleChangeRequest`، لیست فیلدهای مستثنی (`excluded_fields`) شامل `approval_status`، `id`، `created_at`، `updated_at` و روابط خارجی ایجاد شود تا وضعیت تایید شده ناوگان به `pending_supervisor` بازنگردد.
  2. **کنترل قلمرو بخش (BOLA/IDOR):** اصلاح متد `get_queryset` در `VehicleDriverProfileViewSet` تا کاربران غیر سوپریوزر فقط خودروهای پروژه‌ها و بخش‌های مجاز خود را ببینند.
  3. **ایمن‌سازی حذف فیزیکی (Cascade Delete):** بازنویسی متد `destroy` در `VehicleDriverProfileViewSet`؛ در صورتی که خودرو دارای لاگ کارکرد یا فاکتور باشد، خطای ۴۰۰ بازگردانده شده یا با سوییچ `is_active = False` آرشیو نرم شود.
  4. **برودکست رویدادهای وب‌سوکت:** ایجاد تابع کمکی `broadcast_vehicle_update` در لایه سیگنال‌ها یا کنترلر جهت اطلاع‌رسانی لحظه‌ای تغییر وضعیت ناوگان.

### فاز ۴: ورودی اکسل (Import) و همگام‌سازی بلادرنگ وب‌سوکت
- **فایل‌ها:** فرانت‌اند و بک‌اند
- **اقدامات:**
  1. پیاده‌سازی مودال آپلود اکسل و نگاشت ستون‌های فایل به فیلدهای خودرو.
  2. ایجاد اندپوینت اختصاصی `import_excel` در `VehicleDriverProfileViewSet` با اعتبارسنجی اعشاری نرخ‌ها و یکتایی پلاک‌ها.
  3. تزریق `WebSocketService` در کامپوننت `EmployeeNewVehicle` و هندل کردن رویدادهای `vehicle_created`، `vehicle_updated` و `approval_status_changed`.
  4. افزودن تاخیر ۳۰۰ میلی‌ثانیه (`debounceTime(300)`) روی جستجوی جدول.

### فاز ۵: ارگونومی ریسپانسیو موبایل، آزمون‌های خودکار و تضمین کیفیت
- **فایل‌ها:** `employee-new-vehicle.spec.ts` و فایل‌های استایل
- **اقدامات:**
  1. استقرار نمای کارتی ارگونومیک برای صفحات کوچک‌تر از ۷۶۸ پیکسل (بدون اسکرول افقی).
  2. نگارش مجموعه آزمون‌های سریع DOM در محیط Vitest و jsdom (حداقل ۱۰ تست سناریویی).
  3. اجرای دستور تست بیلد فرانت‌اند (`npx tsc --noEmit`) و اطمینان از خروجی کاملاً سبز (Zero Errors).

---

## ۴. برنامه راستی‌آزمایی و ارزیابی (Verification Plan)

### تست‌های خودکار (Automated Tests)
1. **بررسی تایپ‌های فرانت‌اند:**
   ```bash
   npx tsc --noEmit
   ```
2. **آزمون‌های کامپوننت و DOM با Vitest:**
   ```bash
   npx vitest run src/app/components/finance/employee/employee-new-vehicle/employee-new-vehicle.spec.ts
   ```
3. **آزمون‌های چرخه حیات و وب‌سرویس بک‌اند با Django Test:**
   ```bash
   python manage.py test personnel.tests_vehicle_cycle
   ```

### راستی‌آزمایی بصری و تعاملی (Manual / DOM Verification)
- تست باز و بسته شدن مودال و عملکرد دکمه‌های «پیش‌نویس» و «ارسال به سرپرست».
- راستی‌آزمایی پلاک خودرو با فرمت ۴ تکه ایران.
- بررسی عدم تغییر وضعیت مصوب به پیش‌نویس در زمان تایید درخواست تغییرات.
- راستی‌آزمایی واکنش‌گرایی در سایز موبایل (عرض ۳۷۵ پیکسل).

</div>
