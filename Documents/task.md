<div dir="rtl" align="right">

# لیست وظایف جاری پروژه (Current Project Task List)

<!-- id: keyboard_shortcuts_master -->
### فاز ۱: مودال‌ها و دیالوگ‌های مشترک پایه (Shared Dialogs & Core Modals)
- [x] <!-- id: 10 --> ۱.۱. پیاده‌سازی `HostListener` برای کلیدهای `Enter` (تایید) و `Escape` (انصراف) در `ConfirmDialogComponent`
- [x] <!-- id: 11 --> ۱.۲. افزودن شنونده کیبورد `Escape` و `Enter` در `SmartDeleteModalComponent`
- [x] <!-- id: 12 --> ۱.۳. افزودن شنونده `Escape` و `Ctrl+Enter` در `ExcelImportModal` و `AvatarCropperModal`

### فاز ۲: قابلیت‌های کیبورد در جدول داده (Advanced DataTable Shortcuts)
- [x] <!-- id: 20 --> ۲.۱. پیاده‌سازی میانبر `Ctrl + S` جهت ذخیره تمامی تغییرات در انتظار (`saveAllChanges`) در `DataTableComponent`
- [x] <!-- id: 21 --> ۲.۲. پیاده‌سازی کلید `Space` برای انتخاب سطر فعال و `Ctrl + A` برای انتخاب همه سطرها
- [x] <!-- id: 22 --> ۲.۳. پیاده‌سازی کلید `Escape` برای بستن منوی ستون‌ها و پاک‌سازی فیلترها

### فاز ۳: عملیات شمارش و اسکنر انبار (Counter Dashboard & Barcode Scanner)
- [x] <!-- id: 30 --> ۳.۱. پشتیبانی از `Enter` و `Ctrl + Enter` برای ثبت سریع شمارش و بازگشت به لیست در `CounterDashboard`
- [x] <!-- id: 31 --> ۳.۲. پشتیبانی از `Escape` برای انصراف از جزییات کالا و بازگشت به لیست
- [x] <!-- id: 32 --> ۳.۳. پیاده‌سازی کلید میانبر `F2` یا `Alt + B` برای فوکوس مستقیم روی بارکدخوان و `Escape` برای بستن دوربین اسکنر

### فاز ۴: کارتابل سرپرست و بررسی مدیر (Supervisor & Manager Review Fast Actions)
- [x] <!-- id: 40 --> ۴.۱. پیاده‌سازی کلیدهای تایید سریع `A` (Approve) و ارجاع به شمارش مجدد `R` (Reject) در `SupervisorDashboard` و `ManagerReview`
- [x] <!-- id: 41 --> ۴.۲. پشتیبانی از `Ctrl + Enter` جهت تایید و ارسال فرم ثبت دلیل رد
- [x] <!-- id: 42 --> ۴.۳. بستن تمامی پنجره‌های بررسی و اکسل با کلید `Escape`

### فاز ۵: محیط‌های طراحی و چاپ لیبل و کارت پرسنلی (Label Designer & ID Cards Canvas)
- [x] <!-- id: 50 --> ۵.۱. پیاده‌سازی جابجایی دقیق پیکسلی (Nudge) با کلیدهای جهت‌نما و گام‌های ده‌تایی با `Shift + Arrow`
- [x] <!-- id: 51 --> ۵.۲. پیاده‌سازی میانبرهای استاندارد گرافیکی `Delete`, `Ctrl+C`, `Ctrl+V`, `Ctrl+S`, `Ctrl+P`
- [x] <!-- id: 52 --> ۵.۳. فعال‌سازی کنترل بزرگ‌نمایی/کوچک‌نمایی بوم با `+` و `-`

### فاز ۶: سرویس کلیدهای میانبر سراسری و پنجره راهنما (Global Shortcuts & Cheat Sheet)
- [x] <!-- id: 60 --> ۶.۱. ساخت پنجره مودال راهنمای کلیدهای میانبر (`Shift + ?` یا `F1`) در لایه هدر/لی‌اوت
- [x] <!-- id: 61 --> ۶.۲. پیاده‌سازی میانبر جمع/باز کردن سایدبار اصلی دسکتاپ (`Ctrl + B`)
- [x] <!-- id: 62 --> ۶.۳. تست بیلد فرانت‌اند و راستی‌آزمایی عملکردی تمام کلیدها

### فاز ۷: طرح جامع اصلاح کارتابل مالی (Customs Cartable Comprehensive Fixes)
- [x] <!-- id: 70 --> ۷.۱. پیاده‌سازی متدهای `get_export_columns` و `export_excel` در `DocTaskViewSet`
- [x] <!-- id: 71 --> ۷.۲. تفکیک متد `submitCurrentTask` برای ارسال کالای تکی فعال در فرم جزئیات
- [x] <!-- id: 72 --> ۷.۳. افزودن کادر جستجوی پیشرفته به `customs.html` و دیبانس ۳۰۰ms
- [x] <!-- id: 73 --> ۷.۴. افزودن بنر یادداشت رد سرپرست/مدیر و تاریخچه اقدامات در فرم جزئیات
- [x] <!-- id: 74 --> ۷.۵. بهینه‌سازی آفلاین و ذخیره ایمن فیلدهای کالا در `doc-task-store`
- [x] <!-- id: 75 --> ۷.۶. ارتقای عرض صفحه به `max-w-4xl`، دکمه انتخاب همه و همگام‌سازی URL با `taskId`
- [x] <!-- id: 76 --> ۷.۷. تست بیلد فرانت‌اند و بک‌اند و راستی‌آزمایی نهایی

### فاز ۸: طرح پیاده‌سازی وضعیت شمارش اولیه (Initial Count Workflow)
- [x] <!-- id: 80 --> ۸.۱. افزودن وضعیت `INITIAL_COUNT` به مدل `CountTask` و ایجاد مایگریشن جنگو (`0024_alter_counttask_status.py`)
- [x] <!-- id: 81 --> ۸.۲. به‌روزرسانی کوئری‌های فیلتر، متد `bulk_submit`، `bulk_cancel` و رجیستری گزارشات در بک‌اند
- [x] <!-- id: 82 --> ۸.۳. ارتقای مدل فرانت‌اند و منطق `saveDraft` در مخزن محلی `count-task-store`
- [x] <!-- id: 83 --> ۸.۴. بازطراحی فیلترها، شمارنده‌ها و بج‌های بصری `CounterDashboard`
- [x] <!-- id: 84 --> ۸.۵. هماهنگی کارتابل مانیتورینگ زنده `count-tracking` با وضعیت جدید
- [x] <!-- id: 85 --> ۸.۶. تست جامع بیلد، چرخه آفلاین/آنلاین و راستی‌آزمایی عملکردی

### فاز ۹: پیاده‌سازی داشبورد تاریخچه چت‌ها و جستجوگر هوشمند (Chat History Dashboard)
- [x] <!-- id: 90 --> ۹.۱. پیاده‌سازی اسکریپت استخراج داده‌های چت و لاگ‌ها (`update_chat_history.py`)
- [x] <!-- id: 91 --> ۹.۲. طراحی داشبورد تعاملی تک‌صفحه‌ای با جستجوی زنده و فیلتر تگ‌ها (`chat_history_dashboard.html`)
- [x] <!-- id: 92 --> ۹.۳. پیاده‌سازی مودال خواندن و رندر محتوای کامل مکالمات و پلن‌ها
- [x] <!-- id: 93 --> ۹.۴. اجرای اسکریپت، تولید پایگاه داده تاریخچه و راستی‌آزمایی جامع

### فاز ۱۰: بازآرایی فیلترهای داشبورد انبارگردان (Counter Dashboard Pending Default)
- [x] <!-- id: 100 --> ۱۰.۱. تغییر مقدار پیش‌فرض `statusFilter` به `'pending'` در تعاریف اولیه و مدیریت URL
- [x] <!-- id: 101 --> ۱۰.۲. ارتقای متد `applyFilters` جهت شمارش زنده چیپ‌ها بر اساس عبارت جستجو
- [x] <!-- id: 102 --> ۱۰.۳. بازچینی ترتیب چیپ‌های فیلتر و انتقال «همه موارد» به انتها
- [x] <!-- id: 103 --> ۱۰.۴. طراحی بنر راهنمای وضعیت خالی هوشمند برای تب دست‌نخورده با دکمه سوییچ سریع
- [x] <!-- id: 104 --> ۱۰.۵. تست بیلد فرانت‌اند و راستی‌آزمایی عملکردی

### فاز ۱۱: گروه‌بندی زمانی و کپی سریع عنوان انگلیسی در داشبورد چت‌ها (Chat History Time Grouping & Quick Copy)
- [x] <!-- id: 110 --> ۱۱.۱. پیاده‌سازی متد گروه‌بندی هوشمند زمانی و ماه‌های تقویم شمسی
- [x] <!-- id: 111 --> ۱۱.۲. طراحی کلید سوییچ سه‌حالته انتخاب نوع نمایش در نوار کنترل
- [x] <!-- id: 112 --> ۱۱.۳. افزودن دکمه اختصاصی کپی عنوان انگلیسی به همراه بازخورد بصری تیک سبز و توست
- [x] <!-- id: 113 --> ۱۱.۴. ذخیره وضعیت انتخاب در `localStorage` و تست همه‌جانبه

### فاز ۱۲: کارت‌های مینی‌مال آکاردئونی و تفکیک تک‌کلیک و دابل‌کلیک (Minimal Accordion & Double-Click Entry)
- [x] <!-- id: 120 --> ۱۲.۱. بازطراحی کارت‌ها به حالت مینی‌مال (عنوان انگلیسی، دکمه کپی و خلاصه کوتاه یک‌خطی)
- [x] <!-- id: 121 --> ۱۲.۲. پیاده‌سازی آکاردئون انحصاری با تک‌کلیک جهت باز شدن جزئیات و بستن سایر کارت‌ها
- [x] <!-- id: 122 --> ۱۲.۳. پیاده‌سازی ورود به صفحه چت با دابل‌کلیک (`ondblclick`) و دکمه کمکی داخل کارت
- [x] <!-- id: 123 --> ۱۲.۴. تست جامع بیلد، اسکریپت پایتون و راستی‌آزمایی بصری

### فاز ۱۴: استخراج عناوین دقیق چت‌ها و چیدمان عمودی زمانی (Chat History Exact Titles & Vertical Stacking)
- [x] <!-- id: 140 --> ۱۴.۱. استخراج دقیق عناوین رسمی Antigravity (فارسی و انگلیسی) از متادیتای لاگ‌ها و سرفصل‌ها
- [x] <!-- id: 141 --> ۱۴.۲. چیدمان عمودی و زیر هم بخش‌های زمانی با هدر تمام‌عرض و گرید چندستونه داخلی
- [x] <!-- id: 142 --> ۱۴.۳. به‌روزرسانی فونت‌ها و استایل کارت‌ها و ارتقای کپی عنوان چت
- [x] <!-- id: 143 --> ۱۴.۴. اجرای اسکریپت پایتون، تولید فایل داشبورد و راستی‌آزمایی بصری در مرورگر

### فاز ۱۵: تغییر مرتب‌سازی پیش‌فرض داشبورد انبارگردان (Counter Dashboard Default Sort Updated At)
- [x] <!-- id: 150 --> ۱۵.۱. تغییر مقدار اولیه `sortField` به `'updated_at'` و `sortDirection` به `'desc'` در `counter-dashboard.ts`
- [x] <!-- id: 151 --> ۱۵.۲. به‌روزرسانی متدهای `updateUrlState` و `syncStateFromUrl` جهت تنظیم پیش‌فرض `updated_at`
- [x] <!-- id: 152 --> ۱۵.۳. انتقال گزینه «آخرین زمان تغییر» به ردیف اول منوی کشویی مرتب‌سازی در `counter-dashboard.html`
- [x] <!-- id: 153 --> ۱۵.۴. تست بیلد فرانت‌اند و راستی‌آزمایی عملکردی مرتب‌سازی

### فاز ۱۶: بازبینی جامع و رفع ایرادات کارتابل انبارگردان (Counter Dashboard Audit & Fixes)
- [x] <!-- id: 160 --> ۱۶.۱. اصلاح متد `export_excel` و رعایت وضعیت شمارش کور (`is_blind`) در `inventory/views.py`
- [x] <!-- id: 161 --> ۱۶.۲. پایداری آفلاین تغییرات فیلدها و لوکیشن در `saveExtraEditedFields` با استفاده از `OfflineSyncService.enqueue`
- [x] <!-- id: 162 --> ۱۶.۳. تفکیک بصری تسک‌های بازشماری دارای پیش‌نویس (`SUPERVISOR_REJECTED` / `MANAGER_REJECTED`) در `counter-dashboard.html`
- [x] <!-- id: 163 --> ۱۶.۴. افزودن انیمیشن‌های استاندارد CSS (`slideInRight`, `slideInUp`, `slideInLeft`) در `styles.css`
- [x] <!-- id: 164 --> ۱۶.۵. تست جامع بیلد، اعتبارسنجی صفر (`!== null`) و راستی‌آزمایی فرآیندها

### فاز ۱۷: تفکیک تب «مغایرت و بازشماری» و چرخه بازگرداندن کالا (Counter Discrepancy & Revert Workflow)
- [x] <!-- id: 170 --> ۱۷.۱. اصلاح توابع `isRecount`، `isInitial` و `isPending` در متد `applyFilters` و به‌روزرسانی زنده `statusCounts`
- [x] <!-- id: 171 --> ۱۷.۲. پیاده‌سازی متد `revertTaskStatus` جهت بازگشت هوشمند به `SUPERVISOR_REJECTED` یا `PENDING_COUNT`
- [x] <!-- id: 172 --> ۱۷.۳. اصلاح رفتار خالی کردن مقدار در `saveDraft` جهت حفظ وضعیت اصلی کالا
- [x] <!-- id: 173 --> ۱۷.۴. ارتقای چیپ «مغایرت و بازشماری» (`bg-rose-500`)، نشان‌های وضعیت و کادر هشدار کارت‌ها
- [x] <!-- id: 174 --> ۱۷.۵. افزودن دکمه سریع بازگشت (Undo) روی کارت کالا در تب آماده ارسال و فرم جزئیات
- [x] <!-- id: 175 --> ۱۷.۶. تست بیلد فرانت‌اند، سناریوهای ۴ گانه گردش کار و راستی‌آزمایی جامع

### فاز ۱۸: چرخه کامل بک‌اند کارتابل مالی و پیاده‌سازی آزمون‌های ۱۵گانه (Financial Cartable Backend Cycle)
- [x] <!-- id: 180 --> ۱۸.۱. افزودن فیلد `data_snapshot` به مدل `DocTaskHistory` و اعمال میگریشن
- [x] <!-- id: 181 --> ۱۸.۲. پیاده‌سازی متد کمکی ساخت اسنپ‌شات و ثبت تاریخچه در تمام اکشن‌های `DocTaskViewSet`
- [x] <!-- id: 182 --> ۱۸.۳. همگام‌سازی و رونویسی ۱۴ فیلد مالی به جدول `Item` در متد `bulk_manager_approve`
- [x] <!-- id: 183 --> ۱۸.۴. پیاده‌سازی سوئیت جامع ۱۵ سناریوی آزمون بک‌اند در `inventory/tests_docs.py`
- [x] <!-- id: 184 --> ۱۸.۵. اجرای تست‌های `tests_docs.py` و `tests.py`، راستی‌آزمایی و تهیه گزارش `walkthrough.md`

### فاز ۱۹: طرح جامع ارتقا، فرم‌ها و رفع ایرادات کارتابل مالی (Financial Cartable Comprehensive Fixes)
- [ ] <!-- id: 190 --> ۱۹.۱. فاز ۱: کپی مقادیر اولیه در ارجاع، پشتیبانی از `sync_ids`، لاگ Claim، پرمیشن‌ها و تاریخ شمسی بک‌اند
- [ ] <!-- id: 191 --> ۱۹.۲. فاز ۲: ذخیره آفلاین فیلدهای کالا/پویا در Dexie و صف سینک، لود تنظیمات محلی و رفرش فرم
- [ ] <!-- id: 192 --> ۱۹.۳. فاز ۳: فرمت ۳ رقمی مبالغ، محاسبه ارزش کل، تقویم شمسی، رفع باگ ستون‌های اکسل و اسکنر
- [ ] <!-- id: 193 --> ۱۹.۴. فاز ۴: بومی‌سازی تاریخچه اقدامات، ترجمه ارز و فاکتور و مدال جزئیات کامل برای سرپرست و مدیر

### فاز ۲۰: معماری کش آفلاین-اول SWR و بروزرسانی بلادرنگ (Offline-First SWR Caching)
- [ ] <!-- id: 200 --> ۲۰.۱. افزودن جریان `liveDataUpdates$` و متد `notifyDataUpdated` در سرویس آفلاین
- [ ] <!-- id: 202 --> ۲۰.۳. اتصال کامپوننت‌های انبارگردان، سرپرست، مدیر، مالی و پیگیری به رویدادهای پس‌زمینه همراه با افکت فلش
- [ ] <!-- id: 203 --> ۲۰.۴. بیلد فرانت‌اند، راستی‌آزمایی در شرایط آفلاین و خطای ۵۰۲ و ثبت مستندات DUAL-SAVE

### فاز ۲۱: بازبینی و مقاوم‌سازی وب‌سوکت و دوربین اسکنر (Camera Scanner & WebSocket Enhancement)
- [ ] <!-- id: 210 --> ۲۱.۱. پیاده‌سازی سیگنال‌های Channel Layer (`broadcast_count_task_update` و `broadcast_doc_task_update`) در `inventory/signals.py`
- [ ] <!-- id: 211 --> ۲۱.۲. مدیریت پیام‌های Ping/Pong و پایداری در `notifications/consumers.py`
- [ ] <!-- id: 212 --> ۲۱.۳. پیاده‌سازی Heartbeat Ping، Exponential Backoff و شنوندگان رویدادهای `online`/`visibilitychange` در `websocket.service.ts`
- [ ] <!-- id: 213 --> ۲۱.۴. اصلاح تداخل استایل کلاس `hidden` و تضمین نمایش پایدار دوربین در `barcode-scanner.component.ts`
- [ ] <!-- id: 214 --> ۲۱.۵. اصلاح تعبیه اسکنر در `counter-dashboard.html` و `customs.html` و تست سناریوهای Real-time و اسکن

### فاز ۲۲: رفع مشکل صفحه سیاه دوربین و ارتقای تطبیق اسکنر (Camera Scanner Black Screen & Matching Fixes)
- [x] <!-- id: 221 --> ۲۲.۱. اصلاح قیود استریم و فال‌بک نرم به `{ video: true }` و مقداردهی صریح ویژگی‌های DOM ویدیو
- [x] <!-- id: 222 --> ۲۲.۲. پیاده‌سازی استعلام وب‌کم‌ها و افزودن دکمه تعاملی چرخش دوربین (`switchCamera`)
- [x] <!-- id: 223 --> ۲۲.۳. ارتقای موتور تطبیق بارکد چندگانه و نرمال‌سازی ارقام فارسی در کارتابل مالی (`customs.ts`)
- [x] <!-- id: 224 --> ۲۲.۴. تست بیلد کامل فرانت‌اند و راستی‌آزمایی عملکردی

### فاز ۲۳: احیا و بازنشانی استاندارد دوربین اسکنر بارکد بر پایه گیت (Camera Scanner Git-Based Restoration)
- [x] <!-- id: 231 --> ۲۳.۱. بازگرداندن اتصال استریم به متد استاندارد `decodeFromConstraints` از `@zxing/browser`
- [x] <!-- id: 232 --> ۲۳.۲. پیاده‌سازی متد `startNativeDetector` جهت اسکن با موتور بومی مرورگر در اندروید/کروم
- [x] <!-- id: 233 --> ۲۳.۳. حذف لوپ‌های دستی کراپ و بوم‌های آف‌اسکرین که باعث صفحه سیاه می‌شدند
- [x] <!-- id: 234 --> ۲۳.۴. ساده‌سازی قالب بصری اورلی دوربین، تست بیلد فرانت‌اند و راستی‌آزمایی

### فاز ۲۴: بهینه‌سازی حداکثری سرعت و قدرت تشخیص اسکنر دوربین (Camera Scanner Speed & Detection Optimization)
- [x] <!-- id: 241 --> ۲۴.۱. تنظیم فرکانس اسکن بالا (`delayBetweenScanAttempts: 40ms`) و رزولوشن بهینه ۷۲۰p
- [x] <!-- id: 242 --> ۲۴.۲. اولویت‌بندی فرمت‌های پرکاربرد انبار (`QR`, `Code 128`, `EAN-13`, `Data Matrix`, `Code 39`)
- [x] <!-- id: 243 --> ۲۴.۳. فعال‌سازی فوکوس پیوسته و کنترل‌های زوم سخت‌افزاری (۱X و ۲X)
- [x] <!-- id: 244 --> ۲۴.۴. تست بیلد کامل فرانت‌اند و راستی‌آزمایی عملکردی

### فاز ۲۵: بازنشانی کدهای اسکنر به نسخه اصلی با رزولوشن بالای گیت‌هاب (GitHub High-Resolution Scanner Reset)
- [x] <!-- id: 251 --> ۲۵.۱. پاک‌سازی کدهای سفارشی جاری و بازنشانی کامل `barcode-scanner.component.ts` به نسخه باکیفیت گیت‌هاب (Commit `5b66605`)
- [x] <!-- id: 252 --> ۲۵.۲. تنظیم دقیق رزولوشن 2.5K (`2560x1440`) و متد `BrowserQRCodeReader` با قابلیت `TRY_HARDER`
- [x] <!-- id: 253 --> ۲۵.۳. حفظ خصیصه ورودی `@Input() headless` و اتصال رویداد `scanned` جهت سازگاری با کارتابل‌ها
- [x] <!-- id: 254 --> ۲۵.۴. تست بیلد کامل فرانت‌اند با `npm run build` و راستی‌آزمایی پایداری استریم ویدیو

### فاز ۲۶: ارتقای موتور اسکنر دوبعدی و همگام با اسکنر بومی گوشی (2D Scanner Engine & Camera Controls)
- [x] <!-- id: 261 --> ۲۶.۱. بازنویسی و بهینه‌سازی `barcode-scanner.component.ts` (رزولوشن ۱۰۸۰p/۷۲۰p، فوکوس پیوسته، پشتیبانی جامع کدهای ۲ بعدی)
- [x] <!-- id: 262 --> ۲۶.۲. پیاده‌سازی کنترل‌های سخت‌افزاری دوربین (چراغ‌قوه Torch، زوم سخت‌افزاری 1x/2x، بازخورد لرزشی Haptic)
- [x] <!-- id: 263 --> ۲۶.۳. پیاده‌سازی خط لوله تشخیص دوگانه (Native BarcodeDetector با خطایابی پایدار + MultiFormat Fallback)
- [x] <!-- id: 264 --> ۲۶.۴. کامپایل و تست بیلد نهایی فرانت‌اند (`npm run build`) و مستندسازی در Walkthrough

### فاز ۲۷: پیاده‌سازی اسکنر هوشمند چندردیفه، اکسل نمونه داینامیک و به‌روزرسانی دسته‌ای کارتابل مالی (Financial Scanner Multi-Row Batch Update)
- [x] <!-- id: 271 --> ۲۷.۱. پیاده‌سازی متد `DocTaskViewSet.download_template` با فیلدهای داینامیک، سلول‌های کشویی و داده‌های آزمایشی + تنظیمات بک‌اند
- [x] <!-- id: 272 --> ۲۷.۲. ایجاد تایپ‌ها و متد پارسر هوشمند با تشخیص خودکار جداکننده‌ها و تطبیق ستون‌ها با عناوین فارسی/انگلیسی
- [x] <!-- id: 273 --> ۲۷.۳. افزودن دکمه دانلود نمونه در مودال خروجی اکسل و طراحی مودال مدرن پیش‌نمایش با آمار ۴گانه و برچسب استخر اسناد در `customs.html`
- [x] <!-- id: 274 --> ۲۷.۴. پیاده‌سازی متد `applyBulkScanUpdate` با کنترل فیلدهای `editable`، ادعای مالکیت استخر و ذخیره Local-First
- [x] <!-- id: 275 --> ۲۷.۵. افزودن فرم تنظیمات کاراکترهای جداکننده به صفحات تنظیمات و تست بیلد کامل

### فاز ۲۸: طرح جامع اصلاح به‌روزرسانی‌های زنده، وب‌سوکت و عملیات آفلاین در رهگیری شمارش (Count Tracking Live Updates & Offline Fixes)
- [x] <!-- id: 281 --> ۲۸.۱. ایجاد متد پیش‌پردازش استاندارد داده‌ها (`preprocessTasks`) و بازسازی `loadTasks` با قابلیت حفظ وضعیت انتخاب‌ها و شماره صفحه (`preserveState`)
- [x] <!-- id: 282 --> ۲۸.۲. پیاده‌سازی متد به‌روزرسانی موضعی تسک (`updateTaskInPlace`) و اتصال پایدار وب‌سوکت با کنترل Debounce
- [x] <!-- id: 283 --> ۲۸.۳. اصلاح سابسکرایبر استریم SWR با تطبیق دقیق URL نقش رهگیری و اجرای خط لوله پیش‌پردازش داده‌ها
- [x] <!-- id: 284 --> ۲۸.۴. پیاده‌سازی به‌روزرسانی خوش‌بینانه و پشتیبانی صف آفلاین در عملیات گروهی (`bulkApproveGreenTasks`, `bulkCancel`, `cancelSingleAllocation`)
- [x] <!-- id: 285 --> ۲۸.۵. اجرای بیلد فرانت‌اند با `npm run build` و راستی‌آزمایی جامع عملکردی و مستندسازی در Walkthrough

### فاز ۲۹: ارتقای همه‌جانبه اسکریپت و داشبورد تاریخچه چت‌ها (Chat History Comprehensive Enhancements)
- [x] <!-- id: 291 --> ۲۹.۱. استخراج زمان دقیق و واقعی چت‌ها از متادیتا و لاگ‌های JSONL (Time & ISO Extraction Engine)
- [x] <!-- id: 292 --> ۲۹.۲. کش‌گذاری تدریجی و ارتقای چشمگیر سرعت پارس (Incremental Parsing Cache)
- [x] <!-- id: 293 --> ۲۹.۳. تفکیک گزینه‌های مرتب‌سازی و گروه‌بندی در داشبورد (Advanced UI Sorting & Grouping)
- [x] <!-- id: 294 --> ۲۹.۴. پیاده‌سازی قابلیت جستجوی عمیق در متن کل پیام‌ها (Deep Full-Text Search Toggle)
- [x] <!-- id: 295 --> ۲۹.۵. ارتقای موتور رندر مارک‌داون، جداول، هشدارها و هایلایت کدها (Enhanced Markdown & Code Renderer)
- [x] <!-- id: 296 --> ۲۹.۶. افزودن حالت پایش خودکار در پس‌زمینه (Auto-Watch Mode `--watch`) و راستی‌آزمایی جامع

### فاز ۳۰: تشخیص خودکار چت‌های حذف‌شده، کادربندی متمایز و فیلتر سایدبار (Automatic Deleted Chat Detection, Visual Borders & Filter)
- [x] <!-- id: 301 --> ۳۰.۱. پاک‌سازی کامل خطوط جدید (`\n` و `\r`) از عناوین و رفع قطعی باگ کپی عنوان چت‌ها در `copyTitle`
- [x] <!-- id: 302 --> ۳۰.۲. تعریف فیلد `is_deleted` و سیستم ردیابی چت‌های حذف‌شده از سایدبار در `update_chat_history.py`
- [x] <!-- id: 303 --> ۳۰.۳. طراحی کادر خط‌چین کهربایی متمایز (`deleted-card`) و نشان `🗑️ حذف‌شده از سایدبار` در داشبورد
- [x] <!-- id: 304 --> ۳۰.۴. پیاده‌سازی سوئیچ فیلتر `🗑️ نمایش حذف‌شده‌ها` در نوار ابزار بالا با ذخیره وضعیت در `localStorage`
- [x] <!-- id: 305 --> ۳۰.۵. اجرای اسکریپت، اعتبارسنجی خروجی HTML و JSON و تکمیل مستندات طبق دستورالعمل DUAL-SAVE

### فاز ۳۱: طرح جامع اصلاح به‌روزرسانی‌های زنده، وب‌سوکت و عملیات آفلاین در کارتابل‌های سرپرست و مدیر (Supervisor & Manager Real-time & Offline Hardening)
- [x] <!-- id: 311 --> ۳۱.۱. ارتقای سیگنال‌های وب‌سوکت در بک‌اند (`signals.py`): ارسال مستقیم داده‌های تسک در Payload وب‌سوکت جهت پشتیبانی از به‌روزرسانی بدون دانلود مجدد (Zero Fetch)
- [x] <!-- id: 312 --> ۳۱.۲. اصلاح کارتابل سرپرست (`supervisor-dashboard.ts`): پیاده‌سازی متد به‌روزرسانی نقطه‌ای (`updateTaskInPlace`) با فال‌بک `getById` تکی و حفظ انتخاب‌ها (`preserveState`)
- [x] <!-- id: 313 --> ۳۱.۳. اصلاح کارتابل سرپرست: فیلتر دقیق استریم SWR و به‌روزرسانی خوش‌بینانه عملیات تایید/رد گروهی و تکی (`bulkApprove`, `bulkReject`) با پشتیبانی صف آفلاین
- [x] <!-- id: 314 --> ۳۱.۴. اصلاح کارتابل مدیر (`manager-review.ts`): پیاده‌سازی متد به‌روزرسانی نقطه‌ای (`updateTaskInPlace`) با فال‌بک `getById` تکی و حفظ انتخاب‌ها (`preserveState`)
- [x] <!-- id: 315 --> ۳۱.۵. اصلاح کارتابل مدیر: فیلتر دقیق استریم SWR و به‌روزرسانی خوش‌بینانه عملیات تایید/رد گروهی و تکی (`bulkManagerApprove`, `confirmSingleReject`) با پشتیبانی صف آفلاین
- [x] <!-- id: 316 --> ۳۱.۶. اعتبارسنجی بیلد فرانت‌اند با `npm run build`، اجرای تست سیگنال‌های بک‌اند و مستندسازی در Walkthrough

### فاز ۳۲: به‌روزرسانی نقطه‌ای هوشمند (In-Place Zero-Fetch) وب‌سوکت در کارتابل انبارگردان (Counter Dashboard In-Place Live Updates)
- [ ] <!-- id: 321 --> ۳۲.۱. افزودن متد `updateCountTaskInPlace` و فال‌بک `fetchSingleCountTask` به `counter-dashboard.ts`
- [ ] <!-- id: 322 --> ۳۲.۲. ارتقای شنونده وب‌سوکت `wsSub` به پردازش نقطه‌ای داده‌های تسک و حذف فراخوانی `refreshCurrentTab` در رویدادهای استاندارد
- [ ] <!-- id: 323 --> ۳۲.۳. پاک‌سازی فراخوانی متد ناموجود `fetchPoolTasksSilently` و تکمیل پاک‌سازی سابسکرایب‌ها در `ngOnDestroy`
- [ ] <!-- id: 324 --> ۳۲.۴. اجرای `npm run build` جهت اعتبارسنجی بیلد، تست عملیاتی و تکمیل مستندات DUAL-SAVE
</div>
