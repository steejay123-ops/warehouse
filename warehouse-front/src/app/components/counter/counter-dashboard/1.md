graph LR
    A["تخصیص تسک<br>(PENDING_COUNT)"] --> B["شمارش اولیه و ثبت موقت<br>(INITIAL_COUNT)"]
    B --> C["ارسال قطعی نوبت به سرپرست<br>(COUNTED)"]
    C --> D{"بررسی سرپرست / مدیر"}
    D -->|"تایید نهایی"| E["تایید نهایی<br>(FINAL_APPROVED)"]
    D -->|"مغایرت / نیاز به بازشماری"| F["ارجاع به بازشماری<br>(RECOUNT / نوبت بعد)"]
    F --> B
