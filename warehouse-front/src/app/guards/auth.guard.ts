/**
 * ⚠️ COMPATIBILITY SHIM — از این فایل دیگر استفاده نکنید!
 *
 * به جای آن از core/auth/auth.guard.ts استفاده کنید:
 *
 *   import { AuthGuard } from '../core/auth/auth.guard';
 *
 * @deprecated — use core/auth/auth.guard.ts
 */
export { AuthGuard } from '../core/auth/auth.guard';
