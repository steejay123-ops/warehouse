/**
 * ⚠️ COMPATIBILITY SHIM — از این فایل دیگر استفاده نکنید!
 *
 * این فایل فقط برای سازگاری با صفحاتی که هنوز refactor نشده‌اند نگه داشته شده.
 * به جای آن از core/auth/auth.service.ts استفاده کنید:
 *
 *   import { AuthService } from '../core/auth/auth.service';
 *
 * @deprecated — use core/auth/auth.service.ts
 */
export { AuthService } from '../core/auth/auth.service';
