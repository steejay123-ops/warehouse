<div dir="rtl" align="right">

# بررسی فنی پروژه اتوماسیون انبارگردانی

**تاریخ بررسی:** ۲۹ ژوئیه ۲۰۲۶ · **دامنه:** `warehouse-backend` (Django) و `warehouse-front` (Angular) · **روش:** مطالعه مستقیم کد؛ برنامه اجرا نشده است.

---

## ۱. جمع‌بندی کلی

این یک پروژه جدی و قابل احترام است. دامنه‌ی کاری‌اش کوچک نیست: چرخه چندمرحله‌ای شمارش با سه نقش، یک چرخه موازی برای اسناد، ورود اطلاعات از اکسل با استراتژی‌های مختلف تعارض، فیلدهای پویا به ازای هر انبار، طراح لیبل با خروجی PDF فارسی، سیستم نقش‌های سلسله‌مراتبی، و پشتیبان‌گیری رمزنگاری‌شده. اینها را یک نفر یا یک تیم کوچک ساخته و بخش عمده‌شان واقعاً کار می‌کند. مدل داده تمیز است و ردیابی تاریخچه (`CountTaskHistory`) از همان ابتدا دیده شده — این چیزی است که خیلی از پروژه‌های مشابه بعد از دو سال هم ندارند.

اما ارزیابی صادقانه این است: **پروژه از نظر قابلیت‌ها بالغ است و از نظر آمادگی تولید (production) نیست.** فاصله میان این دو، چند هفته کار متمرکز است نه چند ماه، و تقریباً هیچ‌کدام از مشکلات نیازمند بازنویسی نیستند. مشکل اصلی این نیست که چیزی ساخته نشده؛ مشکل این است که لایه‌ی محافظتی اطراف چیزهای ساخته‌شده (مجوزدهی سمت سرور، قفل‌گذاری همزمانی، تنظیمات محیط، تست) هنوز نازک است.

سه مورد وجود دارد که اگر این سیستم روی شبکه‌ی شرکت بالا بیاید، باید **قبل از** آن حل شوند. آنها را در بخش بعد می‌آورم.

**وضعیت خلاصه:**

| محور | ارزیابی | توضیح کوتاه |
|---|---|---|
| منطق کسب‌وکار و پوشش نیازها | خوب | چرخه کار کامل، تاریخچه، اکسل، نقش‌ها |
| مدل داده | نسبتاً خوب | نرمال، ایندکس GIN، چند اشکال در `on_delete` |
| امنیت | ضعیف | چند مسیر واقعی برای دور زدن دسترسی‌ها |
| همزمانی (concurrency) | ضعیف | هیچ قفلی روی گذارهای وضعیت وجود ندارد |
| کیفیت/ساختار کد | متوسط | فایل‌های بسیار بزرگ، لایه سرویس ندارد |
| آمادگی تولید | ضعیف | `DEBUG=True`، کلید ثابت، بدون تست و لاگ |

---

## ۲. سه مسئله‌ای که باید فوراً حل شوند

### ۲.۱ فیلتر دسترسی داده‌ها به پارامتر ارسالی کاربر اعتماد می‌کند

در `CountTaskViewSet.get_queryset()` تعیین اینکه کاربر چه رکوردهایی ببیند بر اساس `as_role` انجام می‌شود که از **query string** خوانده می‌شود (`inventory/views.py:1394`). یعنی کاربری که فقط مجوز «شمارشگر» دارد، با فراخوانی همان endpoint و ارسال `?as_role=tracking` وارد شاخه‌ای می‌شود که هیچ فیلتری روی کاربر ندارد و تمام تسک‌های همه انبارها را برمی‌گرداند (خطوط ۱۴۰۷–۱۴۱۱). با `?as_role=manager` هم تمام تسک‌های استخر مشترک قابل مشاهده‌اند.

کلاس مجوز `HasMenuAccess` این را نمی‌گیرد، چون فقط چک می‌کند «آیا کاربر یکی از آن چهار مجوز مشاهده را دارد؟» (خط ۱۳۷۶) و بعد اجازه می‌دهد `as_role` مسیر داده را تعیین کند. این یک IDOR کلاسیک است.

اصلاح درست: نقش موثر را از خود `request.user` استنتاج کنید و `as_role` را صرفاً به عنوان «فیلتر نمایشی» بپذیرید — یعنی اول مجموعه‌ی مجاز کاربر را بسازید، بعد در صورت درخواست، آن را تنگ‌تر کنید؛ هرگز گشادتر نه. شاخه‌ی `tracking` هم باید مجوز اختصاصی خودش را داشته باشد.

### ۲.۲ دو endpoint بدون احراز هویت، با خواندن مستقیم فایل از دیسک

در `ItemViewSet.get_permissions()` اکشن‌های `download_import_log` و `download_template` روی `AllowAny` تنظیم شده‌اند (`inventory/views.py:152-153`) و خود اکشن هم صراحتاً `permission_classes=[], authentication_classes=[]` دارد (`views.py:666`).

بدنه‌ی آن مسیر فایل را این‌طور می‌سازد:

```python
file_path = os.path.join(tempfile.gettempdir(), f"import_log_{import_id}.xlsx")
```

که `import_id` مستقیماً از query param می‌آید و هیچ اعتبارسنجی‌ای ندارد (`views.py:668-672`). یعنی هر کسی بدون لاگین می‌تواند لاگ ایمپورت دیگران را بردارد، و با مقداری مثل `../../...` می‌تواند هر فایل `xlsx.` روی سرور را بخواند. حتی اگر مسیر پیمایشی را ببندید، این endpoint باید احراز هویت داشته باشد و مالکیت `import_id` را بررسی کند.

### ۲.۳ WebSocket کاملاً باز است

`NotificationConsumer` هیچ بررسی احراز هویتی ندارد و همه را به یک گروه سراسری `global_notifications` اضافه می‌کند (`notifications/consumers.py:5-14`). هر کلاینت ناشناسی می‌تواند وصل شود و تمام اعلان‌های سیستم را ببیند. حداقل کار: رد کردن اتصال وقتی `self.scope["user"]` ناشناس است، و گروه‌بندی به‌ازای کاربر/انبار به‌جای یک گروه سراسری.

---

## ۳. امنیت (سایر موارد)

**تنظیمات، به‌صورت پیش‌فرضِ `startproject` رها شده‌اند.** در `config/settings.py`: کلید `SECRET_KEY` به‌صورت متن ساده در کد است (خط ۲۳)، `DEBUG = True` (خط ۲۶)، `ALLOWED_HOSTS = []` (خط ۲۸)، رمز دیتابیس داخل فایل (خط ۱۰۷) و `CORS_ALLOW_ALL_ORIGINS = True` (خط ۱۵۷). ترکیب `DEBUG=True` با CORS باز یعنی هر سایتی در شبکه می‌تواند به API درخواست بزند و هر خطا، stack trace کامل با مسیر فایل‌ها و بخشی از تنظیمات را برمی‌گرداند. این‌ها باید به متغیر محیطی (`django-environ` یا `os.environ`) منتقل شوند و `CORS_ALLOWED_ORIGINS` به دامنه‌ی فرانت محدود شود.

**رمز پیش‌فرض `123456` و رمزِ برابر با کد ملی.** در `accounts/serializers.py:76` رمز پیش‌فرض کاربر جدید `123456` است، در `accounts/views.py:108` بازنشانی مدیریتی هم همان را می‌گذارد، و در ایمپورت اکسل رمز اولیه **کد ملی فرد** است (`accounts/views.py:164`). کد ملی یک راز نیست — در سازمان معمولاً در دسترس چند واحد است و قابل حدس هم هست. اینکه `requires_password_change=True` گذاشته می‌شود خوب است، اما تا وقتی کاربر لاگین نکرده، حسابش با یک رمز حدس‌زدنی باز است. پیشنهاد: تولید رمز تصادفی و انتقال آن از کانال جداگانه، به‌علاوه‌ی محدود کردن پنجره‌ی اعتبار رمز اولیه.

**نگهداری توکن در `localStorage`.** در `core/auth/auth.service.ts:180-190` هر دو توکن access و refresh در `localStorage` (یا `sessionStorage`) ذخیره می‌شوند. این انتخاب رایج است و اگر برنامه داخلی و پشت VPN باشد قابل دفاع است، اما نتیجه‌اش این است که هر آسیب‌پذیری XSS در برنامه معادل سرقت کامل نشست می‌شود. ضمناً چون خروج فقط سمت کلاینت پاک می‌کند (`auth.service.ts:106-111`)، توکن refresh تا هفت روز معتبر می‌ماند. نکته‌ی جالب اینکه `token_blacklist` در `INSTALLED_APPS` **نصب شده** (`settings.py:45`) ولی استفاده نمی‌شود؛ پیاده‌سازی endpoint خروج با blacklist کردن refresh، کار یک ساعته است و بخش بزرگی از این ریسک را می‌بندد.

**طول عمر توکن و ناسازگاری با فرانت.** بک‌اند access token را یک روزه صادر می‌کند (`settings.py:176`) در حالی که `environment.ts:9` فرض کرده ۱۵ دقیقه است. توکن یک‌روزه با ذخیره در `localStorage` و بدون blacklist یعنی پنجره‌ی سوءاستفاده بسیار طولانی. پیشنهاد: access حدود ۱۵–۳۰ دقیقه، refresh چرخشی (`ROTATE_REFRESH_TOKENS`).

**کاربران ساختگی با رمز داخل باندل فرانت.** آبجکت `mockUsers` در `auth.service.ts:41-82` شامل پنج کاربر با رمز `123456` و لیست کامل مجوزهای سیستم است. چون `useMockData` یک flag زمان‌اجراست و نه شرط کامپایل، این داده در باندل production هم می‌نشیند و عملاً نقشه‌ی کامل مدل مجوزهای شما را در اختیار هر کسی می‌گذارد که فایل JS را باز کند. این باید به یک فایل جدا در `environments/` منتقل شود که فقط در build توسعه جایگزین شود.

**`isLoggedIn` از روی داده‌ی قابل دستکاری محاسبه می‌شود.** `isLoggedIn` صرفاً یعنی «آبجکت کاربر در `localStorage` وجود دارد» (`auth.service.ts:24,29`) و `hasPermission` هم آرایه‌ی permissions ذخیره‌شده در همان‌جا را می‌خواند (خط ۱۴۴). کاربر می‌تواند این آبجکت را دستی ویرایش کند و منوهای قفل را باز کند. تا وقتی سرور همه چیز را دوباره چک کند این فقط یک مشکل ظاهری است — و دقیقاً به همین دلیل رفع مورد ۲.۱ اهمیت دارد.

**فایل‌های حساس در ریشه‌ی پروژه.** `warehouse-backend/` پر از خروجی‌های دیباگ است: `users.json`، `user_dump.json`، `users_debug.json`، `perms.json`، `out.txt`، `recovered.txt`، `error.html`، `db.sqlite3` و اسکریپت‌های موقت `check_user.py` / `create_users.py` / `extract.py`. `.gitignore` فعلی هیچ‌کدام (جز `db.sqlite3`) را نمی‌گیرد. اگر این‌ها شامل داده‌ی واقعی کاربران یا هش رمز باشند، در تاریخچه‌ی git ماندگار شده‌اند. این‌ها باید حذف و به `.gitignore` اضافه شوند.

**نکات مثبت امنیتی که باید به حساب بیایند:** `django-axes` با محدودیت پنج تلاش و قفل پانزده‌دقیقه‌ای روی ترکیب username و IP تنظیم شده (`settings.py:186-189`) — این بیش از چیزی است که معمولاً در پروژه‌های داخلی دیده می‌شود. `CustomJWTAuthentication` توکن‌های صادرشده پیش از تغییر رمز را رد می‌کند (`accounts/authentication.py:10-17`) که یک الگوی درست و کم‌ادعا است. مجوز پیش‌فرض DRF روی `IsAuthenticated` است (`settings.py:163-165`) یعنی مدل «امن به‌صورت پیش‌فرض» رعایت شده و استثناها صریح‌اند.

---

## ۴. همزمانی و صحت داده

این بخش برای یک سیستم انبارگردانی از امنیت هم مهم‌تر است، چون خروجی کار شما یک عدد موجودی است که باید قابل دفاع باشد.

در کل بک‌اند **حتی یک `select_for_update` وجود ندارد**. چند نقطه‌ی مشخص:

**برداشتن تسک از استخر (`claim_tasks`، `views.py:1445-1478`).** ابتدا فهرست تسک‌های آزاد خوانده می‌شود (خط ۱۴۵۸) و سپس در دستور جداگانه‌ای `counter` روی آنها ست می‌شود (خط ۱۴۶۶). فاصله‌ی بین این دو، یک پنجره‌ی TOCTOU است: دو انبارگردان که همزمان روی «برداشتن» کلیک کنند، هر دو موفق می‌شوند و نفر دوم مالکیت نفر اول را بازنویسی می‌کند. رفع آن ساده است — شرط `counter__isnull=True` را داخل خودِ `update()` بیاورید و از مقدار بازگشتی برای گزارش تعداد واقعی استفاده کنید.

**تایید گروهی مدیر (`bulk_manager_approve`، `views.py:1580` به بعد).** داخل `transaction.atomic` هست (خط ۱۵۹۴) که خوب است، اما تراکنش بدون قفل ردیف مانع اجرای همزمان دو تایید روی یک تسک نمی‌شود. چون این اکشن `item.balance` را به‌روزرسانی می‌کند، نتیجه‌ی رقابت، عدد موجودی اشتباه است — دقیقاً همان چیزی که کل سیستم برای جلوگیری از آن ساخته شده.

**نبود ماشین وضعیت.** `CountTask.status` به‌درستی `choices` دارد (`inventory/models.py:174-188`) ولی هیچ‌جا تعریف نشده که از هر وضعیت به کدام وضعیت‌ها می‌شود رفت. قوانین گذار در بدنه‌ی اکشن‌های مختلف `views.py` پخش شده‌اند. نتیجه: هر مسیر جدیدی که اضافه شود باید همه‌ی قوانین را از نو تکرار کند و احتمال جا افتادن یکی از آنها بالاست. یک متد `can_transition_to()` روی مدل، بیست خط کد است و این کلاس مشکلات را برای همیشه می‌بندد.

**ثبت تاریخچه بعد از تغییر وضعیت انجام می‌شود** (`views.py:1490-1507`)، پس اگر بین این دو خطایی رخ دهد، تسک جابه‌جا شده ولی رد پایی از آن نمانده. چون هر دو باید در یک تراکنش باشند، این هم با همان `atomic` حل می‌شود.

**`on_delete=CASCADE` روی کاربر.** در `CountTask` هر سه فیلد `counter`، `supervisor` و `assigned_manager` با `CASCADE` به کاربر وصل‌اند (`models.py:184-186`). یعنی حذف یک کاربر، تمام تسک‌های شمارش او را همراه با تاریخچه‌شان پاک می‌کند. برای سیستمی که ادعای audit trail دارد این یک باگ جدی است؛ باید `PROTECT` یا `SET_NULL` باشد. جالب اینکه در همان مدل `created_by`/`modified_by` درست و با `SET_NULL` تعریف شده‌اند — پس این یک بی‌دقتی موضعی است نه یک تصمیم.

**ارجاع با رشته به‌جای کلید خارجی.** `Item.field_assignee` و `doc_assignee` نام کامل کاربر را به‌صورت رشته نگه می‌دارند (`models.py:101-102` و مقداردهی در `views.py:1469-1470`). با تغییر نام خانوادگی یا وجود دو هم‌نام، این داده بی‌معنا می‌شود.

---

## ۵. معماری و کیفیت کد

### بک‌اند

اندازه‌ها گویا هستند: `inventory/views.py` حدود **۲۰۲۴ خط** است، در حالی که `models.py` همان اپ ۲۶۹ خط و `serializers.py` آن ۱۳۹ خط است. یعنی تقریباً تمام منطق کسب‌وکار داخل view نشسته. فقط اکشن `import_excel` نزدیک ۴۰۰ خط است و هم‌زمان مسئول پارس فایل، اعتبارسنجی، حل تعارض، نگاشت فیلد پویا، ثبت تاریخچه و استریم لاگ است.

این وضعیت خودش «غلط» نیست — Django به شما اجازه می‌دهد این‌طور بنویسید و برای رسیدن سریع به نتیجه جواب داده. اما هزینه‌اش را همین حالا می‌پردازید: هیچ‌کدام از این منطق‌ها بدون بالا آوردن یک درخواست HTTP قابل تست نیستند، و هر تغییر در قوانین شمارش یعنی جراحی در یک فایل دوهزارخطی. جالب است که در `warehouses/services.py` یک لایه سرویس (هرچند کوچک، ۴۴ خط) شروع شده — همان الگو را در `inventory` هم دنبال کنید: یک `services/count_workflow.py` و یک `services/excel_import.py` بیشترِ این حجم را جذب می‌کنند.

مورد دیگر، تکرار ساختاری بین `CountTask` و `DocTask` است. این دو تقریباً آینه‌ی هم‌اند (وضعیت‌ها، نقش‌ها، تاریخچه، اکشن‌های bulk) و در `views.py` هم دو بلوک کاملاً موازی دارند. اگر روزی نقش سومی اضافه شود، سه نسخه از یک منطق خواهید داشت. یک مدل انتزاعی مشترک (`AbstractTask`) این را جمع می‌کند.

سه فایل ابزار اکسل تقریباً موازی وجود دارد: `accounts/excel_utils.py` (۲۷۱ خط)، `accounts/roles_excel_utils.py` (۲۳۸ خط) و `warehouses/excel_utils.py` (۲۳۲ خط) — مجموعاً ۷۴۱ خط که بخش زیادی از آن الگوی تکراری ساخت worksheet و استایل است.

در `ItemViewSet` هم `get_queryset()` فقط `super()` را صدا می‌زند (`views.py:145-146`) بدون هیچ `select_related`، در حالی که سریالایزر برای هر ردیف نام سازنده و ویرایش‌کننده را می‌خواند. روی صفحه‌ای با ۱۰۰ ردیف این یعنی حدود ۲۰۰ کوئری اضافی. در `CountTaskViewSet` این درست انجام شده (خط ۱۳۹۲) — پس الگویش را می‌دانید، فقط یک‌جا اعمال نشده.

### فرانت‌اند

پشته مدرن است: Angular 22، standalone components، سیگنال‌ها، interceptorهای تابعی، Tailwind، Vitest. برای پروژه‌ای که نسخه‌ی قبلی‌اش وانیلا JS بوده (`Archive_Docs/warehouse-old`)، این جهش بزرگی است.

مشکل اصلی، **سه لایه‌ی موازی برای یک کار** است. هم‌زمان `src/app/services/`، `src/app/core/api/` و `src/app/core/http/` وجود دارند؛ دو نسخه از `auth.service.ts`، دو نسخه از `auth.guard.ts`. نسخه‌های زیر `src/app/services/` و `src/app/guards/` صرفاً re-export و علامت‌گذاری‌شده با `@deprecated` هستند و مسیرها از نسخه‌ی `core/` استفاده می‌کنند — پس عملاً کد مرده‌اند. اما تا وقتی حذف نشوند، هر توسعه‌دهنده‌ی جدید (و هر ابزار auto-import) با احتمال پنجاه درصد نسخه‌ی اشتباه را وارد می‌کند. حذفشان یک کامیت است.

نمونه‌ی جدی‌تر همین مسئله، وضعیت state است: `services/state.service.ts` یک آبجکت ساده‌ی قابل تغییر است و `core/stores/auth.store.ts` سیگنال‌محور — و هر دو مفهوم «انبار فعال» را نگه می‌دارند. کامپوننت‌های مختلف از منابع مختلف می‌خوانند. تا وقتی یکی از این دو حذف نشود، باگ‌های «چرا انبار عوض نشد؟» تمام‌نشدنی خواهند بود.

اندازه‌ی کامپوننت‌ها هم قابل توجه است. مجموع پوشه‌ی `components` حدود **۱۱٬۰۰۰ خط** در ۵۹ فایل است، با این سرشاخه‌ها: `dispatch.ts` با ۸۸۷ خط، `label-designer.ts` با ۸۶۸ خط، `users.ts` با ۵۱۷ خط، `counter-dashboard.ts` با ۳۷۱ خط. طراح لیبل ذاتاً پیچیده است و بزرگ بودنش عجیب نیست، اما `dispatch` و `users` عمدتاً به این دلیل بزرگ شده‌اند که فیلتر و مرتب‌سازی و تبدیل داده داخل خودشان انجام می‌شود. انتقال این‌ها به یک facade service هم فایل را نصف می‌کند و هم برای اولین بار قابل تست می‌شودشان.

نکات کوچک‌تر: ۲۳۸ مورد `any` در ۳۶ فایل (متمرکزترین‌ها: `users.ts` با ۳۷، `dispatch.ts` با ۲۹، `data-table.component.ts` با ۲۵) که عملاً type-safety را در پرمخاطره‌ترین بخش‌ها خنثی می‌کند؛ ۱۶ فراخوانی `console` در ۱۰ فایل که باید پشت `environment.production` بروند؛ و دو فایل بی‌ربط `script.js` و `script.py` داخل پوشه‌ی کامپوننت `wh-settings`.

یک نکته‌ی عملی: `puppeteer` در **dependencies** (نه devDependencies) اعلام شده اما در هیچ‌جای `src/` استفاده نشده. این حدود ۳۰۰ مگابایت به نصب پروژه اضافه می‌کند بدون هیچ فایده‌ای. حذفش بی‌خطر است.

---

## ۶. محصول و تجربه کاربری

از منظر محصول، این پروژه چیزهای درستی را هدف گرفته. مواردی که مشخصاً خوب طراحی شده‌اند:

چرخه‌ی سه‌مرحله‌ای با امکان «رد و ارجاع برای بازشماری» و ثبت یادداشت در هر مرحله، به‌علاوه‌ی `skip_supervisor` برای اقلام کم‌ریسک — این یعنی کسی که سیستم را طراحی کرده، فرآیند واقعی انبارگردانی را دیده است. مفهوم «استخر مشترک» در کنار «تخصیص مستقیم به مدیر» هم راه‌حل پخته‌ای برای مسئله‌ی تخصیص کار است. محاسبه‌ی مدت توقف در هر مرحله در داشبورد پیگیری، دقیقاً همان عددی است که مدیر عملیات برای پیدا کردن گلوگاه لازم دارد. فیلدهای پویا به‌ازای هر انبار (`ItemFieldDefinition`) هم انتخاب هوشمندانه‌ای است که جلوی درخواست‌های بی‌پایان «یک ستون هم اضافه کن» را می‌گیرد.

آنچه از منظر محصول کم است:

**شمارش کور (blind counting) به‌صورت واقعی.** در مستندات پروژه به آن اشاره شده، اما چون `ItemViewSet` موجودی سیستمی را در همان payload برمی‌گرداند و صرفاً UI آن را نشان نمی‌دهد، هر انبارگردانی با باز کردن Network tab می‌تواند عدد مورد انتظار را ببیند. اگر کورکورانه بودن شمارش برای شما یک الزام حسابرسی است، باید در سطح سریالایزر اعمال شود نه در UI.

**پشتیبانی از بارکد/QR.** برای انبارگردانی روی تبلت، این تنها موردی است که بیشترین اثر را روی سرعت و دقت واقعی دارد و هنوز وجود ندارد. زیرساخت لازمش (کد یکتای کالا) از قبل هست.

**کار آفلاین.** انبارها معمولاً Wi-Fi ضعیفی دارند. الان اگر ارتباط قطع شود، شمارش وارد‌شده از دست می‌رود. یک بافر ساده در IndexedDB با ارسال دسته‌ای، تفاوت محسوسی در تجربه‌ی میدانی ایجاد می‌کند.

**اعلان‌ها.** زیرساخت WebSocket هست، اما یک گروه سراسری است؛ یعنی سرپرست اعلان مربوط به دیگران را هم می‌گیرد. با گروه‌بندی به‌ازای کاربر، این به یک قابلیت واقعی تبدیل می‌شود.

**دسترس‌پذیری و RTL.** رابط RTL است و متن‌ها فارسی، اما همه‌ی رشته‌ها مستقیماً در تمپلیت‌ها hardcode شده‌اند. اگر هرگز نیاز به نسخه‌ی انگلیسی نداشته باشید این تصمیم درستی است؛ فقط بدانید که برگشت از آن گران است.

---

## ۷. آمادگی برای تولید

**تست عملاً وجود ندارد.** هر سه فایل `tests.py` در بک‌اند دقیقاً سه خط و خالی‌اند. در فرانت، تمام فایل‌های `spec.ts.` همان اسکلت پیش‌فرض `toBeTruthy()` هستند. برای سیستمی که خروجی‌اش عدد موجودی انبار است، این پرخطرترین نقطه‌ی پروژه است. لازم نیست به پوشش صد درصد فکر کنید؛ ده تا تست روی این موارد، بیشتر ارزش دارد تا صد تست جای دیگر: گذار کامل شمارشگر→سرپرست→مدیر، رد شدن از هر مرحله، سقف تعداد بازشماری، برداشتن همزمان یک تسک توسط دو کاربر، و ایمپورت اکسل با کد تکراری.

**لاگ‌برداری وجود ندارد.** در `settings.py` هیچ بخش `LOGGING` نیست. با `DEBUG=False` در تولید، خطاها فقط به stderr می‌روند و عملاً گم می‌شوند. حداقل: یک handler فایل با چرخش، و ثبت جداگانه‌ی رخدادهای امنیتی (لاگین ناموفق، تغییر مجوز).

**پیکربندی کانال برای تولید مناسب نیست.** `CHANNEL_LAYERS` روی `InMemoryChannelLayer` است (`settings.py:93-97`) که خودِ کامنت کد هم می‌گوید «برای توسعه‌ی محلی». اما `channels-redis` در `requirements.txt` هست و Redis در `docker-compose.yml` بالا می‌آید. یعنی زیرساختش آماده است و فقط وصل نشده. با بیش از یک worker، اعلان‌ها به‌صورت تصادفی به بعضی کاربران نمی‌رسند.

**آدرس API در build تولید اشتباه است.** `environment.ts` که همان فایل production است، `apiUrl` را روی `http://localhost:8000/api` گذاشته (خط ۷). این باید دامنه‌ی واقعی و روی HTTPS باشد، وگرنه build تولید روی هیچ ماشینی جز خود سرور کار نمی‌کند.

**نبود مسیر استقرار.** `docker-compose.yml` فقط دیتابیس و Redis را بالا می‌آورد؛ خود اپلیکیشن نه. فایل `cmd.txt` جای مستندات راه‌اندازی را گرفته. `README` معنادار، تنظیمات سرو فایل استاتیک و مدیا، و یک سرویس systemd یا کانتینر برای daphne هنوز لازم است.

**بدون rate limiting در سطح API.** `axes` فقط لاگین را محافظت می‌کند. endpointهای سنگین مثل `import_excel` یا `export_excel` هیچ محدودیتی ندارند؛ `DEFAULT_THROTTLE_CLASSES` در DRF این را ارزان حل می‌کند.

---

## ۸. نقشه راه پیشنهادی

**فوری — پیش از هر استقراری (۲ تا ۳ روز)**

اول سه مورد بخش ۲ را ببندید: فیلتر `as_role` را سمت سرور اجباری کنید، `AllowAny` را از دو endpoint دانلود بردارید و `import_id` را اعتبارسنجی کنید، و WebSocket را احراز هویت‌دار کنید. سپس `SECRET_KEY`، `DEBUG`، `ALLOWED_HOSTS`، رمز دیتابیس و `CORS` را به متغیر محیطی منتقل کنید. فایل‌های دیباگ و dump را از مخزن پاک و به `.gitignore` اضافه کنید. `mockUsers` را از باندل تولید خارج کنید.

**کوتاه‌مدت (۱ تا ۲ هفته)**

قفل‌گذاری همزمانی: `select_for_update` در `bulk_manager_approve` و اصلاح شرط رقابتی `claim_tasks`. یک ماشین وضعیت روی `CountTask` تعریف کنید و همه‌ی گذارها را از آن رد کنید. `on_delete` کاربران را به `PROTECT`/`SET_NULL` تغییر دهید. endpoint خروج با blacklist و کوتاه کردن عمر access token. ده تست حیاتی بالا را بنویسید. `LOGGING` و `Redis channel layer` و `apiUrl` تولید را تنظیم کنید. فایل‌های تکراری فرانت (`services/auth.service.ts`، `guards/auth.guard.ts`) را حذف و `StateService` را در `AuthStore` ادغام کنید. `puppeteer` را از dependencies بردارید.

**میان‌مدت (۱ تا ۲ ماه)**

استخراج لایه سرویس در `inventory` و شکستن `views.py` به بخش‌های زیر ۴۰۰ خط. یکپارچه‌سازی `CountTask` و `DocTask` روی یک مدل انتزاعی. ادغام سه فایل ابزار اکسل. `select_related` در `ItemViewSet` و افزودن ایندکس روی `status` و `item`. کاهش تدریجی `any`ها با تعریف مدل‌های TypeScript واقعی برای پاسخ‌های API. شکستن `dispatch.ts` و `users.ts` به facade service. سپس می‌توانید سراغ قابلیت‌های محصول بروید: بارکدخوان، بافر آفلاین، و گزارش‌های تحلیلی عملکرد که در چشم‌انداز پروژه آمده بود.

---

## ۹. حرف آخر

اگر بخواهم پروژه را در یک جمله توصیف کنم: **کاری که ساخته شده از کاری که محافظت شده جلوتر است.** این الگوی طبیعی پروژه‌هایی است که با فشار زمانی و تمرکز بر «کار کردن» پیش رفته‌اند، و اتفاقاً به این معناست که سخت‌ترین بخش — فهمیدن دامنه‌ی مسئله و مدل کردن درست آن — پشت سر گذاشته شده. مدل داده‌ی شما درست است؛ تاریخچه از ابتدا دیده شده؛ چرخه‌ی کاری واقع‌بینانه است. این‌ها چیزهایی هستند که اگر غلط بودند، اصلاحشان ماه‌ها طول می‌کشید.

آنچه مانده — قفل، مجوز سمت سرور، تنظیمات محیط، تست — کارهای مکانیکی و قابل شمارش‌اند. با دو هفته کار متمرکز روی بخش «فوری» و «کوتاه‌مدت»، این سیستم به جایی می‌رسد که بشود با اطمینان روی داده‌ی واقعی سازمان اجرایش کرد.

</div>
